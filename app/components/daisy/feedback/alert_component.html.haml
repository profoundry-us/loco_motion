= part(:component) do
  - # If we have an icon, assume we want to wrap the content
  - if @icon.present?
    = render_icon
    = part(:content_wrapper) do
      = content
  - else
    = content
  - if closable?
    = part(:close) do
      = loco_icon "x-mark", css: "where:size-4"
