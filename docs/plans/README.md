# Implementation Plans


This directory holds implementation plans for LocoMotion work. Active and
recently-relevant plans live at the top level; completed plans for shipped work
are moved into [`archive/`](archive/) to keep the active set easy to scan. When
a plan's work has shipped, move its file into `archive/`.
