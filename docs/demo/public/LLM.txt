# LocoMotion Component Library

This file provides comprehensive documentation for all LocoMotion components.
LocoMotion is a Ruby on Rails component library built on ViewComponent and DaisyUI.

## Available Components

- **Hero::IconComponent** (Hero): Heroicons are a set of free, MIT-licensed high-quality SVG icons for you to  us...
  API: /api-docs
  Examples: /examples/Hero::IconComponent

- **Daisy::Actions::ButtonComponent** (Actions):  Here are some examples showcasing button. 
  API: /api-docs
  Examples: /examples/Daisy::Actions::ButtonComponent

- **Daisy::Actions::DropdownComponent** (Actions): Dropdowns are a great way to display a list of items in a compact manner. 
  API: /api-docs
  Examples: /examples/Daisy::Actions::DropdownComponent

- **Daisy::Actions::ModalComponent** (Actions): Modals are used to display content that requires user interaction. They are typi...
  API: /api-docs
  Examples: /examples/Daisy::Actions::ModalComponent

- **Daisy::Actions::SwapComponent** (Actions): The Swap component allows you to quickly swap between two icons, images, or  HT...
  API: /api-docs
  Examples: /examples/Daisy::Actions::SwapComponent

- **Daisy::Actions::ThemeControllerComponent** (Actions): doc_note(modifier: :tip) do The `ThemeController` component is a **_Builder_** c...
  API: /api-docs
  Examples: /examples/Daisy::Actions::ThemeControllerComponent

- **Daisy::DataDisplay::AccordionComponent** (DataDisplay): Here are some examples showcasing accordions. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::AccordionComponent

- **Daisy::DataDisplay::AvatarComponent** (DataDisplay): Here are some simple avatar examples. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::AvatarComponent

- **Daisy::DataDisplay::BadgeComponent** (DataDisplay): Here are some examples showcasing badges. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::BadgeComponent

- **Daisy::DataDisplay::CardComponent** (DataDisplay): Here are some examples showcasing cards. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::CardComponent

- **Daisy::DataDisplay::CarouselComponent** (DataDisplay): Carousels are great for showing off a lot of images! 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::CarouselComponent

- **Daisy::DataDisplay::ChatComponent** (DataDisplay):  Here are some examples showcasing chat bubbles. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::ChatComponent

- **Daisy::DataDisplay::CollapseComponent** (DataDisplay): Here are some simple collapse examples. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::CollapseComponent

- **Daisy::DataDisplay::CountdownComponent** (DataDisplay):  Countdowns display an animated countdown to a specific date. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::CountdownComponent

- **Daisy::DataDisplay::DiffComponent** (DataDisplay): Diffs allow you to move a slider back and forth to compare two images,  text, o...
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::DiffComponent

- **Daisy::DataDisplay::FigureComponent** (DataDisplay): Here are some examples showcasing figures. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::FigureComponent

- **Daisy::DataDisplay::KbdComponent** (DataDisplay): The keyboard component is used to visually represent a key on a keyboard. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::KbdComponent

- **Daisy::DataDisplay::ListComponent** (DataDisplay): Lists provide a vertical layout to display information in rows. They can contain...
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::ListComponent

- **Daisy::DataDisplay::StatComponent** (DataDisplay): doc_note do Stats have a transparent background by default. Use `bg-base-100` if...
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::StatComponent

- **Daisy::DataDisplay::StatusComponent** (DataDisplay): Status is a really small icon to visually show the current status of an element,...
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::StatusComponent

- **Daisy::DataDisplay::TableComponent** (DataDisplay): Tables are designed to show data in a tabular layout making it easy to see  lar...
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::TableComponent

- **Daisy::DataDisplay::TimelineComponent** (DataDisplay):  Here are some examples showcasing timelines. 
  API: /api-docs
  Examples: /examples/Daisy::DataDisplay::TimelineComponent

- **Daisy::DataInput::CallyComponent** (DataInput): doc_note(modifier: :warning) do You must include the Cally script tag or <a targ...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::CallyComponent

- **Daisy::DataInput::CallyInputComponent** (DataInput): This component provides an elegant alternative to standard HTML date inputs  wh...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::CallyInputComponent

- **Daisy::DataInput::CheckboxComponent** (DataInput): Checkboxes are great for toggling an item on / off, or selecting multiple items...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::CheckboxComponent

- **Daisy::DataInput::FileInputComponent** (DataInput): Here are some examples showcasing file input components. 
  API: /api-docs
  Examples: /examples/Daisy::DataInput::FileInputComponent

- **Daisy::DataInput::FieldsetComponent** (DataInput): Fieldsets group related form controls and can optionally include a legend. 
  API: /api-docs
  Examples: /examples/Daisy::DataInput::FieldsetComponent

- **Daisy::DataInput::LabelComponent** (DataInput): Labels provide a useful description for various kinds of inputs. 
  API: /api-docs
  Examples: /examples/Daisy::DataInput::LabelComponent

- **Daisy::DataInput::RadioButtonComponent** (DataInput): Here are some examples showcasing radio button components. 
  API: /api-docs
  Examples: /examples/Daisy::DataInput::RadioButtonComponent

- **Daisy::DataInput::RangeComponent** (DataInput): Here are some examples showcasing range input components. 
  API: /api-docs
  Examples: /examples/Daisy::DataInput::RangeComponent

- **Daisy::DataInput::RatingComponent** (DataInput): Here are some examples showcasing rating input components. 
  API: /api-docs
  Examples: /examples/Daisy::DataInput::RatingComponent

- **Daisy::DataInput::SelectComponent** (DataInput): doc_note do Select inputs have a border by default and a width of 20rem. Use `se...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::SelectComponent

- **Daisy::DataInput::TextInputComponent** (DataInput): Text inputs allow you to enter various forms of text such as usernames, password...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::TextInputComponent

- **Daisy::DataInput::TextAreaComponent** (DataInput): doc_note do Text areas have a border by default. Use `textarea-ghost` to remove...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::TextAreaComponent

- **Daisy::DataInput::ToggleComponent** (DataInput): The Toggle component is a wrapper around the  component_link("Checkbox", "Daisy...
  API: /api-docs
  Examples: /examples/Daisy::DataInput::ToggleComponent

- **Daisy::Navigation::BreadcrumbsComponent** (Navigation): Breadcrumbs are used to show the user's location within a website or app. 
  API: /api-docs
  Examples: /examples/Daisy::Navigation::BreadcrumbsComponent

- **Daisy::Navigation::DockComponent** (Navigation): The Dock component provides a navigation bar fixed to the bottom of the screen,...
  API: /api-docs
  Examples: /examples/Daisy::Navigation::DockComponent

- **Daisy::Navigation::LinkComponent** (Navigation): Tailwind removes the styling on standard links so we provide an easy way to  re...
  API: /api-docs
  Examples: /examples/Daisy::Navigation::LinkComponent

- **Daisy::Navigation::MenuComponent** (Navigation): doc_note do Vertical menus no longer have a default width. Use `w-full` or other...
  API: /api-docs
  Examples: /examples/Daisy::Navigation::MenuComponent

- **Daisy::Navigation::NavbarComponent** (Navigation):  Navbars are a common way to organize navigation links at the top of a page. 
  API: /api-docs
  Examples: /examples/Daisy::Navigation::NavbarComponent

- **Daisy::Navigation::PaginationComponent** (Navigation): Pagination can be implemented by utilizing the existing  component_link("Join",...
  API: /api-docs
  Examples: /examples/Daisy::Navigation::PaginationComponent

- **Daisy::Navigation::StepsComponent** (Navigation): Steps allow you to create a series of steps that users can follow to  complete...
  API: /api-docs
  Examples: /examples/Daisy::Navigation::StepsComponent

- **Daisy::Navigation::TabsComponent** (Navigation): Here are some examples showcasing tabs. 
  API: /api-docs
  Examples: /examples/Daisy::Navigation::TabsComponent

- **Daisy::Feedback::AlertComponent** (Feedback):  Alerts inform the user about important information or changes to the system. 
  API: /api-docs
  Examples: /examples/Daisy::Feedback::AlertComponent

- **Daisy::Feedback::LoadingComponent** (Feedback):  Loaders are used to indicate that a process is happening in the background. 
  API: /api-docs
  Examples: /examples/Daisy::Feedback::LoadingComponent

- **Daisy::Feedback::ProgressComponent** (Feedback):  Progress bars are used to indicate the progress of a process. 
  API: /api-docs
  Examples: /examples/Daisy::Feedback::ProgressComponent

- **Daisy::Feedback::RadialProgressComponent** (Feedback): Radial progress bars are used to indicate the progress of a process, in a  circ...
  API: /api-docs
  Examples: /examples/Daisy::Feedback::RadialProgressComponent

- **Daisy::Feedback::SkeletonComponent** (Feedback): Skeletons are used to show a placeholder for content that is loading or  being...
  API: /api-docs
  Examples: /examples/Daisy::Feedback::SkeletonComponent

- **Daisy::Feedback::ToastComponent** (Feedback): Toasts are used to show a message to the user that is not critical, but  import...
  API: /api-docs
  Examples: /examples/Daisy::Feedback::ToastComponent

- **Daisy::Feedback::TooltipComponent** (Feedback): Additionally, we offer the `daisy_tooltip` component to wrap an entire component...
  API: /api-docs
  Examples: /examples/Daisy::Feedback::TooltipComponent

- **Daisy::Layout::DividerComponent** (Layout): Dividers are used to separate content in a meaningful way. They can be used to s...
  API: /api-docs
  Examples: /examples/Daisy::Layout::DividerComponent

- **Daisy::Layout::DrawerComponent** (Layout): doc_note(css: "mb-8 max-w-2xl") do The `relative / absolute` and the `h-48` clas...
  API: /api-docs
  Examples: /examples/Daisy::Layout::DrawerComponent

- **Daisy::Layout::FooterComponent** (Layout): Footers are used to provide additional information about a page or site. They ar...
  API: /api-docs
  Examples: /examples/Daisy::Layout::FooterComponent

- **Daisy::Layout::HeroComponent** (Layout): Heroes are used to provide a large, eye-catching introduction to a page or site....
  API: /api-docs
  Examples: /examples/Daisy::Layout::HeroComponent

- **Daisy::Layout::IndicatorComponent** (Layout): Indicators wrap other elements and provide a visual cue that something requires...
  API: /api-docs
  Examples: /examples/Daisy::Layout::IndicatorComponent

- **Daisy::Layout::JoinComponent** (Layout): Joins allow you to combine multiple components into a singular, joined,  elemen...
  API: /api-docs
  Examples: /examples/Daisy::Layout::JoinComponent

- **Daisy::Layout::MaskComponent** (Layout): Masks allow you to create a variety of shapes and patterns! Masks are **NOT** a...
  API: /api-docs
  Examples: /examples/Daisy::Layout::MaskComponent

- **Daisy::Layout::StackComponent** (Layout): Stacks provide a 3 dimensional effect to your content. This can be helpful for d...
  API: /api-docs
  Examples: /examples/Daisy::Layout::StackComponent

- **Daisy::Mockup::BrowserComponent** (Mockup): The Browser component is a simple mockup that looks and feels like a user's  we...
  API: /api-docs
  Examples: /examples/Daisy::Mockup::BrowserComponent

- **Daisy::Mockup::CodeComponent** (Mockup): Code blocks are a great way to display code snippets in your site or  documenta...
  API: /api-docs
  Examples: /examples/Daisy::Mockup::CodeComponent

- **Daisy::Mockup::DeviceComponent** (Mockup): Device mockups are a great way to showcase your designs in a realistic way! 
  API: /api-docs
  Examples: /examples/Daisy::Mockup::DeviceComponent

- **Daisy::Mockup::FrameComponent** (Mockup): Frames showcase your work as if they were displayed in a window. This is  very...
  API: /api-docs
  Examples: /examples/Daisy::Mockup::FrameComponent

---

## Component Details

=== Component: Hero::IconComponent
Group: Hero
Title: Icons
API URL: /api-docs
Examples URL: /examples/Hero::IconComponent
File: /home/loco_demo/app/views/examples/hero/icons.html.haml

Description:
 Heroicons are a set of free, MIT-licensed high-quality SVG icons for you to  use in your web projects. You can find them at  succeed "." do daisy_link("https://heroicons.com", target: "_blank", right_icon: "arrow-top-right-on-square", right_icon_css: "size-4") 

Helpers:
- hero_icon

Examples:

-- Example: Basic Icons
Description:
By default, icons are displayed with the `size-5` Tailwind class to set the width and height. This can be overridden **without** using the `!` Tailwind modifier because we utilize the `:where()` pseudo-class to ensure that our default classes have the lowest CSS specificity possible. The icon color will inherit the text color of the parent element. Additionally, if it's more convenient for your purposes, you can pass the icon as a keyword argument using the `icon:` option instead of the first positional argument.

Code (HAML):
```haml
= doc_example(title: "Basic Icons", example_css: "gap-x-2") do |doc|
  - doc.with_description do
    By default, icons are displayed with the `size-5` Tailwind class to set the
width and height. This can be overridden **without** using the
`!` Tailwind modifier because we utilize the `:where()` pseudo-class to
ensure that our default classes have the lowest CSS specificity possible.

The icon color will inherit the text color of the parent element.

Additionally, if it's more convenient for your purposes, you can pass the
icon as a keyword argument using the `icon:` option instead of the first
positional argument.



  = hero_icon("academic-cap")

  = hero_icon(icon: "adjustments-horizontal")

  %span.text-blue-500
    = hero_icon("archive-box")
```

URL: /examples/Hero::IconComponent#basic-icons

-- Example: Customized Icons
Description:
You can customize the size, color, and other properties of the icons using standard Tailwind classes.

Code (HAML):
```haml
= doc_example(title: "Customized Icons", example_css: "gap-x-2") do |doc|
  - doc.with_description do
    %p
      You can customize the size, color, and other properties of the icons using

      standard Tailwind classes.

  = hero_icon("no-symbol", css: "size-4 text-red-600")

  = hero_icon("arrow-trending-up", css: "size-10 text-green-600")

  = hero_icon("exclamation-triangle", css: "size-14 text-yellow-400 animate-pulse")
```

URL: /examples/Hero::IconComponent#customized-icons

---

=== Component: Daisy::Actions::ButtonComponent
Group: Actions
Title: Buttons
API URL: /api-docs
Examples URL: /examples/Daisy::Actions::ButtonComponent
File: /home/loco_demo/app/views/examples/daisy/actions/buttons.html.haml

Description:
 Here are some examples showcasing button. 

Helpers:
- daisy_button

Examples:

-- Example: Basic Buttons
Description:
Buttons are used to trigger actions. They can be used to submit forms, navigate to other pages, or trigger any other action. And they come in various styles and colors.

Code (HAML):
```haml
= doc_example(title: "Basic Buttons") do |doc|
  - doc.with_description do
    %p
      Buttons are used to trigger actions. They can be used to submit forms,

      navigate to other pages, or trigger any other action. And they come in

      various styles and colors.

  .my-2.flex.flex-col.lg:flex-row.gap-4
    = daisy_button do
      Plain Button

    = daisy_button(css: "btn-primary") do
      Primary Button

    = daisy_button(css: "btn-secondary") do
      Secondary Button

    = daisy_button(css: "btn-accent") do
      Accent Button

    = daisy_button(css: "btn-ghost") do
      Ghost Button

    = daisy_button(css: "btn-link") do
      Link Button
```

URL: /examples/Daisy::Actions::ButtonComponent#basic-buttons

-- Example: Icon Buttons
Description:
Buttons can also have an icon on the left or right side.

Code (HAML):
```haml
= doc_example(title: "Icon Buttons") do |doc|
  - doc.with_description do
    %p Buttons can also have an icon on the left or right side.

  .flex.flex-col.md:flex-row.items-center.justify-center.gap-4
    = daisy_button(icon: "plus", css: "btn-circle")

    = daisy_button(icon: "minus", css: "btn-circle")

    = daisy_button(css: "hover:btn-secondary", icon: "heart", title: "Favorites")

    = daisy_button(css: "btn-accent", left_icon: "cloud", right_icon: "folder", title: "Cloud Files")

    = daisy_button(css: "btn-primary btn-lg", right_icon: "chevron-right", right_icon_css: "size-8", title: "Go")
```

URL: /examples/Daisy::Actions::ButtonComponent#icon-buttons

-- Example: Multiple Sizes
Description:
You can also use various sizes.

Code (HAML):
```haml
= doc_example(title: "Multiple Sizes") do |doc|
  - doc.with_description do
    %p You can also use various sizes.

  .my-2.flex.flex-col.lg:flex-row.gap-4.items-center
    = daisy_button(css: "btn-lg") do
      Big Button

    = daisy_button do
      Default Button

    = daisy_button(css: "btn-sm") do
      Small Button

    = daisy_button(css: "btn-xs") do
      Tiny Button
```

URL: /examples/Daisy::Actions::ButtonComponent#multiple-sizes

-- Example: Custom Buttons
Description:
You can also create custom buttons.

Code (HAML):
```haml
= doc_example(title: "Custom Buttons") do |doc|
  - doc.with_description do
    %p You can also create custom buttons.

  = daisy_button(css: "bg-linear-to-r from-blue-500 to-blue-700 text-white rounded-full") do
    .loading.loading-spinner.mr-1

    %span
      Gradient Button
```

URL: /examples/Daisy::Actions::ButtonComponent#custom-buttons

-- Example: Outline Buttons
Description:
You can also create outline buttons.

Code (HAML):
```haml
= doc_example(title: "Outline Buttons") do |doc|
  - doc.with_description do
    %p You can also create outline buttons.

  .my-2.flex.flex-col.lg:flex-row.gap-4.items-center
    = daisy_button(css: "btn-outline") do
      Outline Button

    = daisy_button(css: "btn-primary btn-outline") do
      Primary Outline Button

    = daisy_button(css: "btn-secondary btn-outline") do
      Secondary Outline Button

    = daisy_button(css: "btn-accent btn-outline") do
      Accent Outline Button
```

URL: /examples/Daisy::Actions::ButtonComponent#outline-buttons

-- Example: Soft Buttons
Description:
You can also create soft buttons.

Code (HAML):
```haml
= doc_example(title: "Soft Buttons") do |doc|
  - doc.with_description do
    %p You can also create soft buttons.

  .my-2.flex.flex-col.lg:flex-row.gap-4.items-center
    = daisy_button(css: "btn-soft") do
      Soft Button

    = daisy_button(css: "btn-primary btn-soft") do
      Primary Soft Button

    = daisy_button(css: "btn-secondary btn-soft") do
      Secondary Soft Button

    = daisy_button(css: "btn-accent btn-soft") do
      Accent Soft Button
```

URL: /examples/Daisy::Actions::ButtonComponent#soft-buttons

-- Example: Dash Buttons
Description:
You can also create dash buttons.

Code (HAML):
```haml
= doc_example(title: "Dash Buttons") do |doc|
  - doc.with_description do
    %p You can also create dash buttons.

  .my-2.flex.flex-col.lg:flex-row.gap-4.items-center
    = daisy_button(css: "btn-dash") do
      Dash Button

    = daisy_button(css: "btn-primary btn-dash") do
      Primary Dash Button

    = daisy_button(css: "btn-secondary btn-dash") do
      Secondary Dash Button

    = daisy_button(css: "btn-accent btn-dash") do
      Accent Dash Button
```

URL: /examples/Daisy::Actions::ButtonComponent#dash-buttons

---

=== Component: Daisy::Actions::DropdownComponent
Group: Actions
Title: Dropdowns
API URL: /api-docs
Examples URL: /examples/Daisy::Actions::DropdownComponent
File: /home/loco_demo/app/views/examples/daisy/actions/dropdowns.html.haml

Description:
Dropdowns are a great way to display a list of items in a compact manner. 

Helpers:
- daisy_dropdown

Examples:

-- Example: Basic Dropdown
Description:
A basic dropdown has an icon or a title and a list of items (actions).

Code (HAML):
```haml
= doc_example(title: "Basic Dropdown", example_css: "pt-5 pb-24") do |doc|
  - doc.with_description do
    A basic dropdown has an icon or a title and a list of items (actions).



  = daisy_dropdown(title: "Click to Open") do |dropdown|
    - dropdown.with_item do
      = link_to "Ruby", "https://www.ruby-lang.org", target: "_blank"

    - dropdown.with_item do
      = link_to "Rails", "http://www.rubyonrails.org", target: "_blank"

    - dropdown.with_item do
      = link_to "Hotwire", "https://hotwired.dev/", target: "_blank"
```

URL: /examples/Daisy::Actions::DropdownComponent#basic-dropdown

-- Example: Custom Button
Description:
You can pass in your own button element to the dropdown.

Code (HAML):
```haml
= doc_example(title: "Custom Button", example_css: "pb-32") do |doc|
  - doc.with_description do
    You can pass in your own button element to the dropdown.



  = daisy_dropdown do |dropdown|
    - dropdown.with_button(icon: "heart", title: "Favorites", css: "btn-primary")

    - dropdown.with_item do
      = link_to "Ruby", "https://www.ruby-lang.org", target: "_blank"

    - dropdown.with_item do
      = link_to "Rails", "http://www.rubyonrails.org", target: "_blank"

    - dropdown.with_item do
      = link_to "Hotwire", "https://hotwired.dev/", target: "_blank"
```

URL: /examples/Daisy::Actions::DropdownComponent#custom-button

-- Example: Fully Custom Dropdown
Description:
You can also pass a custom `activator` allowing you to use any content instead of just a button to open the dropdown. Additionally, if no items are setup, the dropdown will simply output any content you provide, meaning you can completely customize the dropdown content as well. doc_note(css: "mb-8") do You'll need to **manually** add the `dropdown-content` CSS class to the content wrapper. We **automatically** add the `role="button"` and `tabindex="0"` attributes to the custom activator to make it keyboard accessible.

Code (HAML):
```haml
= doc_example(title: "Fully Custom Dropdown", example_css: "flex-col") do |doc|
  - doc.with_description do
    You can also pass a custom `activator` allowing you to use any content
instead of just a button to open the dropdown.

Additionally, if no items are setup, the dropdown will simply output any
content you provide, meaning you can completely customize the dropdown
content as well.



    = doc_note(css: "mb-8") do
      You'll need to **manually** add the `dropdown-content` CSS class to the
content wrapper.

We **automatically** add the `role="button"` and `tabindex="0"`
attributes to the custom activator to make it keyboard accessible.



  .flex.flex-col.items-end{ class: "max-w-[500px]" }
    = daisy_dropdown(css: "dropdown-end dropdown-hover") do |dropdown|
      - # This is the custom activator that will open the dropdown

      - dropdown.with_activator(css: "group") do
        = daisy_stat(css: "w-60 bg-base-100 border border-base-300 group-focus:border-info rounded-lg", title: "Places to Visit", description: "Hover to see more.") do
          = number_with_delimiter(1208)

      - # This is the custom content that will be displayed in the dropdown

      - # Note that we manually add the `dropdown-content` class to the wrapper

      = daisy_card(css: "mt-2 w-72 dropdown-content bg-base-100 shadow-xl") do |card|
        - card.with_top_figure css: "aspect-video", src: image_path("landscapes/beach.jpg")

        %p Such a beautiful beach!

        - card.with_actions(css: "mt-2 justify-end") do
          = daisy_button(css: "btn-primary") do
            Book Travel

    - # Extra content / space to show the dropdown in action

    .mt-4.pb-48.flex.flex-col.gap-4
      Here is some text which will be hidden by the dropdown. We have a lot of
extra space below this so you can see the dropdown in action.

You can also click on the Stat item (or focus it using your keyboard) to
keep the dropdown open.
```

URL: /examples/Daisy::Actions::DropdownComponent#fully-custom-dropdown

---

=== Component: Daisy::Actions::ModalComponent
Group: Actions
Title: Modals
API URL: /api-docs
Examples URL: /examples/Daisy::Actions::ModalComponent
File: /home/loco_demo/app/views/examples/daisy/actions/modals.html.haml

Description:
Modals are used to display content that requires user interaction. They are typically used to display forms, alerts, or other content that requires user input. 

Helpers:
- daisy_modal

Examples:

-- Example: Simple Modal
Description:
Modals are most often opened through the click of a button. They can be closed by clicking on the `x` icon in the top right, by clicking outside of the modal, or by submitting a `<form method='dialog'>` inside the modal. doc_note(css: "mb-8") do If no button or activator is provided, a very basic default button will be used instead. The button title will match the title of the modal.

Code (HAML):
```haml
%turbo-frame{"id" => "simple-modal"}
  = doc_example(title: "Simple Modal") do |doc|
    - doc.with_description do
      Modals are most often opened through the click of a button. They can be
closed by clicking on the `x` icon in the top right, by clicking outside
of the modal, or by submitting a `<form method='dialog'>` inside the
modal.



      = doc_note(css: "mb-8") do
        If no button or activator is provided, a very basic default button will

        be used instead. The button title will match the title of the modal.

    .flex.flex-col.items-center
      = daisy_modal(title: "Simple Modal") do |modal|
        - modal.with_button(css: 'btn-primary') do
          Open Modal

        Here is some really long modal content that should go well past the

        spot where the close icon appears...

        - modal.with_end_actions(css: "flex flex-row items-center gap-2") do
          %form{ method: :dialog }
            = daisy_button do
              Cancel

          %form{ action: "", method: :get }
            %input{ type: "hidden", name: "submitted", value: "true" }

            = daisy_button(css: "btn-primary") do
              Submit

      - if params[:submitted] == "true"
        %p.my-2.font-bold.italic
          You submitted the modal!
```

URL: /examples/Daisy::Actions::ModalComponent#simple-modal

-- Example: Custom Activator
Description:
Sometimes you want to use something other than a button to trigger the modal. You can use the activator slot to specify any custom HTML element as the trigger.

Code (HAML):
```haml
%turbo-frame{"id" => "custom-activator-modal"}
  = doc_example(title: "Custom Activator") do |doc|
    - doc.with_description do
      %p
        Sometimes you want to use something other than a button to trigger the

        modal. You can use the activator slot to specify any custom HTML

        element as the trigger.

    .flex.flex-col.items-center
      = daisy_modal(title: "Custom Activator Modal") do |modal|
        - modal.with_activator do
          = daisy_card(css: "cursor-pointer border border-base-300 hover:border-primary bg-base-100 transition-colors w-64") do |card|
            - card.with_top_figure css: "aspect-video", src: image_path("landscapes/mountain-river.jpg")

            .flex.justify-center.items-center.gap-2.p-4.font-bold
              = hero_icon(:bell, css: "size-6")

              .text-xl Notify Me

        This modal was opened using a custom activator instead of a button.

        You can use any HTML element as an activator!

        - modal.with_start_actions(css: "flex flex-row items-center gap-2") do
          %form{ method: :dialog }
            = daisy_button do
              I'm Done
```

URL: /examples/Daisy::Actions::ModalComponent#custom-activator

---

=== Component: Daisy::Actions::SwapComponent
Group: Actions
Title: Swaps
API URL: /api-docs
Examples URL: /examples/Daisy::Actions::SwapComponent
File: /home/loco_demo/app/views/examples/daisy/actions/swaps.html.haml

Description:
 The Swap component allows you to quickly swap between two icons, images, or  HTML elements with a simple click. 

Helpers:
- daisy_swap

Examples:

-- Example: Simple Emojis
Description:
The simplest use of the Swap component is to toggle between two emojis. You can even add various animations!

Code (HAML):
```haml
= doc_example(title: "Simple Emojis") do |doc|
  - doc.with_description do
    %p
      The simplest use of the Swap component is to toggle between two emojis.

      You can even add various animations!

  .mt-4.flex.gap-x-6.text-4xl.lg:text-6xl
    = daisy_swap(off: "❤️", on: "💔")

    = daisy_swap(off: "🌚", on: "🌞", css: "swap-rotate")

    = daisy_swap(off: "🔇", on: "🔊", css: "swap-flip")
```

URL: /examples/Daisy::Actions::SwapComponent#simple-emojis

-- Example: Hero Icons
Description:
You can also use the Swap component with Hero Icons. The following example toggles between various icons. You can also use our special `hero_icon` helper which accepts the `css` property and all other options that our custom components accept.

Code (HAML):
```haml
= doc_example(title: "Hero Icons") do |doc|
  - doc.with_description do
    You can also use the Swap component with Hero Icons. The following example
toggles between various icons.

You can also use our special `hero_icon` helper which accepts the `css`
property and all other options that our custom components accept.



  .mt-2.flex.items-center.gap-x-4
    - sizes = "size-16"

    = daisy_swap(checked: true) do |swap|
      - swap.with_on do
        = hero_icon "pause-circle", css: sizes, variant: :outline

      - swap.with_off do
        = heroicon "play-circle", class: sizes, variant: :outline

    = daisy_swap(checked: true) do |swap|
      - swap.with_on do
        = heroicon "bell-alert", class: sizes + " animate-bounce text-purple-600", variant: :outline

      - swap.with_off do
        = heroicon "bell-slash", class: sizes + " text-gray-400", variant: :outline

    - # The standard tooltip hover doesn't work with the swap element well, so we

    - # force it open for this example.

    - #

    - # Also, the tooltip seems to change the spacing, so we add some top margin

    - # to keep the icon centered vertically.

    = daisy_swap(css: "text-blue-600 mt-1") do |swap|
      - swap.with_on do
        .tooltip.tooltip-right.tooltip-open{ data: { tip: "Dollar" }}
          = heroicon "currency-dollar", class: sizes, variant: :outline

      - swap.with_off do
        .tooltip.tooltip-right.tooltip-open{ data: { tip: "Euro" }}
          = heroicon "currency-euro", class: sizes, variant: :outline
```

URL: /examples/Daisy::Actions::SwapComponent#hero-icons

-- Example: Custom HTML Elements
Description:
You can also use the Swap component with custom HTML elements. The following example toggles between a stop sign, a go sign, and a slow sign (when you click the `Make Indeterminate` button).

Code (HAML):
```haml
= doc_example(title: "Custom HTML Elements") do |doc|
  - doc.with_description do
    You can also use the Swap component with custom HTML elements. The
following example toggles between a stop sign, a go sign, and a slow sign
(when you click the `Make Indeterminate` button).



  - shared_classes = "p-8 mask text-center font-bold"

  .w-56.mt-4.flex.flex-col.items-center
    = daisy_swap(css: "swap-rotate", checkbox_html: { id: "stopsign" }) do |swap|
      - swap.with_off(css: "mask-hexagon bg-red-600 text-white #{shared_classes}") do
        Stop

      - swap.with_on(css: "mask-circle bg-green-500 text-white #{shared_classes}") do
        Go

      - swap.with_indeterminate(css: "mask-squircle bg-yellow-500 text-black #{shared_classes}") do
        Slow

    .mt-2.btn.btn-sm.px-4.py-2{ onclick: "document.getElementById('stopsign').indeterminate = true" }
      Make Indeterminate
```

URL: /examples/Daisy::Actions::SwapComponent#custom-html-elements

---

=== Component: Daisy::Actions::ThemeControllerComponent
Group: Actions
Title: Theme Controller
API URL: /api-docs
Examples URL: /examples/Daisy::Actions::ThemeControllerComponent
File: /home/loco_demo/app/views/examples/daisy/actions/theme_controllers.html.haml

Description:
doc_note(modifier: :tip) do The `ThemeController` component is a **_Builder_** component rather than your standard **_Slotable_** ViewComponent. This means that instead of the component providing slots, and handling where your custom components are _inserted_, the developer utilizes helper methods on the component to **choose** where the sub-components are _rendered_. In the examples below, we use the HAML `=` syntax to output the rendered sub-components, whereas typical components use the HAML `-` syntax to "pass" the component to a slot. 

Helpers:
- daisy_theme_controller

Examples:

-- Example: Theme Preview Icons
Description:
One of the most basic sub-components is the theme preview icon. It shows a visual representation of the theme using the `base`, `primary`, `secondary`, and `accent` colors as little dots inside of a rounded square.

Code (HAML):
```haml
= doc_example(title: "Theme Preview Icons") do |doc|
  - doc.with_description do
    One of the most basic sub-components is the theme preview icon. It shows a
visual representation of the theme using the `base`, `primary`,
`secondary`, and `accent` colors as little dots inside of a rounded
square.



  = daisy_theme_controller do |theme_controller|
    .flex.flex-col.sm:flex-row.gap-4.items-start.sm:items-center
      - theme_controller.themes.each do |theme|
        .flex.flex-row.gap-2.items-center
          = theme_controller.build_theme_preview(theme)

          = theme.humanize
```

URL: /examples/Daisy::Actions::ThemeControllerComponent#theme-preview-icons

-- Example: Theme Radio Inputs
Description:
When a theme radio input is selected, the theme's page will change to the value of the the input. doc_note(modifier: :warning, css: "mb-8") do We provide a custom name of `docs-radio-theme` so that this example doesn't interfere with the header theme or other examples. However, because we are **only** using the radio inputs, we do **NOT** call the `setTheme()` method, and thus we do **NOT** save these changes to `localStorage`.

Code (HAML):
```haml
= doc_example(title: "Theme Radio Inputs") do |doc|
  - doc.with_description do
    When a theme radio input is selected, the theme's page will change to the
value of the the input.



    = doc_note(modifier: :warning, css: "mb-8") do
      We provide a custom name of `docs-radio-theme` so that this example
doesn't interfere with the header theme or other examples.

However, because we are **only** using the radio inputs, we do **NOT**
call the `setTheme()` method, and thus we do **NOT** save these changes
to `localStorage`.



  = daisy_theme_controller do |theme_controller|
    .flex.flex-col.sm:flex-row.gap-4.items-start.sm:items-center
      - theme_controller.themes.each do |theme|
        %label.flex.flex-row.gap-2.items-center
          = theme_controller.build_radio_input(theme, name: "docs-radio-theme")

          = theme.humanize
```

URL: /examples/Daisy::Actions::ThemeControllerComponent#theme-radio-inputs

-- Example: Custom Switcher
Description:
You can also build a custom switcher by using all of the sub-components in combination. doc_note(modifier: :warning, css: "mb-8") do Note that the `daisy_theme_controller` may need to be the **parent** of your custom switcher to ensure that it appropriately sets up the Stimulus controller. In the example below, the dropdown doesn't render custom content when using the `with_item` helper, so the Stimulus controller wouldn't be applied if the order were switched.

Code (HAML):
```haml
= doc_example(title: "Custom Switcher", example_css: "pb-48") do |doc|
  - doc.with_description do
    You can also build a custom switcher by using all of the sub-components
in combination.



    = doc_note(modifier: :warning, css: "mb-8") do
      Note that the `daisy_theme_controller` may need to be the **parent** of
your custom switcher to ensure that it appropriately sets up the
Stimulus controller.

In the example below, the dropdown doesn't render custom content when
using the `with_item` helper, so the Stimulus controller wouldn't be
applied if the order were switched.




  = daisy_theme_controller do |tc|
    = daisy_dropdown do |dropdown|
      - dropdown.with_button(css: "btn-error", icon: "swatch", title: "Themes", right_icon: "chevron-down")

      - tc.themes.each do |theme|
        - dropdown.with_item do
          = daisy_link(href: "#", css: "no-underline", html: { data: { action: "click->loco-theme#setTheme" } }) do
            = tc.build_radio_input(theme, name: "docs-custom-theme", css: "hidden peer")

            = tc.build_theme_preview(theme)

            = theme.humanize

            = hero_icon "check", css: "size-4 invisible peer-checked:visible"
```

URL: /examples/Daisy::Actions::ThemeControllerComponent#custom-switcher

---

=== Component: Daisy::DataDisplay::AccordionComponent
Group: DataDisplay
Title: Accordions
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::AccordionComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/accordions.html.haml

Description:
Here are some examples showcasing accordions. 

Helpers:
- daisy_accordion

Examples:

-- Example: Basic Accordion
Description:
You can apply the `collapse-arrow` or `collapse-plus` CSS modifiers directly to each section.

Code (HAML):
```haml
= doc_example(title: "Basic Accordion") do |doc|
  - doc.with_description do
    You can apply the `collapse-arrow` or `collapse-plus` CSS modifiers
directly to each section.



  = daisy_accordion(css: "mt-4 bg-base-200 rounded-lg") do |accordion|
    - accordion.with_section(css: "collapse-arrow", title: "Section 1", checked: true) do
      This is a simple accordion. Click the sections to see more.

    - accordion.with_section(css: "collapse-plus", title: "Section 2") do
      It has three sections.

    - accordion.with_section(title: "Section 3") do
      You can open them independently.
```

URL: /examples/Daisy::DataDisplay::AccordionComponent#basic-accordion

-- Example: Separated Accordion
Description:
Sections can be combined or separated by specifying the `name` attribute. You can also set the `:arrow` or `:plus` modifier to the entire accordion to apply it to all sections.

Code (HAML):
```haml
= doc_example(title: "Separated Accordion") do |doc|
  - doc.with_description do
    Sections can be combined or separated by specifying the `name` attribute.

You can also set the `:arrow` or `:plus` modifier to the entire accordion
to apply it to all sections.



  = daisy_accordion(css: "mt-4", modifier: :arrow) do |accordion|
    - accordion.with_section(title: "Section 1", checked: true) do
      Sections can be combined...

    - accordion.with_section(title: "Section 2") do
      Like these two.

    - accordion.with_section(title: "Section A", name: "ex2-lettered", checked: true) do
      Or separated via the <code>name</code> attribute.

    - accordion.with_section(title: "Section B", name: "ex2-lettered") do
      Like these two.
```

URL: /examples/Daisy::DataDisplay::AccordionComponent#separated-accordion

-- Example: Form Accordion
Description:
Because it uses `<input type="radio">` elements, you can even use it inside a form!

Code (HAML):
```haml
= doc_example(title: "Form Accordion") do |doc|
  - doc.with_description do
    Because it uses `<input type="radio">` elements, you can even use it
inside a form!



  %turbo-frame{"id" => "accordion-form"}
    %form.mt-4.w-80{ action: "#" }
      %label.block.mb-4.italic What is your favorite color?

      = daisy_accordion(modifier: :plus, css: "flex flex-col gap-y-4", name: "favorite-color") do |accordion|
        - accordion.with_section(css: "bg-red-700 text-white", title: "Red", value: "red", checked: params["favorite-color"] == "red") do
          Rosy!

        - accordion.with_section(css: "bg-blue-700 text-white", title: "Blue", value: "blue", checked: params["favorite-color"] == "blue") do
          Solid!

        - accordion.with_section(css: "bg-green-700 text-white", title: "Green", value: "green", checked: params["favorite-color"] == "green") do
          Groovy!

      .mt-4.flex.flex-row.items-center.gap-x-4
        %input{ type: "submit", value: "Submit", class: "btn btn-secondary" }

        - if params["favorite-color"]
          %span.font-bold "You chose #{params["favorite-color"]}!"
```

URL: /examples/Daisy::DataDisplay::AccordionComponent#form-accordion

---

=== Component: Daisy::DataDisplay::AvatarComponent
Group: DataDisplay
Title: Avatars
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::AvatarComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/avatars.html.haml

Description:
Here are some simple avatar examples. 

Helpers:
- daisy_avatar

Examples:

-- Example: Basic Avatars
Description:
You can use the Avatar component to display a user's profile picture, icon, or initials. You can also add an online or offline indicator.

Code (HAML):
```haml
= doc_example(title: "Basic Avatars") do |doc|
  - doc.with_description do
    You can use the Avatar component to display a user's profile picture,
icon, or initials. You can also add an online or offline indicator.



  - # Lady 0 is skipped / unused because she looks kinda fake

  - ladies = (1..4).map { |i| image_path("avatars/lady-smiling-#{i}.jpg") }

  .mt-4.flex.flex-col.lg:flex-row.items-center.gap-4
    - # Avatar without online indicator

    = daisy_avatar(src: ladies[1]) do
      Daisy

    - # Avatar with online indicator

    = daisy_avatar(css: "avatar-online", src: ladies[2]) do
      Daisy

    - # Avatar with offline indicator

    = daisy_avatar(css: "avatar-offline", src: ladies[3]) do
      Daisy

    - # Placeholder avatar without online indicator

    = daisy_avatar do
      AB

    - # Placeholder avatar with icon & online indicator

    = daisy_avatar(css: "avatar-online", icon: "user", icon_css: "size-14 text-sky-400")

    - # Placeholder avatar with offline indicator

    = daisy_avatar(css: "avatar-offline") do
      TF
```

URL: /examples/Daisy::DataDisplay::AvatarComponent#basic-avatars

-- Example: Linked Avatars
Description:
You can link the Avatar component to a URL by using the `href` attribute.

Code (HAML):
```haml
= doc_example(title: "Linked Avatars") do |doc|
  - doc.with_description do
    You can link the Avatar component to a URL by using the `href` attribute.



  .space-x-4
    - # Avatar with link and title

    = daisy_avatar(href: "/", title: "Jane Doe", src: image_path("avatars/lady-smiling-1.jpg")) do
      Daisy

    - # Avatar with online indicator and link

    = daisy_avatar(href: "/", css: "avatar-online", src: image_path("avatars/lady-smiling-2.jpg")) do
      Daisy
```

URL: /examples/Daisy::DataDisplay::AvatarComponent#linked-avatars

---

=== Component: Daisy::DataDisplay::BadgeComponent
Group: DataDisplay
Title: Badges
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::BadgeComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/badges.html.haml

Description:
Here are some examples showcasing badges. 

Helpers:
- daisy_badge

Examples:

-- Example: Simple Badges
Description:
You can use the Badge component to display a small amount of information. Badges can be used to indicate status, category, or other metadata. The `title` can be passed through keyword arguments or as a block.

Code (HAML):
```haml
= doc_example(title: "Simple Badges") do |doc|
  - doc.with_description do
    You can use the Badge component to display a small amount of information.
Badges can be used to indicate status, category, or other metadata.

The `title` can be passed through keyword arguments or as a block.



  .my-2.flex.flex-col.lg:flex-row.gap-4
    = daisy_badge(title: "Plain Badge")

    = daisy_badge(title: "Primary Badge", css: "badge-primary")

    = daisy_badge(css: "badge-secondary") do
      Secondary Badge

    = daisy_badge(css: "badge-accent") do
      Accent Badge

    = daisy_badge(css: "badge-ghost") do
      Ghost Badge
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#simple-badges

-- Example: Badge Sizes
Description:
Badges come in four sizes: large, medium, small, and extra small.

Code (HAML):
```haml
= doc_example(title: "Badge Sizes") do |doc|
  - doc.with_description do
    %p
      Badges come in four sizes: large, medium, small, and extra small.

  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-secondary badge-lg") do
      Large Badge

    = daisy_badge(css: "badge-success badge-md") do
      Medium Badge

    = daisy_badge(css: "badge-error badge-sm") do
      Small Badge

    = daisy_badge(css: "badge-warning badge-xs") do
      Extra Small Badge
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#badge-sizes

-- Example: Badges with Icons
Description:
Badges can include icons on either the left or right side, or both. Use the `left_icon` and `right_icon` options to add icons. You can also use the `icon` option as an alias for `left_icon`.

Code (HAML):
```haml
= doc_example(title: "Badges with Icons") do |doc|
  - doc.with_description do
    Badges can include icons on either the left or right side, or both.
Use the `left_icon` and `right_icon` options to add icons.

You can also use the `icon` option as an alias for `left_icon`.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(title: "New", left_icon: "sparkles", css: "badge-primary")

    = daisy_badge(title: "Download", right_icon: "arrow-down-tray", css: "badge-secondary")

    = daisy_badge(title: "Favorite", icon: "star", css: "badge-warning")

    = daisy_badge(title: "Settings", left_icon: "cog-6-tooth", right_icon: "chevron-right", css: "badge-accent")
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#badges-with-icons

-- Example: Linked Badges
Description:
Badges can be made clickable by providing an `href` option. This transforms the badge into an anchor (`<a>`) tag. You can also specify a `target` attribute for the link, such as `_blank` for opening in a new tab.

Code (HAML):
```haml
= doc_example(title: "Linked Badges") do |doc|
  - doc.with_description do
    Badges can be made clickable by providing an `href` option. This
transforms the badge into an anchor (`<a>`) tag.

You can also specify a `target` attribute for the link, such as `_blank`
for opening in a new tab.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(title: "Documentation", href: "/api-docs", css: "badge-info")

    = daisy_badge(title: "External Link", href: "https://google.com", target: "_blank", css: "badge-secondary")

    = daisy_badge(title: "GitHub", href: "https://github.com/profoundry-us/loco_motion", left_icon: "code-bracket", css: "badge-neutral")

    = daisy_badge(href: "/", css: "badge-primary") do
      %span.flex.items-center.gap-1
        = hero_icon "document-text", css: "size-4"

        Read More
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#linked-badges

-- Example: Outline Badge
Description:
Badges can be displayed in an outline style.

Code (HAML):
```haml
= doc_example(title: "Outline Badge") do |doc|
  - doc.with_description do
    Badges can be displayed in an outline style.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-outline") { "Default" }

    = daisy_badge(css: "badge-primary badge-outline") { "Primary" }

    = daisy_badge(css: "badge-secondary badge-outline") { "Secondary" }

    = daisy_badge(css: "badge-accent badge-outline") { "Accent" }
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#outline-badge

-- Example: Soft Badge
Description:
Badges can be displayed in a soft style.

Code (HAML):
```haml
= doc_example(title: "Soft Badge") do |doc|
  - doc.with_description do
    Badges can be displayed in a soft style.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-soft") { "Default" }

    = daisy_badge(css: "badge-primary badge-soft") { "Primary" }

    = daisy_badge(css: "badge-secondary badge-soft") { "Secondary" }

    = daisy_badge(css: "badge-accent badge-soft") { "Accent" }
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#soft-badge

-- Example: Dash Badge
Description:
Badges can be displayed in a dash style.

Code (HAML):
```haml
= doc_example(title: "Dash Badge") do |doc|
  - doc.with_description do
    Badges can be displayed in a dash style.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-dash") { "Default" }

    = daisy_badge(css: "badge-primary badge-dash") { "Primary" }

    = daisy_badge(css: "badge-secondary badge-dash") { "Secondary" }

    = daisy_badge(css: "badge-accent badge-dash") { "Accent" }
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#dash-badge

-- Example: Outline badges with state colors
Description:
Outline badges can be displayed with state colors.

Code (HAML):
```haml
= doc_example(title: "Outline badges with state colors") do |doc|
  - doc.with_description do
    Outline badges can be displayed with state colors.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-info badge-outline") { "Info" }

    = daisy_badge(css: "badge-success badge-outline") { "Success" }

    = daisy_badge(css: "badge-warning badge-outline") { "Warning" }

    = daisy_badge(css: "badge-error badge-outline") { "Error" }
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#outline-badges-with-state-colors

-- Example: Soft badges with state colors
Description:
Soft badges can be displayed with state colors.

Code (HAML):
```haml
= doc_example(title: "Soft badges with state colors") do |doc|
  - doc.with_description do
    Soft badges can be displayed with state colors.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-info badge-soft") { "Info" }

    = daisy_badge(css: "badge-success badge-soft") { "Success" }

    = daisy_badge(css: "badge-warning badge-soft") { "Warning" }

    = daisy_badge(css: "badge-error badge-soft") { "Error" }
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#soft-badges-with-state-colors

-- Example: Dash badges with state colors
Description:
Dash badges can be displayed with state colors.

Code (HAML):
```haml
= doc_example(title: "Dash badges with state colors") do |doc|
  - doc.with_description do
    Dash badges can be displayed with state colors.



  .flex.flex-col.lg:flex-row.lg:items-center.gap-4
    = daisy_badge(css: "badge-info badge-dash") { "Info" }

    = daisy_badge(css: "badge-success badge-dash") { "Success" }

    = daisy_badge(css: "badge-warning badge-dash") { "Warning" }

    = daisy_badge(css: "badge-error badge-dash") { "Error" }
```

URL: /examples/Daisy::DataDisplay::BadgeComponent#dash-badges-with-state-colors

---

=== Component: Daisy::DataDisplay::CardComponent
Group: DataDisplay
Title: Cards
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::CardComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/cards.html.haml

Description:
Here are some examples showcasing cards. 

Helpers:
- daisy_card

Examples:

-- Example: Basic Card
Description:
Cards are used to display information in a structured way. They can contain images, text, and actions.

Code (HAML):
```haml
= doc_example(title: "Basic Card") do |doc|
  - doc.with_description do
    %p
      Cards are used to display information in a structured way. They can contain

      images, text, and actions.

  = daisy_card(css: "max-w-96 bg-base-100 shadow-xl") do |card|
    - card.with_top_figure css: "aspect-video", src: image_path("landscapes/beach.jpg")

    %p Such a beautiful beach!

    - card.with_actions(css: "mt-2 justify-end") do
      = daisy_button(css: "btn-primary") do
        Book Travel
```

URL: /examples/Daisy::DataDisplay::CardComponent#basic-card

-- Example: Card Figures
Description:
The images (figures) can be on the top or the bottom!

Code (HAML):
```haml
= doc_example(title: "Card Figures") do |doc|
  - doc.with_description do
    %p
      The images (figures) can be on the top or the bottom!

  - pics = ["beach", "forest", "mountain-river", "desert"]

  .mt-8.grid.grid-cols-1.gap-4.lg:grid-cols-2.xl:grid-cols-4
    - pics.each.with_index do |pic, i|
      = daisy_card(title: pic.titleize, css: "max-w-96 bg-base-100 shadow-xl") do |card|
        - if i % 2 == 0
          - card.with_top_figure css: "aspect-video", src: image_path("landscapes/#{pic}.jpg")

          - else

          - card.with_bottom_figure css: "aspect-video", src: image_path("landscapes/#{pic}.jpg")

        %p Such a beautiful place!

        - card.with_actions(css: "mt-2 justify-end") do
          = daisy_button(css: "btn-primary") do
            Book Travel
```

URL: /examples/Daisy::DataDisplay::CardComponent#card-figures

-- Example: Card Sizes
Description:
Cards can be rendered in different sizes. Use the `card-sm` class for a more condensed layout.

Code (HAML):
```haml
= doc_example(title: "Card Sizes") do |doc|
  - doc.with_description do
    Cards can be rendered in different sizes. Use the `card-sm` class for a
more condensed layout.



  .mt-8.space-y-6.md:space-y-0.md:flex.md:flex-row.md:gap-6
    = daisy_card(title: "Large Card", css: "max-w-96 bg-base-100 shadow-xl card-lg") do |card|
      This is a large card with larger text / padding. It provides extra space
for content and is great for highlighting important information.



      - card.with_actions(css: "mt-2 justify-end") do
        = daisy_button(css: "btn-primary") do
          Learn More

    = daisy_card(title: "Normal Card", css: "max-w-96 bg-base-100 shadow-xl") do |card|
      This is a normal card with default padding. It provides ample space for
content and is great for detailed information.



      - card.with_actions(css: "mt-2 justify-end") do
        = daisy_button(css: "btn-primary") do
          Learn More

    = daisy_card(title: "Small Card", css: "max-w-96 bg-base-100 shadow-xl card-sm") do |card|
      This is a small card with reduced padding. It's perfect for displaying
information in a more condensed format.



      - card.with_actions(css: "mt-2 justify-end") do
        = daisy_button(css: "btn-primary btn-sm") do
          Learn More
```

URL: /examples/Daisy::DataDisplay::CardComponent#card-sizes

---

=== Component: Daisy::DataDisplay::CarouselComponent
Group: DataDisplay
Title: Carousels
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::CarouselComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/carousels.html.haml

Description:
Carousels are great for showing off a lot of images! 

Helpers:
- daisy_carousel

Examples:

-- Example: Basic Carousel
Description:
You can use the Carousel component to display a collection of images or other content.

Code (HAML):
```haml
= doc_example(title: "Basic Carousel") do |doc|
  - doc.with_description do
    %p
      You can use the Carousel component to display a collection of images or

      other content.

  - photos = Rails.cache.fetch("unsplash_landscapes") { Unsplash::Photo.search("landscape") }

  - unsplash_link = "https://unsplash.com?#{Unsplash::Client.connection.utm_params.to_param}"

  = daisy_carousel(css: "h-96 gap-x-2 rounded-lg") do |carousel|
    - if photos.empty?
      %p No photos found.

    - photos.each do |photo|
      - carousel.with_item(css: "relative") do
        = link_to photo.links.html, target: "_blank" do
          = image_tag(photo.urls.regular, alt: photo.alt_description, class: "rounded-lg h-96")

        .absolute.bottom-2.left-0.right-0.flex.justify-center
          %span.rounded.text-base-content.px-2.py-1{ class: "bg-base-200/80" }
            Photo by

            = link_to photo.user.name, photo.user.links.html, class: "link hover:text-black", target: "_blank"

            on

            = link_to "Unsplash", unsplash_link, class: "link hover:text-black", target: "_blank"
```

URL: /examples/Daisy::DataDisplay::CarouselComponent#basic-carousel

-- Example: Card Carousel
Description:
The Carousel component can also be used to display other components, such as Cards. This is great for showcasing related content in a compact way.

Code (HAML):
```haml
= doc_example(title: "Card Carousel") do |doc|
  - doc.with_description do
    %p
      The Carousel component can also be used to display other components, such

      as Cards. This is great for showcasing related content in a compact way.

  = daisy_carousel(css: "h-96 gap-x-4 rounded-lg") do |carousel|
    - carousel.with_item(css: "w-96") do
      = daisy_card(css: "h-full bg-base-100 border border-base-200") do |card|
        - card.with_top_figure(src: image_path("landscapes/mountain-river.jpg"))

        - card.with_title { "Mountain River" }

        %p{ class: "text-base-content/70" }
          Experience breathtaking views of pristine rivers winding through majestic peaks.

        - card.with_actions do
          = daisy_button("Learn More")

    - carousel.with_item(css: "w-96") do
      = daisy_card(css: "h-full bg-base-100 border border-base-200") do |card|
        - card.with_top_figure(src: image_path("landscapes/desert.jpg"))

        - card.with_title { "Desert Vista" }

        %p{ class: "text-base-content/70" }
          Witness the stunning beauty of endless desert landscapes under vast skies.

        - card.with_actions do
          = daisy_button("Learn More")

    - carousel.with_item(css: "w-96") do
      = daisy_card(css: "h-full bg-base-100 border border-base-200") do |card|
        - card.with_top_figure(src: image_path("landscapes/forest.jpg"))

        - card.with_title { "Forest Path" }

        %p{ class: "text-base-content/70" }
          Explore winding paths through ancient forests filled with natural wonders.

        - card.with_actions do
          = daisy_button("Learn More")

    - carousel.with_item(css: "w-96") do
      = daisy_card(css: "h-full bg-base-100 border border-base-200") do |card|
        - card.with_top_figure(src: image_path("landscapes/beach.jpg"))

        - card.with_title { "Coastal Paradise" }

        %p{ class: "text-base-content/70" }
          Marvel at pristine beaches where turquoise waters meet golden sands.

        - card.with_actions do
          = daisy_button("Learn More")
```

URL: /examples/Daisy::DataDisplay::CarouselComponent#card-carousel

---

=== Component: Daisy::DataDisplay::ChatComponent
Group: DataDisplay
Title: Chat Bubbles
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::ChatComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/chat_bubbles.html.haml

Description:
 Here are some examples showcasing chat bubbles. 

Helpers:
- daisy_chat

Examples:

-- Example: Advanced Chat
Description:
Chat bubbles are used to display messages in a structured way. They can contain text, user images, a header, and a footer.

Code (HAML):
```haml
= doc_example(title: "Advanced Chat") do |doc|
  - doc.with_description do
    %p
      Chat bubbles are used to display messages in a structured way. They can

      contain text, user images, a header, and a footer.

  .w-full.mx-4
    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-2.jpg"))

      - chat.with_bubble do
        Hey Marcia! How are you doing?

    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-3.jpg"))

      - chat.with_bubble do
        I'm doing great! How about you?

    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-2.jpg"))

      - chat.with_bubble do
        I'm doing great too! Thanks for asking.

    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-3.jpg"))

      - chat.with_bubble do
        That's great to hear!

      - chat.with_footer do
        Read 10:45 AM

    = # Custom content messages

    = daisy_chat(css: "chat-end") do |chat|
      - chat.with_avatar(icon: "user", icon_css: "text-yellow-400")

      - chat.with_header do
        Unknown Number (555) 883-1234

      - chat.with_footer(css: "text-red-600 mt-1 flex items-center gap-x-2") do
        = heroicon("exclamation-triangle", variant: :outline, class: "size-4")

        %span
          Failed to send

      .chat-bubble
        Who dis? Why am I in this group chat?!
```

URL: /examples/Daisy::DataDisplay::ChatComponent#advanced-chat

-- Example: Colored Chat
Description:
Chat bubbles can be colored to differentiate between different users or types of messages.

Code (HAML):
```haml
= doc_example(title: "Colored Chat") do |doc|
  - doc.with_description do
    %p
      Chat bubbles can be colored to differentiate between different users or

      types of messages.

  .w-full.mx-4
    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-2.jpg"))

      - chat.with_bubble(css: "chat-bubble-accent flex items-center gap-x-1") do
        = heroicon "map-pin", class: "size-5"

        %strong
          Jill

        %span
          Started sharing her location.

    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-2.jpg"))

      - chat.with_bubble(css: "bg-base-300 text-base-content") do
        Hey Marcia! How are you doing?

    = daisy_chat(css: "chat-end") do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-4.jpg"))

      - chat.with_bubble(css: "chat-bubble-primary") do
        I'm blue da ba dee da ba daa

    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-2.jpg"))

      - chat.with_bubble(css: "bg-base-300 text-base-content") do
        Maybe this will help!

    = daisy_chat do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-2.jpg"))

      - chat.with_bubble(css: "chat-bubble-success text-base-200") do
        .flex.justify-center
          = heroicon "currency-dollar", class: "size-12"

        Jill sent you <strong>$50</strong>.

    = daisy_chat(css: "chat-end") do |chat|
      - chat.with_avatar(src: image_path("avatars/lady-smiling-4.jpg"))

      - chat.with_bubble(css: "chat-bubble-primary") do
        Wow, thanks! ❤️
```

URL: /examples/Daisy::DataDisplay::ChatComponent#colored-chat

---

=== Component: Daisy::DataDisplay::CollapseComponent
Group: DataDisplay
Title: Collapses
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::CollapseComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/collapses.html.haml

Description:
Here are some simple collapse examples. 

Helpers:
- daisy_collapse

Examples:

-- Example: Basic Collapse
Description:
Collapses are transparent by default and add additional padding.

Code (HAML):
```haml
= doc_example(title: "Basic Collapse") do |doc|
  - doc.with_description do
    %p
      Collapses are transparent by default and add additional padding.

  .mx-auto{ class: "w-[400px]" }
    = daisy_collapse(title: "Click to Open") do |collapse|
      This is the content of the collapse.
```

URL: /examples/Daisy::DataDisplay::CollapseComponent#basic-collapse

-- Example: Arrow & Plus Collapses
Description:
You can apply the `collapse-arrow` or `collapse-plus` CSS modifiers directly to each collapse.

Code (HAML):
```haml
= doc_example(title: "Arrow & Plus Collapses") do |doc|
  - doc.with_description do
    You can apply the `collapse-arrow` or `collapse-plus` CSS modifiers
directly to each collapse.



  .flex.flex-col.gap-4{ class: "w-[400px]" }
    = daisy_collapse(title: "Click to Open", css: "bg-base-100 border border-base-300 collapse-arrow") do |collapse|
      This is the content of the arrow collapse.

    = daisy_collapse(title: "Click to Open", css: "bg-base-100 border border-base-300 collapse-plus") do |collapse|
      This is the content of the plus collapse.
```

URL: /examples/Daisy::DataDisplay::CollapseComponent#arrow-plus-collapses

-- Example: Advanced Collapse
Description:
You can also customize the collapse with any HTML you desire!

Code (HAML):
```haml
= doc_example(title: "Advanced Collapse") do |doc|
  - doc.with_description do
    %p
      You can also customize the collapse with any HTML you desire!

  .mx-auto{ class: "w-[400px]" }
    = daisy_collapse(css: "collapse-arrow bg-base-200 border border-base-300") do |collapse|
      - collapse.with_title(css: "collapse-title bg-base-300") do
        .flex.gap-x-2.items-center
          = heroicon("user-circle", class: "size-6")

          %strong User Profile

      .mt-4.flex.gap-x-4.items-center
        = daisy_avatar(src: image_path("avatars/lady-smiling-1.jpg"))

        
          .text-xl.font-bold Jane Oliver

          .italic jane@oliver.test
```

URL: /examples/Daisy::DataDisplay::CollapseComponent#advanced-collapse

---

=== Component: Daisy::DataDisplay::CountdownComponent
Group: DataDisplay
Title: Countdowns
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::CountdownComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/countdowns.html.haml

Description:
 Countdowns display an animated countdown to a specific date. 

Helpers:
- daisy_countdown

Examples:

-- Example: Basic Countdown
Description:
A basic countdown accepts an ActiveSupport::Duration object as either the first positional argument or as the value of the `:duration` keyword argument.

Code (HAML):
```haml
= doc_example(title: "Basic Countdown", allow_reset: true) do |doc|
  - doc.with_description do
    A basic countdown accepts an ActiveSupport::Duration object as either the
first positional argument or as the value of the `:duration` keyword
argument.



  .flex.flex-col.gap-y-4.font-mono
    = daisy_countdown(60.seconds)

    = daisy_countdown(duration: 60.seconds, modifier: :letters)
```

URL: /examples/Daisy::DataDisplay::CountdownComponent#basic-countdown

-- Example: Longer Countdown
Description:
If you want to display a countdown with multiple parts, you can build one using the link_to "https://api.rubyonrails.org/classes/ActiveSupport/Duration.html", target: "_blank" do ActiveSupport::Duration#build method.

Code (HAML):
```haml
= doc_example(title: "Longer Countdown", allow_reset: true) do |doc|
  - doc.with_description do
    %p
      If you want to display a countdown with multiple parts, you can build one

      using the

      = link_to "https://api.rubyonrails.org/classes/ActiveSupport/Duration.html", target: "_blank" do
        ActiveSupport::Duration#build

      method.

  .flex.flex-col.items-center.gap-y-4.font-mono
    = daisy_countdown(ActiveSupport::Duration.build(3725))

    = daisy_countdown(ActiveSupport::Duration.build(3725), separator: "-")

    = daisy_countdown(ActiveSupport::Duration.build(3725), modifier: :letters)

    = daisy_countdown(ActiveSupport::Duration.build(3725), modifier: :words)
```

URL: /examples/Daisy::DataDisplay::CountdownComponent#longer-countdown

-- Example: Boxed Countdown
Description:
You can also display the countdowns in a box by customizing each part of the countdown. Note that the wrapper `text-sm` class applies to the words ("hours", "minutes", and "seconds") and the global `parts_css` sets the size of the numbers. You can also customize each part individually by using the `days_css`, `hours_css`, `minutes_css`, and `seconds_css` options. And you can add custom HTML using either the global `parts_html` or the individual `days_html`, `hours_html`, `minutes_html`, and `seconds_html` options.

Code (HAML):
```haml
= doc_example(title: "Boxed Countdown", allow_reset: true) do |doc|
  - doc.with_description do
    You can also display the countdowns in a box by customizing each part of
the countdown.

Note that the wrapper `text-sm` class applies to the words ("hours",
"minutes", and "seconds") and the global `parts_css` sets the size of the
numbers.

You can also customize each part individually by using the `days_css`,
`hours_css`, `minutes_css`, and `seconds_css` options.

And you can add custom HTML using either the global `parts_html` or the
individual `days_html`, `hours_html`, `minutes_html`, and `seconds_html`
options.



  .flex.flex-col.items-center.gap-y-4.text-sm
    - box_css = "where:bg-neutral where:text-neutral-content rounded-box flex flex-col items-center justify-center size-24 font-bold [&_span]:font-mono [&_span]:text-5xl"

    = daisy_countdown(ActiveSupport::Duration.build(3725), modifier: :words, css: "gap-x-8", parts_css: box_css, seconds_css: "bg-red-600 text-white", seconds_html: { title: "You better hurry!" })
```

URL: /examples/Daisy::DataDisplay::CountdownComponent#boxed-countdown

---

=== Component: Daisy::DataDisplay::DiffComponent
Group: DataDisplay
Title: Diffs
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::DiffComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/diffs.html.haml

Description:
 Diffs allow you to move a slider back and forth to compare two images,  text, or any HTML. 

Helpers:
- daisy_diff

Examples:

-- Example: Text / HTML Diff
Description:
Diffs are best when they show two mostly similar items that have only a few differences.

Code (HAML):
```haml
= doc_example(title: "Text / HTML Diff") do |doc|
  - doc.with_description do
    %p
      Diffs are best when they show two mostly similar items that have only a

      few differences.

  = daisy_diff(css: "aspect-video max-w-[800px] max-h-[450px]") do |diff|
    - diff.with_item do
      .bg-primary.text-primary-content.grid.place-content-center.text-6xl.lg:text-8xl.font-black
        LocoMotion

    - diff.with_item do
      .bg-base-200.grid.place-content-center.text-6xl.lg:text-8xl.font-black
        LocoMotion
```

URL: /examples/Daisy::DataDisplay::DiffComponent#text-html-diff

-- Example: Image Diff
Description:
Here is an example using two images. One is blurred, and the other is the full-resolution image.

Code (HAML):
```haml
= doc_example(title: "Image Diff") do |doc|
  - doc.with_description do
    %p
      Here is an example using two images. One is blurred, and the other is

      the full-resolution image.

  = daisy_diff(css: "aspect-video max-w-[800px] max-h-[450px]") do |diff|
    - diff.with_item do
      = image_tag("landscapes/desert.jpg")

    - diff.with_item do
      = image_tag("landscapes/desert.jpg", class: "blur-xs")
```

URL: /examples/Daisy::DataDisplay::DiffComponent#image-diff

---

=== Component: Daisy::DataDisplay::FigureComponent
Group: DataDisplay
Title: Figures
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::FigureComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/figures.html.haml

Description:
Here are some examples showcasing figures. 

Helpers:
- daisy_figure

Examples:

-- Example: Basic Figure
Description:
Figures are used to display images with optional captions. They are commonly used within cards and other content containers.

Code (HAML):
```haml
= doc_example(title: "Basic Figure") do |doc|
  - doc.with_description do
    %p
      Figures are used to display images with optional captions. They are commonly

      used within cards and other content containers.

  = daisy_figure(css: "max-w-96", src: image_path("landscapes/beach.jpg"))
```

URL: /examples/Daisy::DataDisplay::FigureComponent#basic-figure

-- Example: Figure with Caption
Description:
You can add a caption to your figure by providing content to the component.

Code (HAML):
```haml
= doc_example(title: "Figure with Caption") do |doc|
  - doc.with_description do
    %p
      You can add a caption to your figure by providing content to the component.

  = daisy_figure(css: "max-w-96", src: image_path("landscapes/mountain-river.jpg")) do
    %p.text-center.italic A beautiful mountain river landscape
```

URL: /examples/Daisy::DataDisplay::FigureComponent#figure-with-caption

-- Example: Custom Content
Description:
Figures can also be used without an image to display custom content.

Code (HAML):
```haml
= doc_example(title: "Custom Content") do |doc|
  - doc.with_description do
    %p
      Figures can also be used without an image to display custom content.

  = daisy_figure(css: "max-w-96 bg-base-100 border border-base-300 p-4 rounded-lg") do
    .flex.flex-col.items-center.gap-4
      = hero_icon("photo", css: "size-16 text-base-content/50")

      %p.text-center.italic Custom content without an image
```

URL: /examples/Daisy::DataDisplay::FigureComponent#custom-content

---

=== Component: Daisy::DataDisplay::KbdComponent
Group: DataDisplay
Title: Keyboard (KBD)
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::KbdComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/kbds.html.haml

Description:
The keyboard component is used to visually represent a key on a keyboard. 

Helpers:
- daisy_kbd

Examples:

-- Example: Basic Example
Code (HAML):
```haml
= doc_example(title: "Basic Example") do |doc|
  = daisy_kbd do
    
```

URL: /examples/Daisy::DataDisplay::KbdComponent#basic-example

-- Example: Various Sizes
Code (HAML):
```haml
= doc_example(title: "Various Sizes") do |doc|
  .my-2.flex.flex-row.gap-4.items-center
    = daisy_kbd(css: "kbd-lg") do
      Shift

    = daisy_kbd(css: "kbd-md") do
      Shift

    = daisy_kbd(css: "kbd-sm") do
      Shift

    = daisy_kbd(css: "kbd-xs") do
      Shift
```

URL: /examples/Daisy::DataDisplay::KbdComponent#various-sizes

-- Example: Inside a Sentence
Code (HAML):
```haml
= doc_example(title: "Inside a Sentence") do |doc|
  .my-2.flex.flex-row.gap-2.items-center
    %p Press

    = daisy_kbd do
      F

    %p to pay respects.
```

URL: /examples/Daisy::DataDisplay::KbdComponent#inside-a-sentence

-- Example: Combined Keys
Code (HAML):
```haml
= doc_example(title: "Combined Keys") do |doc|
  .my-2.flex.flex-row.gap-2.items-center
    = daisy_kbd do
      Ctrl

    +

    = daisy_kbd do
      Alt

    +

    = daisy_kbd do
      Del
```

URL: /examples/Daisy::DataDisplay::KbdComponent#combined-keys

-- Example: Function Keys
Code (HAML):
```haml
= doc_example(title: "Function Keys") do |doc|
  .my-2.flex.flex-row.gap-4.items-center
    = daisy_kbd do
      ⌘

    = daisy_kbd do
      ⌥

    = daisy_kbd do
      ⇧

    = daisy_kbd do
      ⌃
```

URL: /examples/Daisy::DataDisplay::KbdComponent#function-keys

-- Example: Full Keyboard
Code (HAML):
```haml
= doc_example(title: "Full Keyboard") do |doc|
  
    .flex.justify-center.gap-1.my-1.w-full
      = daisy_kbd do
        tab

      = daisy_kbd do
        q

      = daisy_kbd do
        w

      = daisy_kbd do
        e

      = daisy_kbd do
        r

      = daisy_kbd do
        t

      = daisy_kbd do
        y

      = daisy_kbd do
        u

      = daisy_kbd do
        i

      = daisy_kbd do
        o

      = daisy_kbd do
        p

      = daisy_kbd do
        [

      = daisy_kbd do
        ]

      = daisy_kbd do
        \

    .flex.justify-center.gap-1.my-1.w-full
      = daisy_kbd do
        caps lock

      = daisy_kbd do
        a

      = daisy_kbd do
        s

      = daisy_kbd do
        d

      = daisy_kbd do
        f

      = daisy_kbd do
        g

      = daisy_kbd do
        h

      = daisy_kbd do
        j

      = daisy_kbd do
        k

      = daisy_kbd do
        l

      = daisy_kbd do
        :

      = daisy_kbd do
        '

      = daisy_kbd do
        return

    .flex.justify-center.gap-1.my-1.w-full
      = daisy_kbd do
        shift

      = daisy_kbd do
        z

      = daisy_kbd do
        x

      = daisy_kbd do
        c

      = daisy_kbd do
        v

      = daisy_kbd do
        b

      = daisy_kbd do
        n

      = daisy_kbd do
        m

      = daisy_kbd do
        ,

      = daisy_kbd do
        .

      = daisy_kbd do
        /

      = daisy_kbd do
        shift
```

URL: /examples/Daisy::DataDisplay::KbdComponent#full-keyboard

---

=== Component: Daisy::DataDisplay::ListComponent
Group: DataDisplay
Title: Lists
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::ListComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/lists.html.haml

Description:
Lists provide a vertical layout to display information in rows. They can contain various content types including text, images, and actions arranged in a consistent format. 

Helpers:
- daisy_list

Examples:

-- Example: Basic List
Description:
A simple list that displays items in a vertical arrangement. Each item is rendered as a list element with basic styling.

Code (HAML):
```haml
= doc_example(title: "Basic List") do |doc|
  - doc.with_description do
    A simple list that displays items in a vertical arrangement. Each item is
rendered as a list element with basic styling.



  = daisy_list do |list|
    - list.with_item { "Item 1" }

    - list.with_item { "Item 2" }

    - list.with_item { "Item 3" }
```

URL: /examples/Daisy::DataDisplay::ListComponent#basic-list

-- Example: List with Header
Description:
Lists can include a header element that describes the content below. This provides context for the items and improves organization.

Code (HAML):
```haml
= doc_example(title: "List with Header") do |doc|
  - doc.with_description do
    Lists can include a header element that describes the content below.
This provides context for the items and improves organization.



  = daisy_list(header: "Featured Items") do |list|
    - list.with_item { "Featured Item 1" }

    - list.with_item { "Featured Item 2" }

    - list.with_item { "Featured Item 3" }
```

URL: /examples/Daisy::DataDisplay::ListComponent#list-with-header

-- Example: Styled List
Description:
Lists can be customized with additional CSS classes for enhanced visual presentation. This example adds a background, rounded corners, shadow, and dividers between items.

Code (HAML):
```haml
= doc_example(title: "Styled List") do |doc|
  - doc.with_description do
    Lists can be customized with additional CSS classes for enhanced visual
presentation. This example adds a background, rounded corners, shadow,
and dividers between items.



  = daisy_list(css: "bg-base-100 shadow-md") do |list|
    - list.with_item { "Item 1" }

    - list.with_item { "Item 2" }

    - list.with_item { "Item 3" }
```

URL: /examples/Daisy::DataDisplay::ListComponent#styled-list

-- Example: List with Images
Description:
List items can contain rich content including images and structured text. This is useful for displaying user profiles or other complex information.

Code (HAML):
```haml
= doc_example(title: "List with Images") do |doc|
  - doc.with_description do
    List items can contain rich content including images and structured text.
This is useful for displaying user profiles or other complex information.



  = daisy_list(header: "User Profiles", header_css: "p-4 pb-2 text-xs opacity-60 tracking-wide", css: "bg-base-100 rounded-box shadow-md") do |list|
    - list.with_item do |item|
      = image_tag("avatars/lady-smiling-1.jpg", class: "size-10 rounded-box")

      .flex.flex-col
        .font-medium Alice Johnson

        .text-xs.opacity-60 Product Designer

    - list.with_item do |item|
      = image_tag("avatars/lady-smiling-2.jpg", class: "size-10 rounded-box")

      .flex.flex-col
        .font-medium Jane Smith

        .text-xs.opacity-60 Software Engineer

    - list.with_item do |item|
      = image_tag("avatars/lady-smiling-3.jpg", class: "size-10 rounded-box")

      .flex.flex-col
        .font-medium Carol Davis

        .text-xs.opacity-60 UX Researcher
```

URL: /examples/Daisy::DataDisplay::ListComponent#list-with-images

-- Example: Interactive List
Description:
Lists can include interactive elements like buttons to provide additional functionality. This example shows a music player interface with play and favorite buttons.

Code (HAML):
```haml
= doc_example(title: "Interactive List") do |doc|
  - doc.with_description do
    Lists can include interactive elements like buttons to provide additional
functionality. This example shows a music player interface with play
and favorite buttons.



  = daisy_list(header: "Most played songs", header_css: "p-4 pb-2 text-xs opacity-60 tracking-wide", css: "w-100 bg-base-100 rounded-box shadow-md") do |list|
    - list.with_item do |item|
      = image_tag("avatars/lady-smiling-1.jpg", class: "size-10 rounded-box")

      .flex.flex-col
        .font-medium Remaining Reason

        .text-xs.uppercase.font-semibold.opacity-60 Dio Lupa

      = daisy_button(css: "btn-square btn-ghost") do
        = hero_icon("play")

      = daisy_button(css: "btn-square btn-ghost") do
        = hero_icon("heart")

    - list.with_item do |item|
      = image_tag("avatars/lady-smiling-4.jpg", class: "size-10 rounded-box")

      .flex.flex-col
        .font-medium Bears of a Fever

        .text-xs.uppercase.font-semibold.opacity-60 Ellie Beilish

      = daisy_button(css: "btn-square btn-ghost") do
        = hero_icon("play")

      = daisy_button(css: "btn-square btn-ghost") do
        = hero_icon("heart")

    - list.with_item do |item|
      = image_tag("avatars/lady-smiling-3.jpg", class: "size-10 rounded-box")

      .flex.flex-col
        .font-medium Cappuccino

        .text-xs.uppercase.font-semibold.opacity-60 Sabrino Gardener

      = daisy_button(css: "btn-square btn-ghost") do
        = hero_icon("play")

      = daisy_button(css: "btn-square btn-ghost") do
        = hero_icon("heart")
```

URL: /examples/Daisy::DataDisplay::ListComponent#interactive-list

---

=== Component: Daisy::DataDisplay::StatComponent
Group: DataDisplay
Title: Stats
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::StatComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/stats.html.haml

Description:
doc_note do Stats have a transparent background by default. Use `bg-base-100` if you need a background color. 

Helpers:
- daisy_stat

Examples:

-- Example: Basic Stats
Description:
Stats are used to display simple data points. They can be used to display a number (the main content), a title, and a description.

Code (HAML):
```haml
= doc_example(title: "Basic Stats") do |doc|
  - doc.with_description do
    Stats are used to display simple data points. They can be used to display
a number (the main content), a title, and a description.



  .bg-base-100.lg:grid.lg:grid-flow-col.lg:auto-cols-auto.items-center.gap-x-4.border.rounded-lg{ class: "w-[300px] lg:w-[800px]" }
    = daisy_stat do
      = number_to_currency(8619.45)

    = daisy_stat(title: "Views", description: "Views in the last 24 hours.") do
      = number_with_delimiter(1208)

    - # Example passing options as blocks. This allows you to pass in HTML.

    = daisy_stat do |stat|
      - stat.with_title do
        %p.stat-title Average Cart Value

      = number_to_currency(319, precision: 0)

      - stat.with_description do
        %p.stat-desc You're doing quite well on your ACV!
```

URL: /examples/Daisy::DataDisplay::StatComponent#basic-stats

-- Example: Complex Stats
Description:
Stats can also be used to display more complex data points. You can pass in an image, an icon, or any other content you'd like.

Code (HAML):
```haml
= doc_example(title: "Complex Stats") do |doc|
  - doc.with_description do
    Stats can also be used to display more complex data points. You can pass
in an image, an icon, or any other content you'd like.



  .bg-base-100.lg:grid.lg:grid-flow-col.lg:auto-cols-auto.items-center.gap-x-4.border.rounded-lg{ class: "w-[300px] lg:w-[900px]" }
    = daisy_stat(css: "max-w-80", title: "Tasks", description: "Completed this month.", src: image_path("avatars/lady-smiling-1.jpg")) do
      25

    = daisy_stat(css: "max-w-80", title: "Students", description: "Total enrollments.", icon: "academic-cap") do
      = number_with_delimiter(1112)

    = daisy_stat(css: "max-w-80") do |stat|
      - stat.with_title do
        %p.stat-title New Students

      42

      - stat.with_description do
        %p.stat-desc New students in July!

      - stat.with_figure do
        = daisy_badge(css: "badge-primary") do
          New!
```

URL: /examples/Daisy::DataDisplay::StatComponent#complex-stats

---

=== Component: Daisy::DataDisplay::StatusComponent
Group: DataDisplay
Title: Status
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::StatusComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/statuses.html.haml

Description:
Status is a really small icon to visually show the current status of an element, like online, offline, error, etc. 

Helpers:
- daisy_status

Examples:

-- Example: Basic Status
Description:
The basic status component is a simple dot indicator with no additional styling.

Code (HAML):
```haml
= doc_example(title: "Basic Status") do |doc|
  - doc.with_description do
    The basic status component is a simple dot indicator with no additional
styling.



  = daisy_status
```

URL: /examples/Daisy::DataDisplay::StatusComponent#basic-status

-- Example: Status Sizes
Description:
Status indicators come in five sizes: extra small, small, medium (default), large, and extra large.

Code (HAML):
```haml
= doc_example(title: "Status Sizes") do |doc|
  - doc.with_description do
    Status indicators come in five sizes: extra small, small, medium
(default), large, and extra large.



  .flex.flex-col.items-start.gap-2
    .flex.items-center.gap-2
      = daisy_status(css: "status-xs mr-2")

      %span Extra Small

    .flex.items-center.gap-2
      = daisy_status(css: "status-sm mr-2")

      %span Small

    .flex.items-center.gap-2
      = daisy_status(css: "status-md mr-2")

      %span Medium (Default)

    .flex.items-center.gap-2
      = daisy_status(css: "status-lg mr-2")

      %span Large

    .flex.items-center.gap-2
      = daisy_status(css: "status-xl mr-2")

      %span Extra Large
```

URL: /examples/Daisy::DataDisplay::StatusComponent#status-sizes

-- Example: Status Colors
Description:
Status indicators can use different colors to represent various states or conditions.

Code (HAML):
```haml
= doc_example(title: "Status Colors") do |doc|
  - doc.with_description do
    Status indicators can use different colors to represent various states or
conditions.



  .flex.flex-col.items-start.gap-2
    .flex.items-center.gap-2
      = daisy_status(css: "status-primary mr-2")

      %span Primary

    .flex.items-center.gap-2
      = daisy_status(css: "status-secondary mr-2")

      %span Secondary

    .flex.items-center.gap-2
      = daisy_status(css: "status-accent mr-2")

      %span Accent

    .flex.items-center.gap-2
      = daisy_status(css: "status-neutral mr-2")

      %span Neutral

    .flex.items-center.gap-2
      = daisy_status(css: "status-info mr-2")

      %span Info

    .flex.items-center.gap-2
      = daisy_status(css: "status-success mr-2")

      %span Success

    .flex.items-center.gap-2
      = daisy_status(css: "status-warning mr-2")

      %span Warning

    .flex.items-center.gap-2
      = daisy_status(css: "status-error mr-2")

      %span Error
```

URL: /examples/Daisy::DataDisplay::StatusComponent#status-colors

-- Example: Status with Tooltip and Aria Label
Description:
For accessibility, status indicators should include descriptive ARIA labels.

Code (HAML):
```haml
= doc_example(title: "Status with Tooltip and Aria Label") do |doc|
  - doc.with_description do
    For accessibility, status indicators should include descriptive ARIA
labels.



  = daisy_status(css: "status-md status-success", tip: "Online", html: { aria: { label: "Status: Online" } })
```

URL: /examples/Daisy::DataDisplay::StatusComponent#status-with-tooltip-and-aria-label

---

=== Component: Daisy::DataDisplay::TableComponent
Group: DataDisplay
Title: Tables
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::TableComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/tables.html.haml

Description:
 Tables are designed to show data in a tabular layout making it easy to see  large amounts of data at once and compare rows with each other. 

Helpers:
- daisy_table

Examples:

-- Example: Basic Table
Description:
Here is a super-basic table.

Code (HAML):
```haml
= doc_example(title: "Basic Table") do |doc|
  - doc.with_description do
    %p
      Here is a super-basic table.

  - data = [{col1: "foo", col2: "bar"}, {col1: "fizz", col2: "buzz"}]

  = daisy_table do |table|
    - table.with_head do |head|
      - head.with_column do
        Column 1

      - head.with_column do
        Column 2

    - data.each do |d|
      - table.with_row do |row|
        - row.with_column do
          = d[:col1]

        - row.with_column do
          = d[:col2]
```

URL: /examples/Daisy::DataDisplay::TableComponent#basic-table

-- Example: Pinning Rows (Using Sections)
Description:
The following example shows how to have a table with multiple headers pinned as you scroll.

Code (HAML):
```haml
= doc_example(title: "Pinning Rows (Using Sections)") do |doc|
  - doc.with_description do
    %p
      The following example shows how to have a table with multiple headers

      pinned as you scroll.

  .size-96.border.border-base-300.rounded.overflow-y-auto
    = daisy_table(css: "bg-base-100 table-pin-rows") do |table|
      - (0..90).step(10).with_index do |step, index|
        - table.with_section do |section|
          - section.with_head do |head|
            - head.with_column(css: 'bg-secondary text-secondary-content') do
              - if step == 0
                1900s

                - else

                = "19#{index * 10}s"

          - section.with_body do |body|
            - (0..9).each do |i|
              - body.with_row do |row|
                - row.with_column do
                  - if step == 0
                    = "190#{step + i}"

                    - else

                    = "19#{step + i}"
```

URL: /examples/Daisy::DataDisplay::TableComponent#pinning-rows-using-sections

---

=== Component: Daisy::DataDisplay::TimelineComponent
Group: DataDisplay
Title: Timelines
API URL: /api-docs
Examples URL: /examples/Daisy::DataDisplay::TimelineComponent
File: /home/loco_demo/app/views/examples/daisy/data_display/timelines.html.haml

Description:
 Here are some examples showcasing timelines. 

Helpers:
- daisy_timeline

Examples:

-- Example: Basic Timeline
Description:
Timelines are used to display a series of events in chronological order.

Code (HAML):
```haml
= doc_example(title: "Basic Timeline") do |doc|
  - doc.with_description do
    %p
      Timelines are used to display a series of events in chronological order.

  = daisy_timeline do |timeline|
    - timeline.with_event(start: "1985", middle_icon: "user-circle", end: "Topher Born")

    - timeline.with_event(start: "College", middle_icon: "academic-cap", end: "2006")

    - timeline.with_event(start: "2010", middle_icon: "cake", end: "Married Marnie")

    - timeline.with_event(start: "Built LocoMotion", middle_icon: "code-bracket", end: "2024")
```

URL: /examples/Daisy::DataDisplay::TimelineComponent#basic-timeline

-- Example: Custom Timeline
Description:
The `start`, `middle_icon`, and `end` properties can be customized. Or you can provide a completely custom `middle` block.

Code (HAML):
```haml
= doc_example(title: "Custom Timeline") do |doc|
  - doc.with_description do
    The `start`, `middle_icon`, and `end` properties can be customized. Or you
can provide a completely custom `middle` block.



  = daisy_timeline do |timeline|
    - timeline.with_event do |event|
      - event.with_start(css: "font-bold") do
        1985

      - event.with_middle do
        = hero_icon("check-circle", variant: :solid)

      - event.with_end(css: "timeline-box") do
        Born

    - timeline.with_event do |event|
      - event.with_start(css: "font-bold") do
        2006

      - event.with_middle do
        = hero_icon("check-circle", variant: :solid)

      - event.with_end(css: "timeline-box") do
        Graduated College

    - timeline.with_event do |event|
      - event.with_start(css: "font-bold") do
        2010

      - event.with_middle do
        = hero_icon("check-circle", variant: :solid)

      - event.with_end(css: "timeline-box") do
        Married
```

URL: /examples/Daisy::DataDisplay::TimelineComponent#custom-timeline

---

=== Component: Daisy::DataInput::CallyComponent
Group: DataInput
Title: Calendars
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::CallyComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/calendars.html.haml

Description:
doc_note(modifier: :warning) do You must include the Cally script tag or <a target="_blank" href="https://wicky.nillia.ms/cally/#installation">node module</a> in your application for this component to render and function properly.  doc_code(language: "sh") do npm install cally # or yarn add cally  doc_code(language: "js") do # application.js import "cally"; 

Helpers:
- daisy_cally

Examples:

-- Example: Calendar
Description:
Calendars can be used to select a single date. Use the `update` option to pass a CSS selector of a `<div>` or other element to update. Note that this uses the `onchange` event along with `getElementById` to provide a very basic change system. You may want to use Stimulus controllers to handle more complex scenarios. doc_code(language: "js") do { onchange: "document.getElementById('\#{@update}').innerHTML = this.value" }

Code (HAML):
```haml
= doc_example(title: "Calendar", example_css: "flex-col", allow_reset: true) do |doc|
  - doc.with_description do
    Calendars can be used to select a single date. Use the `update` option to
pass a CSS selector of a `<div>` or other element to update.

Note that this uses the `onchange` event along with `getElementById` to
provide a very basic change system. You may want to use Stimulus
controllers to handle more complex scenarios.



    = doc_code(language: "js") do
      { onchange: "document.getElementById('\#{@update}').innerHTML = this.value" }



  = daisy_cally(update: "cally")

  .text-center.italic{"id" => "cally"}
    Select a date...
```

URL: /examples/Daisy::DataInput::CallyComponent#calendar

-- Example: Calendar with Input
Description:
You can utilize the `change` option to change the value of an HTML input when a date is selected. Similar to the `update` option, this uses `onchange` and `getElementById`, but is specialized for updating an input element's `value`. doc_code(language: "js") do { onchange: "document.getElementById('\#{@update}').value = this.value" } doc_note(modifier: :tip, css: "mb-8") do To attach a Cally calendar to an input in a popover, use the component_link("Cally Input", "Daisy::DataInput::CallyInputComponent") component.

Code (HAML):
```haml
= doc_example(title: "Calendar with Input", example_css: "flex-col", allow_reset: true) do |doc|
  - doc.with_description do
    You can utilize the `change` option to change the value of an HTML input
when a date is selected.

Similar to the `update` option, this uses `onchange` and `getElementById`,
but is specialized for updating an input element's `value`.



    = doc_code(language: "js") do
      { onchange: "document.getElementById('\#{@update}').value = this.value" }



    = doc_note(modifier: :tip, css: "mb-8") do
      %p
        To attach a Cally calendar to an input in a popover, use the

        = component_link("Cally Input", "Daisy::DataInput::CallyInputComponent")

        component.

  = daisy_input(id: "cally_input", placeholder: "Select a date...")

  = daisy_cally(change: "cally_input")
```

URL: /examples/Daisy::DataInput::CallyComponent#calendar-with-input

-- Example: Calendar Range
Description:
Calendars can also allow selection of a range of dates.

Code (HAML):
```haml
= doc_example(title: "Calendar Range", allow_reset: true) do |doc|
  - doc.with_description do
    Calendars can also allow selection of a range of dates.



  = daisy_card(css: "card-border bg-base-100 shadow-lg") do
    = daisy_cally(modifier: :range, months: 2, months_css: "grid md:grid-cols-2 gap-8", update: "cally_range")

    .text-center.italic{"id" => "cally_range"}
      Select a date range...
```

URL: /examples/Daisy::DataInput::CallyComponent#calendar-range

---

=== Component: Daisy::DataInput::CallyInputComponent
Group: DataInput
Title: Cally Inputs
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::CallyInputComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/cally_inputs.html.haml

Description:
 This component provides an elegant alternative to standard HTML date inputs  while maintaining a consistent visual style with other form components. 

Helpers:
- daisy_cally_input

Examples:

-- Example: Basic Usage
Description:
The basic Cally Input provides a text field that, when clicked, displays a calendar popover for date selection. doc_note(modifier: :tip, css: "mb-8") do For keyboard navigation, you can use the daisy_kbd("space") or daisy_kbd("↓") keys to open the calendar popover.

Code (HAML):
```haml
= doc_example(title: "Basic Usage") do |doc|
  - doc.with_description do
    The basic Cally Input provides a text field that, when clicked, displays a
calendar popover for date selection.



    = doc_note(modifier: :tip, css: "mb-8") do
      %p
        For keyboard navigation, you can use the

        = daisy_kbd("space")

        or

        = daisy_kbd("↓")

        keys to open the calendar popover.

  = daisy_cally_input(value: "2025-07-24")
```

URL: /examples/Daisy::DataInput::CallyInputComponent#basic-usage

-- Example: Custom Input
Description:
You can customize the input or calendar by using the appropriate slots.

Code (HAML):
```haml
= doc_example(title: "Custom Input") do |doc|
  - doc.with_description do
    You can customize the input or calendar by using the appropriate slots.



  = daisy_cally_input(placeholder: "parent placeholder", popover_css: "dropdown-top dropdown-center", auto_scroll_padding: 200) do |c|
    = c.with_input(placeholder: "Select a date...")

    = c.with_calendar(css: "bg-accent")
```

URL: /examples/Daisy::DataInput::CallyInputComponent#custom-input

-- Example: Rails Form Example
Description:
The built-in Form Builder extension makes it easy to use Cally Inputs in your Rails forms. The component automatically extracts the name, ID, and value attributes from the form object.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension makes it easy to use Cally Inputs in your
Rails forms. The component automatically extracts the name, ID, and value
attributes from the form object.



  = form_with(url: "#", method: :post, scope: :event) do |form|
    .my-2.flex.flex-col.gap-4
      = form.daisy_cally_input(:start_date, start: "Start Date")

      = form.daisy_cally_input(:end_date, end: "End Date")

      = form.daisy_cally_input(:event_date, floating_placeholder: "Select an event date")

      = form.daisy_label(:registration_deadline, "Registration Deadline", css: "mt-4")

      = form.daisy_cally_input(:registration_deadline, value: Date.today.next_month.to_s) do |cally|
        - cally.with_input(placeholder: "Select a registration deadline...")

      = form.daisy_label(:reminder_date, "Reminder Date", css: "mt-4")

      = form.daisy_cally_input(:reminder_date, popover_css: "dropdown-top") do |cally|
        - cally.with_input(placeholder: "Select a reminder date...")

        - cally.with_calendar(css: "bg-secondary")
```

URL: /examples/Daisy::DataInput::CallyInputComponent#rails-form-example

---

=== Component: Daisy::DataInput::CheckboxComponent
Group: DataInput
Title: Checkboxes
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::CheckboxComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/checkboxes.html.haml

Description:
Checkboxes are great for toggling an item on / off, or selecting multiple items from a list. 

Helpers:
- daisy_checkbox

Examples:

-- Example: Basic Checkboxes
Description:
Basic checkboxes can be rendered with or without a label through standard heirarchy means, or by utilizing the `start` and `end` wrappers. doc_note(css: "mb-8") do While the <a href="#{doc_url("LocoMotion/Concerns/LabelableComponent")}" target="_blank">LabelableComponent</a> Concern does implement a `floating_label` attribute, it is not relevant for checkboxes and is not implemented.

Code (HAML):
```haml
= doc_example(title: "Basic Checkboxes") do |doc|
  - doc.with_description do
    Basic checkboxes can be rendered with or without a label through standard
heirarchy means, or by utilizing the `start` and `end` wrappers.



    = doc_note(css: "mb-8") do
      While the
<a href="#{doc_url("LocoMotion/Concerns/LabelableComponent")}" target="_blank">LabelableComponent</a>
Concern does implement a `floating_label` attribute, it is not relevant
for checkboxes and is not implemented.



  .my-2.flex.flex-col.gap-4
    
      = daisy_checkbox(name: "checkbox_no_label", id: "checkbox_no_label")

      .tooltip.tooltip-warning{ "data-tip": "CANNOT click the text to activate" }
        Checkbox with no label

    = daisy_label(for: "checkbox_with_label") do
      = daisy_checkbox(name: "checkbox_with_label", id: "checkbox_with_label")

      Checkbox with regular label

    = daisy_checkbox(name: "checkbox_end", id: "checkbox_end", checked: true, end: "Checkbox using end wrapper")

    = daisy_checkbox(name: "checkbox_start", id: "checkbox_start", checked: true, start: "Checkbox using start wrapper")

    = daisy_checkbox(name: "checkbox_end_block", id: "checkbox_end_block", checked: true) do |checkbox|
      - checkbox.with_end do
        Checkbox using `end` block



    = daisy_checkbox(name: "checkbox_both_labels", id: "checkbox_both_labels", checked: true) do |checkbox|
      - checkbox.with_start(css: "flex flex-row items-center") do
        Using

        = daisy_badge(css: "mx-1 badge-secondary badge-sm") do
          start

      - checkbox.with_end(css: "flex flex-row items-center") do
        and

        = daisy_badge(css: "mx-1 badge-secondary badge-sm") do
          end

        blocks
```

URL: /examples/Daisy::DataInput::CheckboxComponent#basic-checkboxes

-- Example: Checkboxes with Colors
Description:
Checkboxes can be styled with different colors using DaisyUI's color classes. doc_note(css: "mb-8") do Uses `checkbox-neutral` with dark backgrounds for dark mode compatibility.

Code (HAML):
```haml
= doc_example(title: "Checkboxes with Colors") do |doc|
  - doc.with_description do
    Checkboxes can be styled with different colors using DaisyUI's color classes.



    = doc_note(css: "mb-8") do
      Uses `checkbox-neutral` with dark backgrounds for dark mode compatibility.



  .my-2.grid.grid-cols-1.md:grid-cols-2.gap-4
    = daisy_label(for: "checkbox_dark_mode") do
      = daisy_checkbox(name: "checkbox_dark_mode", id: "checkbox_dark_mode", css: "checkbox-neutral theme-controller", value: "dark", checked: false)

      Enable Dark Mode

    = daisy_label(for: "checkbox_primary") do
      = daisy_checkbox(name: "checkbox_primary", css: "checkbox-primary", id: "checkbox_primary", checked: true)

      Primary

    = daisy_label(for: "checkbox_secondary") do
      = daisy_checkbox(name: "checkbox_secondary", css: "checkbox-secondary", id: "checkbox_secondary", checked: true)

      Secondary

    = daisy_label(for: "checkbox_accent") do
      = daisy_checkbox(name: "checkbox_accent", css: "checkbox-accent", id: "checkbox_accent", checked: true)

      Accent

    = daisy_label(for: "checkbox_success") do
      = daisy_checkbox(name: "checkbox_success", css: "checkbox-success", id: "checkbox_success", checked: true)

      Success

    = daisy_label(for: "checkbox_warning") do
      = daisy_checkbox(name: "checkbox_warning", css: "checkbox-warning", id: "checkbox_warning", checked: true)

      Warning

    = daisy_label(for: "checkbox_error") do
      = daisy_checkbox(name: "checkbox_error", css: "checkbox-error", id: "checkbox_error", checked: true)

      Error

    = daisy_label(for: "checkbox_info") do
      = daisy_checkbox(name: "checkbox_info", css: "checkbox-info", id: "checkbox_info", checked: true)

      Info
```

URL: /examples/Daisy::DataInput::CheckboxComponent#checkboxes-with-colors

-- Example: Checkbox Sizes
Description:
Checkboxes come in different sizes using DaisyUI's size classes.

Code (HAML):
```haml
= doc_example(title: "Checkbox Sizes") do |doc|
  - doc.with_description do
    Checkboxes come in different sizes using DaisyUI's size classes.



  .my-2.flex.flex-col.gap-8
    .flex.items-center.gap-4
      = daisy_label(for: "checkbox_extra_small") do
        = daisy_checkbox(name: "checkbox_extra_small", id: "checkbox_extra_small", css: "checkbox-xs", checked: true)

        Extra Small

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox_small") do
        = daisy_checkbox(name: "checkbox_small", id: "checkbox_small", css: "checkbox-sm", checked: true)

        Small

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox_medium") do
        = daisy_checkbox(name: "checkbox_medium", id: "checkbox_medium", css: "checkbox-md", checked: true)

        Medium (Default)

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox_large") do
        = daisy_checkbox(name: "checkbox_large", id: "checkbox_large", css: "checkbox-lg", checked: true)

        Large

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox_extra_large") do
        = daisy_checkbox(name: "checkbox_extra_large", id: "checkbox_extra_large", css: "checkbox-xl", checked: true)

        Extra Large
```

URL: /examples/Daisy::DataInput::CheckboxComponent#checkbox-sizes

-- Example: Combining Colors and Sizes
Description:
You can combine both color and size classes to create different variations.

Code (HAML):
```haml
= doc_example(title: "Combining Colors and Sizes") do |doc|
  - doc.with_description do
    You can combine both color and size classes to create different variations.



  .my-2.grid.grid-cols-1.md:grid-cols-2.lg:grid-cols-3.gap-6
    = daisy_label(for: "checkbox_primary_small") do
      = daisy_checkbox(name: "checkbox_primary_small", id: "checkbox_primary_small", css: "checkbox-primary checkbox-sm", checked: true)

      Primary Small

    = daisy_label(for: "checkbox_secondary_medium") do
      = daisy_checkbox(name: "checkbox_secondary_medium", id: "checkbox_secondary_medium", css: "checkbox-secondary checkbox-md", checked: true)

      Secondary Medium

    = daisy_label(for: "checkbox_accent_large") do
      = daisy_checkbox(name: "checkbox_accent_large", id: "checkbox_accent_large", css: "checkbox-accent checkbox-lg", checked: true)

      Accent Large

    = daisy_label(for: "checkbox_info_small") do
      = daisy_checkbox(name: "checkbox_info_small", id: "checkbox_info_small", css: "checkbox-info checkbox-sm", checked: true)

      Info Small

    = daisy_label(for: "checkbox_success_medium") do
      = daisy_checkbox(name: "checkbox_success_medium", id: "checkbox_success_medium", css: "checkbox-success checkbox-md", checked: true)

      Success Medium

    = daisy_label(for: "checkbox_warning_large") do
      = daisy_checkbox(name: "checkbox_warning_large", id: "checkbox_warning_large", css: "checkbox-warning checkbox-lg", checked: true)

      Warning Large
```

URL: /examples/Daisy::DataInput::CheckboxComponent#combining-colors-and-sizes

-- Example: Indeterminate State
Description:
Checkboxes can be set to an indeterminate state to indicate that the value is neither checked nor unchecked. This is commonly used for "Select All" checkboxes where some items are selected and others are not. doc_note(css: "mb-8") do You must set the `indeterminate` attribute through JavaScript.

Code (HAML):
```haml
= doc_example(title: "Indeterminate State") do |doc|
  - doc.with_description do
    Checkboxes can be set to an indeterminate state to indicate that the value
is neither checked nor unchecked. This is commonly used for "Select All"
checkboxes where some items are selected and others are not.



    = doc_note(css: "mb-8") do
      You must set the `indeterminate` attribute through JavaScript.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "checkbox_indeterminate_state") do
      = daisy_checkbox(name: "checkbox_indeterminate_state", id: "checkbox_indeterminate_state")

      Indeterminate Checkbox

    document.getElementById("checkbox_indeterminate_state").indeterminate = true;
```

URL: /examples/Daisy::DataInput::CheckboxComponent#indeterminate-state

-- Example: Disabled Checkbox
Description:
Checkboxes can be disabled using the `disabled` attribute.

Code (HAML):
```haml
= doc_example(title: "Disabled Checkbox") do |doc|
  - doc.with_description do
    Checkboxes can be disabled using the `disabled` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "checkbox_disabled_state", css: "cursor-not-allowed") do
      = daisy_checkbox(name: "checkbox_disabled_state", id: "checkbox_disabled_state", disabled: true)

      Disabled Checkbox
```

URL: /examples/Daisy::DataInput::CheckboxComponent#disabled-checkbox

-- Example: Custom ID
Description:
Checkboxes can be given a custom ID using the `id` attribute.

Code (HAML):
```haml
= doc_example(title: "Custom ID") do |doc|
  - doc.with_description do
    Checkboxes can be given a custom ID using the `id` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "custom-checkbox") do
      = daisy_checkbox(name: "checkbox12", id: "custom-checkbox")

      Checkbox with Custom ID
```

URL: /examples/Daisy::DataInput::CheckboxComponent#custom-id

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an even easier way to use checkboxes in your pages by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an even easier way to use
checkboxes in your pages by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user, class: "flex flex-col gap-8") do |form|
    = form.daisy_label(:terms) do
      = form.daisy_checkbox(:terms)

      I accept the terms and conditions

    = form.daisy_checkbox(:newsletter, toggle: true, end: "Subscribe to newsletter")
```

URL: /examples/Daisy::DataInput::CheckboxComponent#rails-form-example

-- Example: Using Label Component
Description:
You can also use the Label component directly with checkboxes for fully custom approach.

Code (HAML):
```haml
= doc_example(title: "Using Label Component") do |doc|
  - doc.with_description do
    You can also use the Label component directly with checkboxes for fully
custom approach.



  .my-2.flex.flex-col.gap-4
    .flex.items-center.space-x-2
      = daisy_checkbox(name: "agree", id: "agree")

      = daisy_label(for: "agree", title: "I agree to the terms of service")

    .flex.items-center.space-x-2.mt-4
      = daisy_checkbox(name: "updates", id: "updates", toggle: true)

      = daisy_label(for: "updates") do
        Receive product updates
```

URL: /examples/Daisy::DataInput::CheckboxComponent#using-label-component

---

=== Component: Daisy::DataInput::FileInputComponent
Group: DataInput
Title: File Inputs
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::FileInputComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/file_inputs.html.haml

Description:
Here are some examples showcasing file input components. 

Helpers:
- daisy_file_input

Examples:

-- Example: Basic File Input
Description:
File inputs allow users to select and upload files.

Code (HAML):
```haml
= doc_example(title: "Basic File Input") do |doc|
  - doc.with_description do
    File inputs allow users to select and upload files.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "file1", title: "Upload Document")

    = daisy_file_input(name: "file1", id: "file1")
```

URL: /examples/Daisy::DataInput::FileInputComponent#basic-file-input

-- Example: File Input with Accept Attribute
Description:
You can restrict the file types that can be selected using the `accept` attribute.

Code (HAML):
```haml
= doc_example(title: "File Input with Accept Attribute") do |doc|
  - doc.with_description do
    You can restrict the file types that can be selected using the `accept` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "image", title: "Upload Image")

    = daisy_file_input(name: "image", id: "image", accept: "image/*")

    = daisy_label(for: "pdf", title: "Upload PDF", css: "mt-4")

    = daisy_file_input(name: "pdf", id: "pdf", accept: ".pdf")
```

URL: /examples/Daisy::DataInput::FileInputComponent#file-input-with-accept-attribute

-- Example: Multiple File Selection
Description:
Enable multiple file selection with the `multiple` attribute.

Code (HAML):
```haml
= doc_example(title: "Multiple File Selection") do |doc|
  - doc.with_description do
    Enable multiple file selection with the `multiple` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "documents", title: "Upload Documents")

    = daisy_file_input(name: "documents[]", id: "documents", multiple: true)
```

URL: /examples/Daisy::DataInput::FileInputComponent#multiple-file-selection

-- Example: File Input with Different Colors
Description:
File inputs can be styled with additional CSS classes to change their appearance.

Code (HAML):
```haml
= doc_example(title: "File Input with Different Colors") do |doc|
  - doc.with_description do
    File inputs can be styled with additional CSS classes to change their appearance.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "file_primary", title: "Primary File Input")

    = daisy_file_input(name: "file_primary", id: "file_primary", css: "file-input-primary")

    = daisy_label(for: "file_secondary", title: "Secondary File Input", css: "mt-4")

    = daisy_file_input(name: "file_secondary", id: "file_secondary", css: "file-input-secondary")

    = daisy_label(for: "file_accent", title: "Accent File Input", css: "mt-4")

    = daisy_file_input(name: "file_accent", id: "file_accent", css: "file-input-accent")
```

URL: /examples/Daisy::DataInput::FileInputComponent#file-input-with-different-colors

-- Example: Ghost Style File Input
Description:
File inputs can use the `file-input-ghost` class for a minimalist appearance that only shows borders on focus.

Code (HAML):
```haml
= doc_example(title: "Ghost Style File Input") do |doc|
  - doc.with_description do
    File inputs can use the `file-input-ghost` class for a minimalist appearance
that only shows borders on focus.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "file_ghost", title: "Ghost Style File Input")

    = daisy_file_input(name: "file_ghost", id: "file_ghost", css: "file-input-ghost")

    .mt-4.p-4.border.rounded-lg.bg-accent.text-accent-content
      %p.text-sm.mb-2 Ghost style with background container:

      = daisy_label(for: "file_ghost_bg", title: "Upload Files")

      = daisy_file_input(name: "file_ghost_bg", id: "file_ghost_bg", css: "file-input-ghost")
```

URL: /examples/Daisy::DataInput::FileInputComponent#ghost-style-file-input

-- Example: Disabled File Input
Description:
File inputs can be disabled using the `disabled` attribute.

Code (HAML):
```haml
= doc_example(title: "Disabled File Input") do |doc|
  - doc.with_description do
    File inputs can be disabled using the `disabled` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "file_disabled", title: "Disabled File Input", css: "cursor-not-allowed")

    = daisy_file_input(name: "file_disabled", id: "file_disabled", disabled: true)
```

URL: /examples/Daisy::DataInput::FileInputComponent#disabled-file-input

-- Example: Required File Input
Description:
File inputs can be marked as required using the `required` attribute.

Code (HAML):
```haml
= doc_example(title: "Required File Input") do |doc|
  - doc.with_description do
    File inputs can be marked as required using the `required` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "file_required", title: "Required File Input")

    = daisy_file_input(name: "file_required", id: "file_required", required: true)
```

URL: /examples/Daisy::DataInput::FileInputComponent#required-file-input

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an easy way to use file inputs in your forms by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an easy way to use
file inputs in your forms by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user, html: { multipart: true }) do |form|
    .my-2.flex.flex-col.gap-4
      = form.daisy_label(:avatar)

      = form.daisy_file_input(:avatar)

      = form.daisy_label(:documents, "Upload Documents", css: "mt-4")

      = form.daisy_file_input(:documents, multiple: true)
```

URL: /examples/Daisy::DataInput::FileInputComponent#rails-form-example

---

=== Component: Daisy::DataInput::FieldsetComponent
Group: DataInput
Title: Fieldsets
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::FieldsetComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/fieldsets.html.haml

Description:
Fieldsets group related form controls and can optionally include a legend. 

Helpers:
- daisy_fieldset

Examples:

-- Example: Basic Fieldset (No Legend)
Description:
A simple fieldset rendered without a legend.

Code (HAML):
```haml
= doc_example(title: "Basic Fieldset (No Legend)") do |doc|
  - doc.with_description do
    A simple fieldset rendered without a legend.



  = daisy_fieldset do
    = daisy_toggle(end: "Enable Feature X")
```

URL: /examples/Daisy::DataInput::FieldsetComponent#basic-fieldset-no-legend

-- Example: Fieldset with Legend (Argument)
Description:
Pass simple text for the legend directly using the `legend:` argument.

Code (HAML):
```haml
= doc_example(title: "Fieldset with Legend (Argument)") do |doc|
  - doc.with_description do
    Pass simple text for the legend directly using the `legend:` argument.



  = daisy_fieldset(legend: "Settings (Argument)") do
    = daisy_toggle(end: "Enable Feature X")
```

URL: /examples/Daisy::DataInput::FieldsetComponent#fieldset-with-legend-argument

-- Example: Fieldset with Legend (Slot)
Description:
Use the `with_legend` slot to provide custom content for the legend, including HTML.

Code (HAML):
```haml
= doc_example(title: "Fieldset with Legend (Slot)") do |doc|
  - doc.with_description do
    Use the `with_legend` slot to provide custom content for the legend,
including HTML.



  = daisy_fieldset do |fieldset|
    - fieldset.with_legend { "Settings (Slot)" }

    = daisy_toggle(end: "Enable Notifications")

    = daisy_toggle(end: "Enable Dark Mode")
```

URL: /examples/Daisy::DataInput::FieldsetComponent#fieldset-with-legend-slot

-- Example: Fieldset with Various Inputs
Description:
Fieldsets are useful for grouping related form inputs under a common legend.

Code (HAML):
```haml
= doc_example(title: "Fieldset with Various Inputs") do |doc|
  - doc.with_description do
    Fieldsets are useful for grouping related form inputs under a common legend.



  = daisy_fieldset(legend: "User Profile") do
    .mt-4.space-y-4
      = daisy_input(floating: "Name")

      = daisy_text_area(placeholder: "Bio", rows: 3) # Text areas don't currently support floating labels

      = daisy_select(floating: "Country", options: ["USA", "Canada", "Mexico"])
```

URL: /examples/Daisy::DataInput::FieldsetComponent#fieldset-with-various-inputs

-- Example: Fieldset with Custom Styling
Description:
You can customize the Fieldset with standard `css` options to make it look exactly like you want.

Code (HAML):
```haml
= doc_example(title: "Fieldset with Custom Styling") do |doc|
  - doc.with_description do
    You can customize the Fieldset with standard `css` options to make it look
exactly like you want.



  = daisy_fieldset(css: "p-4 border rounded bg-base-200 w-80") do |fieldset|
    - fieldset.with_legend(css: "border rounded bg-base-100 px-4") { "Advanced Options" }

    = daisy_label("API Key")

    = daisy_input(placeholder: "asdf1234")

    = daisy_button("Save Settings", css: "mt-4")
```

URL: /examples/Daisy::DataInput::FieldsetComponent#fieldset-with-custom-styling

---

=== Component: Daisy::DataInput::LabelComponent
Group: DataInput
Title: Labels
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::LabelComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/labels.html.haml

Description:
Labels provide a useful description for various kinds of inputs. 

Helpers:
- daisy_label

Examples:

-- Example: Basic Labels
Description:
Labels are used to provide text descriptions for form inputs. They can be used with any form input component.

Code (HAML):
```haml
= doc_example(title: "Basic Labels") do |doc|
  - doc.with_description do
    Labels are used to provide text descriptions for form inputs. They can be
used with any form input component.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input1", title: "Standard Label")

    = daisy_label(for: "input2") do
      Content Block Label
```

URL: /examples/Daisy::DataInput::LabelComponent#basic-labels

-- Example: With Form Inputs
Description:
Labels can be used with form inputs to provide a description for the input. Here's an example with a checkbox.

Code (HAML):
```haml
= doc_example(title: "With Form Inputs") do |doc|
  - doc.with_description do
    Labels can be used with form inputs to provide a description for the input.
Here's an example with a checkbox.



  .my-2.flex.flex-col.gap-4
    .flex.items-center.space-x-2
      = daisy_checkbox(name: "accept", id: "accept")

      = daisy_label(for: "accept", title: "I accept the terms and conditions")
```

URL: /examples/Daisy::DataInput::LabelComponent#with-form-inputs

-- Example: LabelableComponent Functionality
Description:
Many of our input components include the `LabelableComponent` concern, which provides built-in label functionality without needing separate label components. This makes your code cleaner and ensures proper semantic markup. The component_link("TextInput", "Daisy::DataInput::TextInputComponent") , component_link("Select", "Daisy::DataInput::SelectComponent") , component_link("Checkbox", "Daisy::DataInput::CheckboxComponent") , and component_link("Toggle", "Daisy::DataInput::ToggleComponent") components include this functionality.

Code (HAML):
```haml
= doc_example(title: "LabelableComponent Functionality") do |doc|
  - doc.with_description do
    Many of our input components include the `LabelableComponent` concern,
which provides built-in label functionality without needing separate label
components. This makes your code cleaner and ensures proper semantic
markup.



    %p
      The

      = component_link("TextInput", "Daisy::DataInput::TextInputComponent")

      ,

      = component_link("Select", "Daisy::DataInput::SelectComponent")

      ,

      = component_link("Checkbox", "Daisy::DataInput::CheckboxComponent")

      , and

      = component_link("Toggle", "Daisy::DataInput::ToggleComponent")

      components include this functionality.

  .my-2.flex.flex-col.gap-4
    %p.font-medium.mb-2 Start Labels (appear before the input)

    = daisy_text_input(name: "username", id: "username", start: "Username:")

    .mt-4
      = daisy_select(name: "country", id: "country", start: "Country:", options: ["USA", "Canada", "UK"])
```

URL: /examples/Daisy::DataInput::LabelComponent#labelablecomponent-functionality

-- Example: End Labels
Description:
End labels appear after the input. They are especially useful for checkboxes and toggles, but can be used with any input component that includes the `LabelableComponent` concern.

Code (HAML):
```haml
= doc_example(title: "End Labels") do |doc|
  - doc.with_description do
    End labels appear after the input. They are especially useful for checkboxes
and toggles, but can be used with any input component that includes the
`LabelableComponent` concern.



  .my-2.flex.flex-col.gap-4
    = daisy_checkbox(name: "terms", id: "terms", end: "I agree to the terms and conditions")

    .mt-4
      = daisy_toggle(name: "notifications", id: "notifications", end: "Enable notifications")

    .mt-4
      = daisy_text_input(name: "email", id: "email", end: "@example.com")
```

URL: /examples/Daisy::DataInput::LabelComponent#end-labels

-- Example: Floating Labels
Description:
Floating labels appear above the input when it has focus or contains a value. This provides a clean, modern look while maintaining good UX.

Code (HAML):
```haml
= doc_example(title: "Floating Labels") do |doc|
  - doc.with_description do
    Floating labels appear above the input when it has focus or contains a value.
This provides a clean, modern look while maintaining good UX.



  .my-2.flex.flex-col.gap-4
    = daisy_text_input(name: "email", id: "email_floating", floating: "Email Address", placeholder: "example@domain.com")

    .mt-4
      = daisy_select(name: "language", id: "language_floating", floating: "Preferred Language", options: ["English", "Spanish", "French"])
```

URL: /examples/Daisy::DataInput::LabelComponent#floating-labels

-- Example: Custom Label Content
Description:
For more complex labels, you can use the slot API to provide custom content to your labels. This is useful when you need to add HTML or other components within your label.

Code (HAML):
```haml
= doc_example(title: "Custom Label Content") do |doc|
  - doc.with_description do
    For more complex labels, you can use the slot API to provide custom content
to your labels. This is useful when you need to add HTML or other components
within your label.



  .my-2.flex.flex-col.gap-4
    = daisy_text_input(name: "password", id: "password", type: "password") do |input|
      - input.with_start do
        Password

        %span.text-error.ml-1 *

    .mt-4
      = daisy_checkbox(name: "newsletter", id: "newsletter") do |cb|
        - cb.with_end do
          Subscribe to newsletter

          %span.text-xs.text-gray-500.ml-1 (Optional)
```

URL: /examples/Daisy::DataInput::LabelComponent#custom-label-content

-- Example: Form Builder Example
Description:
We've also extended the standard Rails FormBuilder with custom helper methods for rendering Daisy Inputs with labels.

Code (HAML):
```haml
= doc_example(title: "Form Builder Example") do |doc|
  - doc.with_description do
    We've also extended the standard Rails FormBuilder with custom helper
methods for rendering Daisy Inputs with labels.



  = form_with(url: "#", class: "my-2 flex flex-col gap-4") do |f|
    = f.daisy_text_input :name, floating_placeholder: "Name"

    = f.daisy_text_input :email, type: :email, floating_placeholder: "Email"
```

URL: /examples/Daisy::DataInput::LabelComponent#form-builder-example

---

=== Component: Daisy::DataInput::RadioButtonComponent
Group: DataInput
Title: Radio Buttons
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::RadioButtonComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/radio_buttons.html.haml

Description:
Here are some examples showcasing radio button components. 

Helpers:
- daisy_radio

Examples:

-- Example: Basic Radio Buttons
Description:
Radio buttons are used to select a single option from a set of options. Radio buttons with the same name attribute are automatically grouped together.

Code (HAML):
```haml
= doc_example(title: "Basic Radio Buttons") do |doc|
  - doc.with_description do
    Radio buttons are used to select a single option from a set of options.
Radio buttons with the same name attribute are automatically grouped together.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "radio1") do
      = daisy_radio(name: "radio_group1", id: "radio1", value: "1")

      Option 1

    = daisy_label(for: "radio2") do
      = daisy_radio(name: "radio_group1", id: "radio2", value: "2", checked: true)

      Option 2

    = daisy_label(for: "radio3") do
      = daisy_radio(name: "radio_group1", id: "radio3", value: "3")

      Option 3
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#basic-radio-buttons

-- Example: Radio Buttons with Colors
Description:
Radio buttons can be styled with different colors using DaisyUI utility classes.

Code (HAML):
```haml
= doc_example(title: "Radio Buttons with Colors") do |doc|
  - doc.with_description do
    Radio buttons can be styled with different colors using DaisyUI utility classes.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "radio4") do
      = daisy_radio(name: "radio_group2", id: "radio4", value: "1", css: "radio-primary")

      Primary Radio Button

    = daisy_label(for: "radio5") do
      = daisy_radio(name: "radio_group2", id: "radio5", value: "2", css: "radio-secondary")

      Secondary Radio Button

    = daisy_label(for: "radio6") do
      = daisy_radio(name: "radio_group2", id: "radio6", value: "3", css: "radio-accent")

      Accent Radio Button
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#radio-buttons-with-colors

-- Example: Radio Button States
Description:
Radio buttons can be in different states such as checked, disabled, or required.

Code (HAML):
```haml
= doc_example(title: "Radio Button States") do |doc|
  - doc.with_description do
    Radio buttons can be in different states such as checked, disabled, or required.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "radio7") do
      = daisy_radio(name: "radio_group3", id: "radio7", value: "1")

      Standard Radio Button

    = daisy_label(for: "radio8") do
      = daisy_radio(name: "radio_group3", id: "radio8", value: "2", checked: true)

      Checked Radio Button

    = daisy_label(for: "radio9", css: "cursor-not-allowed") do
      = daisy_radio(name: "radio_group3", id: "radio9", value: "3", disabled: true)

      Disabled Radio Button

    = daisy_label(for: "radio10") do
      = daisy_radio(name: "radio_group3", id: "radio10", value: "4", required: true)

      Required Radio Button
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#radio-button-states

-- Example: Disabled Radio Button
Description:
Radio buttons can be disabled using the `disabled` attribute.

Code (HAML):
```haml
= doc_example(title: "Disabled Radio Button") do |doc|
  - doc.with_description do
    Radio buttons can be disabled using the `disabled` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "radio11", css: "cursor-not-allowed") do
      = daisy_radio(name: "radio_group4", id: "radio11", value: "1", disabled: true)

      Disabled Radio Button
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#disabled-radio-button

-- Example: Custom ID
Description:
Radio buttons can be given a custom ID using the `id` attribute.

Code (HAML):
```haml
= doc_example(title: "Custom ID") do |doc|
  - doc.with_description do
    Radio buttons can be given a custom ID using the `id` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "custom-radio-id") do
      = daisy_radio(name: "radio_group5", id: "custom-radio-id", value: "1")

      Radio Button with Custom ID
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#custom-id

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an even easier way to use radio buttons in your pages by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an even easier way to use
radio buttons in your pages by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user) do |form|
    .flex.flex-col.gap-4
      = form.daisy_label(:gender_male) do
        = form.daisy_radio(:gender, value: "male")

        Male

      = form.daisy_label(:gender_female) do
        = form.daisy_radio(:gender, value: "female")

        Female

      = form.daisy_label(:gender_other) do
        = form.daisy_radio(:gender, value: "other")

        Other
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#rails-form-example

-- Example: Using Label Component
Description:
You can also use the Label component directly with radio buttons for a fully custom approach.

Code (HAML):
```haml
= doc_example(title: "Using Label Component") do |doc|
  - doc.with_description do
    You can also use the Label component directly with radio buttons for a fully
custom approach.



  .my-2.flex.flex-col.gap-4
    .flex.items-center.space-x-2
      = daisy_radio(name: "contact_preference", id: "email", value: "email")

      = daisy_label(for: "email", title: "Email")

    .flex.items-center.space-x-2.mt-4
      = daisy_radio(name: "contact_preference", id: "phone", value: "phone")

      = daisy_label(for: "phone", title: "Phone")

    .flex.items-center.space-x-2.mt-4
      = daisy_radio(name: "contact_preference", id: "mail", value: "mail")

      = daisy_label(for: "mail", title: "Mail")
```

URL: /examples/Daisy::DataInput::RadioButtonComponent#using-label-component

---

=== Component: Daisy::DataInput::RangeComponent
Group: DataInput
Title: Range Inputs
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::RangeComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/ranges.html.haml

Description:
Here are some examples showcasing range input components. 

Helpers:
- daisy_range

Examples:

-- Example: Basic Range
Description:
Range inputs allow users to select a value within a specific range by sliding a handle along a track.

Code (HAML):
```haml
= doc_example(title: "Basic Range") do |doc|
  - doc.with_description do
    Range inputs allow users to select a value within a specific range by sliding
a handle along a track.



  .w-full.max-w-xs.my-2.space-y-2
    = daisy_label(for: "volume", title: "Volume")

    = daisy_range(name: "volume", id: "volume", min: 0, max: 100, value: 50)
```

URL: /examples/Daisy::DataInput::RangeComponent#basic-range

-- Example: Range with Colors
Description:
Range inputs can be styled with different colors using DaisyUI utility classes.

Code (HAML):
```haml
= doc_example(title: "Range with Colors") do |doc|
  - doc.with_description do
    Range inputs can be styled with different colors using DaisyUI utility classes.



  .w-full.max-w-xs.my-2.space-y-6
    .space-y-2
      = daisy_label(for: "primary_range", title: "Primary Range")

      = daisy_range(name: "primary_range", id: "primary_range", value: 60, css: "range-primary")

    .space-y-2
      = daisy_label(for: "secondary_range", title: "Secondary Range")

      = daisy_range(name: "secondary_range", id: "secondary_range", value: 70, css: "range-secondary")

    .space-y-2
      = daisy_label(for: "accent_range", title: "Accent Range")

      = daisy_range(name: "accent_range", id: "accent_range", value: 80, css: "range-accent")

    .space-y-2
      = daisy_label(for: "success_range", title: "Success Range")

      = daisy_range(name: "success_range", id: "success_range", value: 90, css: "range-success")

    .space-y-2
      = daisy_label(for: "warning_range", title: "Warning Range")

      = daisy_range(name: "warning_range", id: "warning_range", value: 30, css: "range-warning")

    .space-y-2
      = daisy_label(for: "error_range", title: "Error Range")

      = daisy_range(name: "error_range", id: "error_range", value: 10, css: "range-error")
```

URL: /examples/Daisy::DataInput::RangeComponent#range-with-colors

-- Example: Range with Steps
Description:
Range inputs can be configured with specific step values and visual indicators to help users select precise values.

Code (HAML):
```haml
= doc_example(title: "Range with Steps") do |doc|
  - doc.with_description do
    Range inputs can be configured with specific step values and visual indicators
to help users select precise values.



  .w-full.max-w-sm
    = daisy_label(for: "step_range", title: "Satisfaction Level")

    = daisy_range(name: "step_range", id: "step_range", css: "w-full", min: 0, max: 100, step: 25, value: 50)

    .flex.justify-between.px-2.mt-2.text-xs
      %span "|"

      %span "|"

      %span "|"

      %span "|"

      %span "|"

    .flex.justify-between.px-2.mt-2.text-xs
      %span 1

      %span 2

      %span 3

      %span 4

      %span 5
```

URL: /examples/Daisy::DataInput::RangeComponent#range-with-steps

-- Example: Range States
Description:
Range inputs can be in different states such as disabled or required.

Code (HAML):
```haml
= doc_example(title: "Range States") do |doc|
  - doc.with_description do
    Range inputs can be in different states such as disabled or required.



  .w-full.max-w-xs.my-2.space-y-2
    = daisy_label(for: "disabled_range", title: "Disabled Range", css: "cursor-not-allowed")

    = daisy_range(name: "disabled_range", id: "disabled_range", value: 50, disabled: true)

    = daisy_label(for: "required_range", title: "Required Range")

    = daisy_range(name: "required_range", id: "required_range", value: 75, required: true)
```

URL: /examples/Daisy::DataInput::RangeComponent#range-states

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an even easier way to use range inputs in your pages by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an even easier way to use
range inputs in your pages by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user, html: { class: "w-full max-w-xs space-y-2" }) do |form|
    = form.daisy_label(:volume, "Volume Control") do
      .flex.items-center.gap-2
        = hero_icon("speaker-wave", class: "size-4 inline-block")

        Volume Control

    = form.daisy_range(:volume, min: 0, max: 100, step: 5, value: 65)

    = form.daisy_label(:brightness, "Brightness") do
      .flex.items-center.gap-2
        = hero_icon("sun", class: "size-4 inline-block")

        Brightness

    = form.daisy_range(:brightness, css: "range-primary", value: 80)
```

URL: /examples/Daisy::DataInput::RangeComponent#rails-form-example

---

=== Component: Daisy::DataInput::RatingComponent
Group: DataInput
Title: Rating Inputs
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::RatingComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/ratings.html.haml

Description:
Here are some examples showcasing rating input components. 

Helpers:
- daisy_rating

Examples:

-- Example: Basic Rating
Description:
Rating inputs allow users to provide a rating by selecting from a set of stars. They are implemented using radio buttons styled with DaisyUI's mask classes.

Code (HAML):
```haml
= doc_example(title: "Basic Rating") do |doc|
  - doc.with_description do
    Rating inputs allow users to provide a rating by selecting from a set of
stars. They are implemented using radio buttons styled with DaisyUI's mask
classes.



  .flex.flex-col.max-w-xs.my-2.space-y-2
    = daisy_label(for: "basic_rating", title: "Basic Rating")

    = daisy_rating(name: "basic_rating", id: "basic_rating")
```

URL: /examples/Daisy::DataInput::RatingComponent#basic-rating

-- Example: Rating with Heart Style
Description:
The rating style can be customized using DaisyUI's mask classes. Here we use the `mask-heart` class for a heart-shaped rating.

Code (HAML):
```haml
= doc_example(title: "Rating with Heart Style") do |doc|
  - doc.with_description do
    The rating style can be customized using DaisyUI's mask classes. Here we use
the `mask-heart` class for a heart-shaped rating.



  .flex.flex-col.max-w-xs.my-2.space-y-2
    = daisy_label(for: "heart_rating", title: "Heart Rating")

    = daisy_rating(name: "heart_rating", id: "heart_rating", css: "space-x-1", inputs_css: "mask-heart bg-red-500", value: 2)
```

URL: /examples/Daisy::DataInput::RatingComponent#rating-with-heart-style

-- Example: Rating with Colors
Description:
Rating inputs can be styled with different colors using DaisyUI utility classes.

Code (HAML):
```haml
= doc_example(title: "Rating with Colors") do |doc|
  - doc.with_description do
    Rating inputs can be styled with different colors using DaisyUI utility
classes.



  .max-w-xs.my-2.space-y-6
    .flex.flex-col.space-y-2
      = daisy_label(for: "primary_rating", title: "Primary Color Rating")

      = daisy_rating(name: "primary_rating", id: "primary_rating", value: 2, inputs_css: "bg-primary")

    .flex.flex-col.space-y-2
      = daisy_label(for: "warning_rating", title: "Warning Color Rating")

      = daisy_rating(name: "warning_rating", id: "warning_rating", value: 3, inputs_css: "bg-warning")

    .flex.flex-col.space-y-2
      = daisy_label(for: "success_rating", title: "Success Color Rating")

      = daisy_rating(name: "success_rating", id: "success_rating", value: 4, inputs_css: "bg-success")
```

URL: /examples/Daisy::DataInput::RatingComponent#rating-with-colors

-- Example: Rating with Different Sizes
Description:
Rating inputs can be rendered in different sizes using DaisyUI's size classes.

Code (HAML):
```haml
= doc_example(title: "Rating with Different Sizes") do |doc|
  - doc.with_description do
    Rating inputs can be rendered in different sizes using DaisyUI's size
classes.



  .max-w-xs.my-2.space-y-6
    .flex.flex-col.space-y-2
      = daisy_label(for: "rating_xs", title: "Extra Small Rating")

      = daisy_rating(name: "rating_xs", id: "rating_xs", css: "rating-xs")

    .flex.flex-col.space-y-2
      = daisy_label(for: "rating_sm", title: "Small Rating")

      = daisy_rating(name: "rating_sm", id: "rating_sm", css: "rating-sm")

    .flex.flex-col.space-y-2
      = daisy_label(for: "rating_md", title: "Medium Rating (Default)")

      = daisy_rating(name: "rating_md", id: "rating_md")

    .flex.flex-col.space-y-2
      = daisy_label(for: "rating_lg", title: "Large Rating")

      = daisy_rating(name: "rating_lg", id: "rating_lg", css: "rating-lg")
```

URL: /examples/Daisy::DataInput::RatingComponent#rating-with-different-sizes

-- Example: Half-Star Rating
Description:
Half-star ratings can be achieved by using the `mask-half-1` and `mask-half-2` classes along with a higher max value. Each star is made up of two inputs, one for each half.

Code (HAML):
```haml
= doc_example(title: "Half-Star Rating") do |doc|
  - doc.with_description do
    Half-star ratings can be achieved by using the `mask-half-1` and
`mask-half-2` classes along with a higher max value. Each star is made up of
two inputs, one for each half.



  .flex.flex-col.max-w-xs.my-2.space-y-2
    = daisy_label(for: "half_star_rating", title: "Half-Star Rating")

    = daisy_rating(name: "half_star_rating", id: "half_star_rating", css: "rating-half rating-lg") do |rating|
      - (1..10).each do |i|
        - rating.with_item(css: "mask mask-star mask-half-#{i.even? ? 2 : 1}")
```

URL: /examples/Daisy::DataInput::RatingComponent#half-star-rating

-- Example: Read-Only Rating
Description:
Rating inputs can be set to read-only / disdbled mode to display a static rating that cannot be changed. doc_note(css: "mb-8") do You may also want to add the `cursor-not-allowed!` class to the rating's `inputs_css` to make each radio button appear disabled.

Code (HAML):
```haml
= doc_example(title: "Read-Only Rating") do |doc|
  - doc.with_description do
    Rating inputs can be set to read-only / disdbled mode to display a static
rating that cannot be changed.



    = doc_note(css: "mb-8") do
      You may also want to add the `cursor-not-allowed!` class to the rating's
`inputs_css` to make each radio button appear disabled.



  .flex.flex-col.max-w-xs.my-2.space-y-2
    = daisy_label(for: "disabled_rating", title: "Disabled Rating (3/5)")

    = daisy_rating(name: "disabled_rating", id: "disabled_rating", inputs_css: "cursor-not-allowed!", value: 3, disabled: true)
```

URL: /examples/Daisy::DataInput::RatingComponent#read-only-rating

-- Example: Custom Content Rating
Description:
You can provide custom content to the Rating component using ViewComponent's standard `content` feature. This gives you full control over the input elements while still maintaining the component's structure and behavior. Note that you'll need to set the proper attributes on your inputs to ensure they work correctly with the component.

Code (HAML):
```haml
= doc_example(title: "Custom Content Rating") do |doc|
  - doc.with_description do
    You can provide custom content to the Rating component using ViewComponent's
standard `content` feature. This gives you full control over the input
elements while still maintaining the component's structure and behavior.

Note that you'll need to set the proper attributes on your inputs to ensure
they work correctly with the component.



  .flex.flex-col.max-w-xs.my-2.space-y-2
    = daisy_label(for: "custom_rating", title: "Custom Rating")

    = daisy_rating(name: "custom_rating", id: "custom_rating", css: "gap-4") do
      %input.mask.mask-triangle.bg-accent{type: "radio", name: "custom_rating", value: "1"}

      %input.mask.mask-square.scale-90.bg-info{type: "radio", name: "custom_rating", value: "2"}

      %input.mask.mask-pentagon.-translate-y-px.bg-success{type: "radio", name: "custom_rating", value: "3"}

      %input.mask.mask-decagon.bg-primary{type: "radio", name: "custom_rating", value: "4"}

      %input.mask.mask-circle.bg-secondary{type: "radio", name: "custom_rating", value: "5"}
```

URL: /examples/Daisy::DataInput::RatingComponent#custom-content-rating

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an even easier way to use rating inputs in your pages by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an even easier way to use
rating inputs in your pages by extracting the name and ID attributes from
the form object and attributes.



  = form_with(url: "#", method: :post, scope: :review, html: { class: "max-w-xs space-y-4" }) do |form|
    .flex.flex-col.space-y-2
      = form.daisy_label(:rating) do
        .flex.items-center.gap-2
          = hero_icon("star", class: "size-4 inline-block")

          Your Rating

      = form.daisy_rating(:rating, inputs_css: "mask-star-2")

    .flex.flex-col.space-y-2
      = form.daisy_label(:comment) do
        .flex.items-center.gap-2
          = hero_icon("chat-bubble-bottom-center-text", class: "size-4 inline-block")

          Your Comment

      = form.text_area(:comment, class: "textarea textarea-bordered h-24")

    = form.submit "Submit Review", class: "btn btn-primary"
```

URL: /examples/Daisy::DataInput::RatingComponent#rails-form-example

---

=== Component: Daisy::DataInput::SelectComponent
Group: DataInput
Title: Select Inputs
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::SelectComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/selects.html.haml

Description:
doc_note do Select inputs have a border by default and a width of 20rem. Use `select-ghost` to remove the border. 

Helpers:
- daisy_select

Examples:

-- Example: Basic Select
Description:
Select inputs allow users to choose an option from a dropdown list. They are implemented using the native HTML select element styled with DaisyUI classes.

Code (HAML):
```haml
= doc_example(title: "Basic Select") do |doc|
  - doc.with_description do
    Select inputs allow users to choose an option from a dropdown list. They are
implemented using the native HTML select element styled with DaisyUI classes.



  .flex.flex-col.gap-2
    = daisy_label(for: "basic_select", title: "Basic Select")

    = daisy_select(name: "basic_select", id: "basic_select", placeholder: "Select a country") do |select|
      - select.with_option(value: "us", label: "United States")

      - select.with_option(value: "ca", label: "Canada")

      - select.with_option(value: "mx", label: "Mexico")

    = daisy_label(css: "mt-2", for: "left_label_select", title: "Country")

    = daisy_select(name: "left_label_select", id: "left_label_select", placeholder: "Select a country", start: "Country") do |select|
      - select.with_option(value: "us", label: "United States")

      - select.with_option(value: "ca", label: "Canada")

      - select.with_option(value: "mx", label: "Mexico")

    = daisy_label(css: "mt-2", for: "right_label_select", title: "Country")

    = daisy_tip("DaisyUI end-labeled selects don't currently allow you to select the label area.", css: "tooltip-warning") do
      = daisy_select(name: "right_label_select", id: "right_label_select", placeholder: "Select a country", end: "Country") do |select|
        - select.with_option(value: "us", label: "United States")

        - select.with_option(value: "ca", label: "Canada")

        - select.with_option(value: "mx", label: "Mexico")

    = daisy_select(label_wrapper_css: "mt-8", name: "floating_label_select", id: "floating_label_select", include_blank: true, floating: "Country", placeholder: "Select a country...") do |select|
      - select.with_option(value: "us", label: "United States")

      - select.with_option(value: "ca", label: "Canada")

      - select.with_option(value: "mx", label: "Mexico")
```

URL: /examples/Daisy::DataInput::SelectComponent#basic-select

-- Example: Select with Simple Options
Description:
For simpler cases, you can pass a list of strings as options. The value and label will both be set to the string value. Or you can pass an array of Hashes. You can set the label and value keys through the `option_label` and `option_value` parameters.

Code (HAML):
```haml
= doc_example(title: "Select with Simple Options") do |doc|
  - doc.with_description do
    For simpler cases, you can pass a list of strings as options. The value and
label will both be set to the string value.

Or you can pass an array of Hashes. You can set the label and value keys
through the `option_label` and `option_value` parameters.



  .flex.flex-col.gap-8
    - tshirt_sizes = ["Small", "Medium", "Large", "X-Large"]

    = daisy_select(css: "w-60", name: "tshirt_size", id: "tshirt_size", floating: "Select a t-shirt size", options: tshirt_sizes)

    table_sizes = [
  { code: "sm", display: "Small (fits 1-2 people)" },
  { code: "md", display: "Medium (fits 2-4 people)" },
  { code: "lg", display: "Large (fits 4-6 people)" },
  { code: "xl", display: "X-Large (fits 6+ people)" }
]



    = daisy_select(name: "table_size", id: "table_size", floating: "Select a table size", options: table_sizes, option_label: :display, option_value: :code)
```

URL: /examples/Daisy::DataInput::SelectComponent#select-with-simple-options

-- Example: Select Sizes
Description:
Select inputs can be rendered in different sizes using DaisyUI's size classes.

Code (HAML):
```haml
= doc_example(title: "Select Sizes") do |doc|
  - doc.with_description do
    Select inputs can be rendered in different sizes using DaisyUI's size
classes.



  .my-2.space-y-6
    .space-y-2
      = daisy_label(for: "select_xs", title: "Extra Small Select")

      = daisy_select(name: "select_xs", id: "select_xs", css: "select-xs", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")

    .space-y-2
      = daisy_label(for: "select_sm", title: "Small Select")

      = daisy_select(name: "select_sm", id: "select_sm", css: "select-sm", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")

    .space-y-2
      = daisy_label(for: "select_md", title: "Medium Select (Default)")

      = daisy_select(name: "select_md", id: "select_md", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")

    .space-y-2
      = daisy_label(for: "select_lg", title: "Large Select")

      = daisy_select(name: "select_lg", id: "select_lg", css: "select-lg", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")
```

URL: /examples/Daisy::DataInput::SelectComponent#select-sizes

-- Example: Select with Colors
Description:
Select inputs can be styled with different colors using DaisyUI's color classes.

Code (HAML):
```haml
= doc_example(title: "Select with Colors") do |doc|
  - doc.with_description do
    Select inputs can be styled with different colors using DaisyUI's color
classes.



  .my-2.space-y-6
    .space-y-2
      = daisy_label(for: "select_primary", title: "Primary Select")

      = daisy_select(name: "select_primary", id: "select_primary", css: "select-primary", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")

    .space-y-2
      = daisy_label(for: "select_secondary", title: "Secondary Select")

      = daisy_select(name: "select_secondary", id: "select_secondary", css: "select-secondary", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")

    .space-y-2
      = daisy_label(for: "select_accent", title: "Accent Select")

      = daisy_select(name: "select_accent", id: "select_accent", css: "select-accent", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")

    .space-y-2
      = daisy_label(for: "select_warning", title: "Warning Select")

      = daisy_select(name: "select_warning", id: "select_warning", css: "select-warning", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")
```

URL: /examples/Daisy::DataInput::SelectComponent#select-with-colors

-- Example: Ghost Select
Description:
Select inputs can be styled without a border using the `select-ghost` class. The border will appear on focus.

Code (HAML):
```haml
= doc_example(title: "Ghost Select") do |doc|
  - doc.with_description do
    Select inputs can be styled without a border using the `select-ghost` class.
The border will appear on focus.



  .my-2.space-y-6
    .space-y-2
      = daisy_label(for: "select_ghost", title: "Ghost Select")

      = daisy_select(name: "select_ghost", id: "select_ghost", css: "select-ghost", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option")
```

URL: /examples/Daisy::DataInput::SelectComponent#ghost-select

-- Example: Disabled Select
Description:
Select inputs can be disabled using the `disabled` attribute.

Code (HAML):
```haml
= doc_example(title: "Disabled Select") do |doc|
  - doc.with_description do
    Select inputs can be disabled using the `disabled` attribute.



  .my-2.space-y-2
    = daisy_label(for: "disabled_select", title: "Disabled Select", css: "cursor-not-allowed")

    = daisy_select(name: "disabled_select", id: "disabled_select", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option", disabled: true)
```

URL: /examples/Daisy::DataInput::SelectComponent#disabled-select

-- Example: Required Select
Description:
Select inputs can be marked as required using the `required` attribute.

Code (HAML):
```haml
= doc_example(title: "Required Select") do |doc|
  - doc.with_description do
    Select inputs can be marked as required using the `required` attribute.



  .my-2.space-y-2
    = daisy_label(for: "required_select", title: "Required Select *")

    = daisy_select(name: "required_select", id: "required_select", options: ["Option 1", "Option 2", "Option 3"], placeholder: "Select an option", required: true)
```

URL: /examples/Daisy::DataInput::SelectComponent#required-select

-- Example: Pre-Selected Value
Description:
Select inputs can have a pre-selected value by setting the `value` attribute.

Code (HAML):
```haml
= doc_example(title: "Pre-Selected Value") do |doc|
  - doc.with_description do
    Select inputs can have a pre-selected value by setting the `value` attribute.



  .my-2.space-y-2
    = daisy_label(for: "preselected_select", title: "Pre-Selected Select")

    = daisy_select(name: "preselected_select", id: "preselected_select", placeholder: "Select a country") do |select|
      - select.with_option(value: "us", label: "United States")

      - select.with_option(value: "ca", label: "Canada", selected: true)

      - select.with_option(value: "mx", label: "Mexico")
```

URL: /examples/Daisy::DataInput::SelectComponent#pre-selected-value

-- Example: Block Syntax for Options
Description:
You can use block syntax to define options for more control over each option.

Code (HAML):
```haml
= doc_example(title: "Block Syntax for Options") do |doc|
  - doc.with_description do
    You can use block syntax to define options for more control over each option.



  .my-2.space-y-2
    = daisy_label(for: "block_select", title: "Block Syntax Select")

    = daisy_select(name: "block_select", id: "block_select", css: "select-primary") do |select|
      - select.with_option(value: "red", label: "Red")

      - select.with_option(value: "green", label: "Green")

      - select.with_option(value: "blue", label: "Blue")

      - select.with_option(value: "disabled_option", label: "Disabled Option", disabled: true)
```

URL: /examples/Daisy::DataInput::SelectComponent#block-syntax-for-options

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an even easier way to use select inputs in your pages by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an even easier way to use
select inputs in your pages by extracting the name and ID attributes from
the form object and attributes.



  = form_with(url: "#", method: :get, scope: :product, html: { class: "space-y-4" }) do |form|
    .space-y-2
      = form.daisy_label(:category) do
        .flex.items-center.gap-2
          = hero_icon("tag", class: "size-4 inline-block")

          Product Category

      = form.daisy_select(:category, placeholder: "Select a category") do |select|
        - select.with_option(value: "electronics", label: "Electronics")

        - select.with_option(value: "clothing", label: "Clothing")

        - select.with_option(value: "home", label: "Home & Kitchen")

        - select.with_option(value: "books", label: "Books")

    .space-y-2
      - options = [ "Under $25", "$25 - $50", "$50 - $100", "$100 - $200", "Over $200" ]

      = form.daisy_label(:price_range) do
        .flex.items-center.gap-2
          = hero_icon("currency-dollar", class: "size-4 inline-block")

          Price Range

      = form.daisy_select(:price_range, options: options, placeholder: "Select a price range")

    = form.submit "Filter Results", class: "mt-2 btn btn-primary w-full"
```

URL: /examples/Daisy::DataInput::SelectComponent#rails-form-example

---

=== Component: Daisy::DataInput::TextInputComponent
Group: DataInput
Title: Text Inputs
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::TextInputComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/text_inputs.html.haml

Description:
Text inputs allow you to enter various forms of text such as usernames, passwords, numbers, and more. 

Helpers:
- daisy_input
- daisy_text_input

Examples:

-- Example: Basic Text Input
Description:
Text inputs can be used with or without labels. doc_note(css: "mb-8") do Adding the `label` class to the `start/end` slots will apply a thin separator between the label and the input.

Code (HAML):
```haml
= doc_example(title: "Basic Text Input") do |doc|
  - doc.with_description do
    Text inputs can be used with or without labels.



    = doc_note(css: "mb-8") do
      Adding the `label` class to the `start/end` slots will apply a thin
separator between the label and the input.



  .flex.flex-col.gap-4
    = daisy_label("Basic Inputs")

    = daisy_text_input(name: "username_input", id: "username_input", placeholder: "Username")

    = daisy_text_input(name: "password_input", id: "password_input", placeholder: "Password")

    = daisy_divider("OR")

    .flex.flex-col.gap-2
      = daisy_label(for: "url_input", title: "URL")

      = daisy_text_input(name: "url_input", id: "url_input", start: "https://", start_css: "label")

    .flex.flex-col.gap-2
      = daisy_label(for: "email_input", title: "Email")

      = daisy_text_input(name: "email_input", id: "email_input") do |text|
        - text.with_end(css: "label") do
          @gmail.com
```

URL: /examples/Daisy::DataInput::TextInputComponent#basic-text-input

-- Example: Floating Label Inputs
Description:
Inputs can also utilize a floating label with or without a placeholder to control animation.

Code (HAML):
```haml
= doc_example(title: "Floating Label Inputs") do |doc|
  - doc.with_description do
    Inputs can also utilize a floating label with or without a placeholder to
control animation.



  .flex.flex-col.gap-4
    .space-y-4
      = daisy_text_input(name: "first_name_input", id: "first_name_input", floating: "First Name")

      = daisy_text_input(name: "last_name_input", id: "last_name_input", floating: "Last Name")

    = daisy_divider("OR")

    .space-y-4
      = daisy_text_input(name: "phone_floating_input", id: "phone_floating_input", floating: "Phone Number", placeholder: "ph: (123) 456-7890")

      = daisy_text_input(name: "fax_floating_input", id: "fax_floating_input", floating: "Fax", placeholder: "fax: (123) 456-7890")
```

URL: /examples/Daisy::DataInput::TextInputComponent#floating-label-inputs

-- Example: Different Input Types
Description:
Text inputs can have different types such as password, email, number, etc.

Code (HAML):
```haml
= doc_example(title: "Different Input Types") do |doc|
  - doc.with_description do
    Text inputs can have different types such as password, email, number, etc.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "email", title: "Email", css: "mt-4")

    = daisy_text_input(name: "email", id: "email", type: "email")

    = daisy_label(for: "password", title: "Password")

    = daisy_text_input(name: "password", id: "password", type: "password")

    = daisy_label(for: "number", title: "Number", css: "mt-4")

    = daisy_text_input(name: "number", id: "number", type: "number")

    = daisy_label(for: "date", title: "Date", css: "mt-4")

    = daisy_text_input(name: "date", id: "date", type: "date")

    .text-sm.text-right
      %span{ class: "text-base-content/50" } See also:

      = component_link("Cally Input", "Daisy::DataInput::CallyInputComponent")

    = daisy_label(for: "time", title: "Time", css: "mt-4")

    = daisy_text_input(name: "time", id: "time", type: "time")

    = daisy_label(for: "datetime-local", title: "Datetime Local", css: "mt-4")

    = daisy_text_input(name: "datetime-local", id: "datetime-local", type: "datetime-local")
```

URL: /examples/Daisy::DataInput::TextInputComponent#different-input-types

-- Example: Text Input Sizes
Description:
Text inputs can have different sizes using the `input-lg`, `input-md`, `input-sm`, or `input-xs` classes.

Code (HAML):
```haml
= doc_example(title: "Text Input Sizes") do |doc|
  - doc.with_description do
    Text inputs can have different sizes using the `input-lg`, `input-md`, `input-sm`, or `input-xs` classes.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input-lg", title: "Large Input")

    = daisy_text_input(name: "input-lg", id: "input-lg", placeholder: "Large input", css: "input-lg")

    = daisy_label(for: "input-md", title: "Medium Input (default)", css: "mt-4")

    = daisy_text_input(name: "input-md", id: "input-md", placeholder: "Medium input", css: "input-md")

    = daisy_label(for: "input-sm", title: "Small Input", css: "mt-4")

    = daisy_text_input(name: "input-sm", id: "input-sm", placeholder: "Small input", css: "input-sm")

    = daisy_label(for: "input-xs", title: "Extra Small Input", css: "mt-4")

    = daisy_text_input(name: "input-xs", id: "input-xs", placeholder: "Extra small input", css: "input-xs")
```

URL: /examples/Daisy::DataInput::TextInputComponent#text-input-sizes

-- Example: Text Input with Different Colors
Description:
Text inputs can be styled with additional CSS classes to change their appearance.

Code (HAML):
```haml
= doc_example(title: "Text Input with Different Colors") do |doc|
  - doc.with_description do
    Text inputs can be styled with additional CSS classes to change their appearance.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input_primary", title: "Primary Text Input")

    = daisy_text_input(name: "input_primary", id: "input_primary", css: "input-primary")

    = daisy_label(for: "input_secondary", title: "Secondary Text Input", css: "mt-4")

    = daisy_text_input(name: "input_secondary", id: "input_secondary", css: "input-secondary")

    = daisy_label(for: "input_accent", title: "Accent Text Input", css: "mt-4")

    = daisy_text_input(name: "input_accent", id: "input_accent", css: "input-accent")
```

URL: /examples/Daisy::DataInput::TextInputComponent#text-input-with-different-colors

-- Example: Ghost Style Text Input
Description:
Text inputs can use the `input-ghost` class to remove the border. The border will appear on focus.

Code (HAML):
```haml
= doc_example(title: "Ghost Style Text Input") do |doc|
  - doc.with_description do
    Text inputs can use the `input-ghost` class to remove the border. The border
will appear on focus.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input_ghost", title: "Ghost Style Text Input")

    = daisy_text_input(name: "input_ghost", id: "input_ghost", css: "input-ghost")

    .mt-4.p-4.border.rounded-lg.bg-secondary.text-secondary-content
      %p.text-sm.mb-2 Ghost style with background container:

      = daisy_label(for: "input_ghost_bg", title: "Search")

      = daisy_text_input(name: "input_ghost_bg", id: "input_ghost_bg", css: "input-ghost", placeholder: "Search the docs...")
```

URL: /examples/Daisy::DataInput::TextInputComponent#ghost-style-text-input

-- Example: Disabled Text Input
Description:
Text inputs can be disabled using the `disabled` attribute.

Code (HAML):
```haml
= doc_example(title: "Disabled Text Input") do |doc|
  - doc.with_description do
    Text inputs can be disabled using the `disabled` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input_disabled", title: "Disabled Text Input", css: "cursor-not-allowed")

    = daisy_text_input(name: "input_disabled", id: "input_disabled", disabled: true)
```

URL: /examples/Daisy::DataInput::TextInputComponent#disabled-text-input

-- Example: Required Text Input
Description:
Text inputs can be marked as required using the `required` attribute.

Code (HAML):
```haml
= doc_example(title: "Required Text Input") do |doc|
  - doc.with_description do
    Text inputs can be marked as required using the `required` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input_required", title: "Required Text Input")

    = daisy_text_input(name: "input_required", id: "input_required", required: true)
```

URL: /examples/Daisy::DataInput::TextInputComponent#required-text-input

-- Example: Text Input with Icons
Description:
Text inputs can be combined with icons and other elements using the `start` and `end` options or slots, which appear before and after the input field.

Code (HAML):
```haml
= doc_example(title: "Text Input with Icons") do |doc|
  - doc.with_description do
    Text inputs can be combined with icons and other elements using the `start` and `end`
options or slots, which appear before and after the input field.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "input_icon_prefix", title: "Search with Icon Prefix")

    = daisy_text_input(name: "input_icon_prefix", id: "input_icon_prefix", placeholder: "Search...") do |text_input|
      - text_input.with_start(css: "label") do
        = hero_icon("magnifying-glass", size: 5, css: "text-gray-400")

    = daisy_label(for: "input_icon_suffix", title: "Email with Icon Suffix", css: "mt-4")

    = daisy_text_input(name: "input_icon_suffix", id: "input_icon_suffix", type: "email", placeholder: "Enter your email") do |text_input|
      - text_input.with_end do
        = hero_icon("envelope", size: 5, css: "text-gray-400")

    = daisy_label(for: "input_icon_both", title: "Input with Both Icons", css: "mt-4")

    = daisy_text_input(name: "input_icon_both", id: "input_icon_both", placeholder: "Username") do |text_input|
      - text_input.with_start do
        = hero_icon("user", size: 5, css: "text-gray-400")

      - text_input.with_end do
        = hero_icon("check", size: 5, css: "text-success")
```

URL: /examples/Daisy::DataInput::TextInputComponent#text-input-with-icons

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an easy way to use text inputs in your forms by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an easy way to use
text inputs in your forms by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user) do |form|
    .my-2.flex.flex-col.gap-4
      = form.daisy_label(:username)

      = form.daisy_text_input(:username)

      = form.daisy_label(:email, "Email Address", css: "mt-4")

      = form.daisy_text_input(:email, type: "email", placeholder: "example@mail.com")

      = form.daisy_label(:password, css: "mt-4")

      = form.daisy_text_input(:password, type: "password")
```

URL: /examples/Daisy::DataInput::TextInputComponent#rails-form-example

---

=== Component: Daisy::DataInput::TextAreaComponent
Group: DataInput
Title: Text Areas
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::TextAreaComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/text_areas.html.haml

Description:
doc_note do Text areas have a border by default. Use `textarea-ghost` to remove the border. 

Helpers:
- daisy_text_area

Examples:

-- Example: Basic Text Area
Description:
Text areas allow users to enter multiple lines of text.

Code (HAML):
```haml
= doc_example(title: "Basic Text Area") do |doc|
  - doc.with_description do
    Text areas allow users to enter multiple lines of text.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "message1", title: "Message")

    = daisy_text_area(name: "message1", id: "message1")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#basic-text-area

-- Example: Text Area with Placeholder
Description:
You can add placeholder text to guide users on what to enter.

Code (HAML):
```haml
= doc_example(title: "Text Area with Placeholder") do |doc|
  - doc.with_description do
    You can add placeholder text to guide users on what to enter.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "feedback", title: "Feedback")

    = daisy_text_area(name: "feedback", id: "feedback", placeholder: "Tell us what you think...")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#text-area-with-placeholder

-- Example: Text Area with Initial Value
Description:
You can pre-populate a text area with an initial value.

Code (HAML):
```haml
= doc_example(title: "Text Area with Initial Value") do |doc|
  - doc.with_description do
    You can pre-populate a text area with an initial value.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "bio", title: "Biography")

    = daisy_text_area(name: "bio", id: "bio", value: "I am a software developer with expertise in Ruby on Rails and JavaScript.")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#text-area-with-initial-value

-- Example: Custom Rows
Description:
You can customize the height of the text area by specifying the number of rows.

Code (HAML):
```haml
= doc_example(title: "Custom Rows") do |doc|
  - doc.with_description do
    You can customize the height of the text area by specifying the number of rows.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "short_note", title: "Short Note (2 rows)")

    = daisy_text_area(name: "short_note", id: "short_note", rows: 2)

    = daisy_label(for: "long_note", title: "Long Note (8 rows)", css: "mt-4")

    = daisy_text_area(name: "long_note", id: "long_note", rows: 8)
```

URL: /examples/Daisy::DataInput::TextAreaComponent#custom-rows

-- Example: Text Area with Different Colors
Description:
Text areas can be styled with additional CSS classes to change their appearance.

Code (HAML):
```haml
= doc_example(title: "Text Area with Different Colors") do |doc|
  - doc.with_description do
    Text areas can be styled with additional CSS classes to change their appearance.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "textarea_primary", title: "Primary Text Area")

    = daisy_text_area(name: "textarea_primary", id: "textarea_primary", css: "textarea-primary")

    = daisy_label(for: "textarea_secondary", title: "Secondary Text Area", css: "mt-4")

    = daisy_text_area(name: "textarea_secondary", id: "textarea_secondary", css: "textarea-secondary")

    = daisy_label(for: "textarea_accent", title: "Accent Text Area", css: "mt-4")

    = daisy_text_area(name: "textarea_accent", id: "textarea_accent", css: "textarea-accent")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#text-area-with-different-colors

-- Example: Borderless (Ghost) Text Area
Description:
Use `textarea-ghost` to remove the border from a text area. This creates a minimalist appearance that only shows borders on hover.

Code (HAML):
```haml
= doc_example(title: "Borderless (Ghost) Text Area") do |doc|
  - doc.with_description do
    Use `textarea-ghost` to remove the border from a text area. This creates a
minimalist appearance that only shows borders on hover.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "textarea_ghost", title: "Ghost Style Text Area")

    = daisy_text_area(name: "textarea_ghost", id: "textarea_ghost", css: "textarea-ghost")

    .mt-4.p-4.border.rounded-lg.bg-teal-200
      %p.text-sm.mb-2 Ghost style with background container:

      = daisy_label(for: "textarea_ghost_bg", title: "Your Message")

      = daisy_text_area(name: "textarea_ghost_bg", id: "textarea_ghost_bg", css: "textarea-ghost")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#borderless-ghost-text-area

-- Example: Disabled Text Area
Description:
Text areas can be disabled using the `disabled` attribute.

Code (HAML):
```haml
= doc_example(title: "Disabled Text Area") do |doc|
  - doc.with_description do
    Text areas can be disabled using the `disabled` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "textarea_disabled", title: "Disabled Text Area", css: "cursor-not-allowed")

    = daisy_text_area(name: "textarea_disabled", id: "textarea_disabled", disabled: true, value: "This text area is disabled and cannot be edited.")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#disabled-text-area

-- Example: Read-only Text Area
Description:
Text areas can be set to read-only using the `readonly` attribute, allowing users to see but not edit the content.

Code (HAML):
```haml
= doc_example(title: "Read-only Text Area") do |doc|
  - doc.with_description do
    Text areas can be set to read-only using the `readonly` attribute, allowing users to see but not edit the content.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "textarea_readonly", title: "Read-only Text Area")

    = daisy_text_area(name: "textarea_readonly", id: "textarea_readonly", readonly: true, value: "This text area is read-only. You can select the text but cannot edit it.")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#read-only-text-area

-- Example: Required Text Area
Description:
Text areas can be marked as required using the `required` attribute.

Code (HAML):
```haml
= doc_example(title: "Required Text Area") do |doc|
  - doc.with_description do
    Text areas can be marked as required using the `required` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "textarea_required", title: "Required Text Area")

    = daisy_text_area(name: "textarea_required", id: "textarea_required", required: true)
```

URL: /examples/Daisy::DataInput::TextAreaComponent#required-text-area

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an easy way to use text areas in your forms by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an easy way to use
text areas in your forms by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user) do |form|
    .my-2.flex.flex-col.gap-4
      = form.daisy_label(:bio)

      = form.daisy_text_area(:bio)

      = form.daisy_label(:comments, "Additional Comments", css: "mt-4")

      = form.daisy_text_area(:comments, rows: 6, placeholder: "Share your thoughts...")
```

URL: /examples/Daisy::DataInput::TextAreaComponent#rails-form-example

---

=== Component: Daisy::DataInput::ToggleComponent
Group: DataInput
Title: Toggles
API URL: /api-docs
Examples URL: /examples/Daisy::DataInput::ToggleComponent
File: /home/loco_demo/app/views/examples/daisy/data_input/toggles.html.haml

Description:
 The Toggle component is a wrapper around the  component_link("Checkbox", "Daisy::DataInput::CheckboxComponent")  component that sets the <code>toggle</code> property to <code>true</code> by default, providing a switch-like appearance. 

Helpers:
- daisy_toggle

Examples:

-- Example: Basic Toggles
Description:
Toggles are switches that allow users to turn options on or off. They're a visual alternative to checkboxes, ideal for settings and preferences. doc_note(css: "mb-8") do While the <a href="#{doc_url("LocoMotion/Concerns/LabelableComponent")}" target="_blank">LabelableComponent</a> Concern does implement a `floating` attribute, it is not relevant for toggles and does not render properly.

Code (HAML):
```haml
= doc_example(title: "Basic Toggles") do |doc|
  - doc.with_description do
    Toggles are switches that allow users to turn options on or off. They're a visual
alternative to checkboxes, ideal for settings and preferences.



    = doc_note(css: "mb-8") do
      While the
<a href="#{doc_url("LocoMotion/Concerns/LabelableComponent")}" target="_blank">LabelableComponent</a>
Concern does implement a `floating` attribute, it is not relevant
for toggles and does not render properly.



  .my-2.flex.flex-col.gap-4
    
      = daisy_toggle(name: "toggle0", id: "toggle0")

      .tooltip.tooltip-warning{ "data-tip": "CANNOT click the text to activate" }
        Toggle with no label

    = daisy_label(for: "toggle1") do
      = daisy_toggle(name: "toggle1", id: "toggle1")

      Default Toggle with regular label

    = daisy_toggle(name: "toggle_end", id: "toggle_end", checked: true, end: "Toggle using end wrapper")

    = daisy_toggle(name: "toggle_start", id: "toggle_start", checked: true, start: "Toggle using start wrapper")

    = daisy_toggle(name: "toggle_end_block", id: "toggle_end_block", checked: true) do |toggle|
      - toggle.with_end do
        Toggle using `end` block



    = daisy_toggle(name: "toggle_start_block", id: "toggle_start_block", checked: true) do |toggle|
      - toggle.with_start(css: "flex flex-row items-center") do
        Using

        = daisy_badge(css: "mx-1 badge-secondary badge-sm") do
          start

      - toggle.with_end(css: "flex flex-row items-center") do
        and

        = daisy_badge(css: "mx-1 badge-secondary badge-sm") do
          end

        blocks
```

URL: /examples/Daisy::DataInput::ToggleComponent#basic-toggles

-- Example: Toggles with Colors
Description:
Toggles can be styled with different colors using CSS classes like `toggle-primary`.

Code (HAML):
```haml
= doc_example(title: "Toggles with Colors") do |doc|
  - doc.with_description do
    Toggles can be styled with different colors using CSS classes like `toggle-primary`.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "toggle-dark") do
      = daisy_toggle(name: "toggle-dark", id: "toggle-dark", css: "toggle-neutral theme-controller", value: "dark", checked: false)

      Enable Dark Mode

    = daisy_label(for: "toggle_primary") do
      = daisy_toggle(name: "toggle_primary", css: "toggle-primary", id: "toggle_primary", checked: true)

      Primary Toggle

    = daisy_label(for: "toggle_secondary") do
      = daisy_toggle(name: "toggle_secondary", css: "toggle-secondary", id: "toggle_secondary", checked: true)

      Secondary Toggle

    = daisy_label(for: "toggle_accent") do
      = daisy_toggle(name: "toggle_accent", css: "toggle-accent", id: "toggle_accent", checked: true)

      Accent Toggle

    = daisy_label(for: "toggle_success") do
      = daisy_toggle(name: "toggle_success", css: "toggle-success", id: "toggle_success", checked: true)

      Success Toggle

    = daisy_label(for: "toggle_warning") do
      = daisy_toggle(name: "toggle_warning", css: "toggle-warning", id: "toggle_warning", checked: true)

      Warning Toggle

    = daisy_label(for: "toggle_error") do
      = daisy_toggle(name: "toggle_error", css: "toggle-error", id: "toggle_error", checked: true)

      Error Toggle
```

URL: /examples/Daisy::DataInput::ToggleComponent#toggles-with-colors

-- Example: Toggle Sizes
Description:
Toggles come in different sizes by applying CSS classes.

Code (HAML):
```haml
= doc_example(title: "Toggle Sizes") do |doc|
  - doc.with_description do
    Toggles come in different sizes by applying CSS classes.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "toggle_small") do
      = daisy_toggle(name: "toggle_small", id: "toggle_small", css: "toggle-sm")

      Small Toggle

    = daisy_label(for: "toggle_default") do
      = daisy_toggle(name: "toggle_default", id: "toggle_default")

      Default Size Toggle

    = daisy_label(for: "toggle_large") do
      = daisy_toggle(name: "toggle_large", id: "toggle_large", css: "toggle-lg")

      Large Toggle
```

URL: /examples/Daisy::DataInput::ToggleComponent#toggle-sizes

-- Example: Toggle States
Description:
Toggles can be in different states such as checked, disabled, or required.

Code (HAML):
```haml
= doc_example(title: "Toggle States") do |doc|
  - doc.with_description do
    Toggles can be in different states such as checked, disabled, or required.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "toggle_unchecked") do
      = daisy_toggle(name: "toggle_unchecked", id: "toggle_unchecked")

      Unchecked Toggle

    = daisy_label(for: "toggle_checked") do
      = daisy_toggle(name: "toggle_checked", id: "toggle_checked", checked: true)

      Checked Toggle

    = daisy_label(for: "toggle_disabled", css: "cursor-not-allowed") do
      = daisy_toggle(name: "toggle_disabled", id: "toggle_disabled", disabled: true)

      Disabled Toggle

    = daisy_label(for: "toggle_required") do
      = daisy_toggle(name: "toggle_required", id: "toggle_required", required: true)

      Required Toggle
```

URL: /examples/Daisy::DataInput::ToggleComponent#toggle-states

-- Example: Custom ID
Description:
Toggles can be given a custom ID using the `id` attribute.

Code (HAML):
```haml
= doc_example(title: "Custom ID") do |doc|
  - doc.with_description do
    Toggles can be given a custom ID using the `id` attribute.



  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "custom-toggle-id") do
      = daisy_toggle(name: "toggle_custom", id: "custom-toggle-id")

      Toggle with Custom ID
```

URL: /examples/Daisy::DataInput::ToggleComponent#custom-id

-- Example: Rails Form Example
Description:
The built-in Form Builder extension provides an even easier way to use toggles in your pages by extracting the name and ID attributes from the form object and attributes.

Code (HAML):
```haml
= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    The built-in Form Builder extension provides an even easier way to use
toggles in your pages by extracting the name and ID attributes from the
form object and attributes.



  = form_with(url: "#", method: :post, scope: :user) do |form|
    .flex.flex-col.gap-4
      = form.daisy_label(:notifications) do
        = form.daisy_toggle(:notifications)

        Enable notifications

      = form.daisy_label(:dark_mode) do
        = form.daisy_toggle(:dark_mode, css: "toggle-primary theme-controller", value: "dark")

        Enable dark mode
```

URL: /examples/Daisy::DataInput::ToggleComponent#rails-form-example

-- Example: Using Label Component
Description:
You can also use the Label component directly with toggles for a fully custom approach.

Code (HAML):
```haml
= doc_example(title: "Using Label Component") do |doc|
  - doc.with_description do
    You can also use the Label component directly with toggles for a fully
custom approach.



  .my-2.flex.flex-col.gap-4.w-80
    .flex.items-center.justify-between
      = daisy_label(for: "location", title: "Enable location services")

      = daisy_toggle(name: "location", id: "location")

    .flex.items-center.justify-between
      = daisy_label(for: "marketing") do
        Receive marketing emails

      = daisy_toggle(name: "marketing", id: "marketing", css: "toggle-primary")
```

URL: /examples/Daisy::DataInput::ToggleComponent#using-label-component

---

=== Component: Daisy::Navigation::BreadcrumbsComponent
Group: Navigation
Title: Breadcrumbs
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::BreadcrumbsComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/breadcrumbs.html.haml

Description:
Breadcrumbs are used to show the user's location within a website or app. 

Helpers:
- daisy_breadcrumbs

Examples:

-- Example: Basic Breadcrumbs
Description:
Basic breadcrumbs show only text and a separator.

Code (HAML):
```haml
= doc_example(title: "Basic Breadcrumbs") do |doc|
  - doc.with_description do
    Basic breadcrumbs show only text and a separator.



  = daisy_breadcrumbs do |breadcrumbs|
    - # Options via keyword arguments

    - breadcrumbs.with_item(title: "Docs", href: "/")

    - # Options via positional arguments

    - breadcrumbs.with_item("Breadcrumbs", "/examples/Daisy::Navigation::BreadcrumbsComponent")

    - # Custom content

    - breadcrumbs.with_item(css: "italic selection:bg-secondary selection:text-secondary-content") do
      This example
```

URL: /examples/Daisy::Navigation::BreadcrumbsComponent#basic-breadcrumbs

-- Example: Breadcrumbs with Icons
Description:
Breadcrumbs can also utilize icons to help users quickly identify different pages.

Code (HAML):
```haml
= doc_example(title: "Breadcrumbs with Icons") do |doc|
  - doc.with_description do
    Breadcrumbs can also utilize icons to help users quickly identify
different pages.



  - links = [ ["Home", "home"], ["Docs", "document"], ["Components", "cube"], ["Breadcrumbs", "squares-2x2"] ]

  = daisy_breadcrumbs do |breadcrumbs|
    - links.each do |link|
      - breadcrumbs.with_item(title: link[0], href: "#", icon: link[1], icon_css: "size-4 mr-1 text-base-content/50", icon_options: { variant: :mini })
```

URL: /examples/Daisy::Navigation::BreadcrumbsComponent#breadcrumbs-with-icons

-- Example: Custom Breadcrumbs
Description:
You can also pass any content to fully customize your breadcrumbs.

Code (HAML):
```haml
= doc_example(title: "Custom Breadcrumbs") do |doc|
  - doc.with_description do
    You can also pass any content to fully customize your breadcrumbs.



  - links = [ [ "🙂", "Favorites", ""], ["😃", "Super Favorites", "italic"], ["🤩", "Amazeballs Favorites", "font-bold italic"] ]

  = daisy_breadcrumbs do |breadcrumbs|
    - links.each.with_index do |link, index|
      - breadcrumbs.with_item do
        = daisy_link "#", css: "no-underline! hover:text-secondary" do
          .text-3xl.-rotate-12
            = link.first

          .text-xl{ class: link.last }
            = link.second
```

URL: /examples/Daisy::Navigation::BreadcrumbsComponent#custom-breadcrumbs

---

=== Component: Daisy::Navigation::DockComponent
Group: Navigation
Title: Dock
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::DockComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/docks.html.haml

Description:
The Dock component provides a navigation bar fixed to the bottom of the screen, commonly used in mobile interfaces. 

Helpers:
- daisy_dock

Examples:

-- Example: Basic Dock
Description:
A simple dock component with three sections. doc_note(modifier: :tip, css: "mb-8") do We add the `relative` class to ensure visibility in the example rather than the default of being fixed to the bottom of the screen.

Code (HAML):
```haml
= doc_example(title: "Basic Dock", example_css: "mx-auto max-w-[400px]") do |doc|
  - doc.with_description do
    A simple dock component with three sections.



    = doc_note(modifier: :tip, css: "mb-8") do
      We add the `relative` class to ensure visibility in the example rather
than the default of being fixed to the bottom of the screen.



  = daisy_dock(css: "relative border border-base-300") do |dock|
    - dock.with_section(icon: "home", href: "#")

    - dock.with_section(icon: "information-circle", href: "#", active: true)

    - dock.with_section(icon: "chart-bar", href: "#")
```

URL: /examples/Daisy::Navigation::DockComponent#basic-dock

-- Example: Dock with Titles
Description:
The Dock sections can also have a simple title passed via the `title` attribute, or a more complex title by passing a block to the section.

Code (HAML):
```haml
= doc_example(title: "Dock with Titles", example_css: "mx-auto max-w-[400px]") do |doc|
  - doc.with_description do
    The Dock sections can also have a simple title passed via the `title`
attribute, or a more complex title by passing a block to the section.



  = daisy_dock(css: "relative border border-base-content") do |dock|
    - dock.with_section(icon: "home", href: "#", title: "Home")

    - dock.with_section(icon: "information-circle", icon_css: "text-blue-600 -rotate-45", href: "#", active: true, title: "Info")

    - dock.with_section(icon: "chart-bar", href: "#") do
      .font-bold.italic.text-xs.text-error.animate-bounce
        Stats
```

URL: /examples/Daisy::Navigation::DockComponent#dock-with-titles

-- Example: Colored Dock
Description:
You can also use any of the built-in Daisy, Tailwind, or custom colors.

Code (HAML):
```haml
= doc_example(title: "Colored Dock", example_css: "flex-col space-y-8 mx-auto max-w-[400px]") do |doc|
  - doc.with_description do
    You can also use any of the built-in Daisy, Tailwind, or custom colors.



  - colors = ["text-primary", "text-secondary", "text-accent", "text-info", "text-success", "text-warning", "text-error", "text-cyan-400", "text-[#449944]"]

  - colors.each do |color|
    = daisy_dock(css: "relative border border-base-300") do |dock|
      - dock.with_section(icon: "home", href: "#", title: "Home", css: color)

      - dock.with_section(icon: "information-circle", title: color, href: "#", css: color)

      - dock.with_section(icon: "chart-bar", href: "#", title: "Stats", css: color)
```

URL: /examples/Daisy::Navigation::DockComponent#colored-dock

---

=== Component: Daisy::Navigation::LinkComponent
Group: Navigation
Title: Links
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::LinkComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/links.html.haml

Description:
 Tailwind removes the styling on standard links so we provide an easy way to  re-add them. This works very similarly to the  link_to "link_to", "https://api.rubyonrails.org/classes/ActionView/Helpers/UrlHelper.html#method-i-link_to", class: "font-mono"  helper in Rails. 

Helpers:
- daisy_link

Examples:

-- Example: Basic Links
Description:
Basic links are styled with the `.link` class. The `title` and `href` options can be passed as keyword arguments, or as the first two positional arguments. The `title` can also be passed as a block.

Code (HAML):
```haml
= doc_example(title: "Basic Links") do |doc|
  - doc.with_description do
    Basic links are styled with the `.link` class. The `title` and `href`
options can be passed as keyword arguments, or as the first two positional
arguments.

The `title` can also be passed as a block.



  .flex.items-end.space-x-4
    = daisy_link title: "Home", href: "#"

    = daisy_link "Docs", "#", css: "link-primary text-xl"

    = daisy_link "Components", "#", css: "link-secondary text-2xl"

    = daisy_link "#", css: "link-success link-hover text-3xl" do
      Big Hover Link
```

URL: /examples/Daisy::Navigation::LinkComponent#basic-links

---

=== Component: Daisy::Navigation::MenuComponent
Group: Navigation
Title: Menus
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::MenuComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/menus.html.haml

Description:
doc_note do Vertical menus no longer have a default width. Use `w-full` or other width classes if you want to control the width. 

Helpers:
- daisy_menu

Examples:

-- Example: Basic Menu
Description:
Menus typically contain a list of links or buttons that users can click. The styling is very minimal, just adding a hover effect.

Code (HAML):
```haml
= doc_example(title: "Basic Menu") do |doc|
  - doc.with_description do
    %p
      Menus typically contain a list of links or buttons that users can click.

      The styling is very minimal, just adding a hover effect.

  = daisy_menu(css: "w-56") do |menu|
    - # Menu Item Group with a title keyword argument

    - menu.with_item(title: "Group 1") do
      = link_to "Item 1 - 1", "#"

    - # Menu Item Group with a title as the first positional argument

    - menu.with_item("Group 2") do
      = link_to "Item 2 - 1", "#"

    - menu.with_item do
      = link_to "Item 2 - 2", "#"
```

URL: /examples/Daisy::Navigation::MenuComponent#basic-menu

-- Example: Horizontal Menu
Description:
Menus can be displayed horizontally by adding the `.menu-horizontal` class to the menu. They can also have icons, disabled / active items, and many other options. Use `menu-active`, `menu-focus`, and `menu-disabled` classes to style menu items in different states. doc_note(css: "mb-4") do We can't actually disable the link since the HTML spec does not allow that (instead you should generally just make it text). However, we can disable the link with CSS and use `.pointer-events-none` to ensure it cannot be clicked. You may also want to add `tabindex: -1` to your link to ensure it cannot be focused with the keyboard.

Code (HAML):
```haml
= doc_example(title: "Horizontal Menu") do |doc|
  - doc.with_description do
    Menus can be displayed horizontally by adding the `.menu-horizontal` class
to the menu. They can also have icons, disabled / active items, and many
other options.

Use `menu-active`, `menu-focus`, and `menu-disabled` classes to style menu
items in different states.



    = doc_note(css: "mb-4") do
      We can't actually disable the link since the HTML spec does not allow
that (instead you should generally just make it text). However, we can
disable the link with CSS and use `.pointer-events-none` to ensure it
cannot be clicked. You may also want to add `tabindex: -1` to your link
to ensure it cannot be focused with the keyboard.



  = daisy_menu(css: "menu-horizontal") do |menu|
    - menu.with_item do
      = link_to "Item 1", "#"

    - menu.with_item(disabled: true) do
      = link_to "Item 2", "#", tabindex: -1

    - menu.with_item do
      = link_to "Item 3", "#", class: "menu-active"
```

URL: /examples/Daisy::Navigation::MenuComponent#horizontal-menu

-- Example: Nested Menus
Description:
Menus can also contain nested menus. This is useful for organizing a large number of options.

Code (HAML):
```haml
= doc_example(title: "Nested Menus") do |doc|
  - doc.with_description do
    %p
      Menus can also contain nested menus. This is useful for organizing a large

      number of options.

  - icon_options = { css: "size-4 mr-1 text-slate-600", variant: :mini }

  = daisy_menu(css: "w-56 bg-base-100 border border-base-300 rounded-lg shadow-md") do |menu|
    - menu.with_item do
      = link_to "#" do
        = hero_icon("home", **icon_options)

        Home

    - menu.with_item do
      = link_to "#" do
        = hero_icon("bell-alert", **icon_options)

        Notifications

    - menu.with_item(title: "Parent") do
      = daisy_menu(css: "w-full") do |menu|
        - menu.with_item do
          = link_to "Child 1", "#"

        - menu.with_item do
          = link_to "Child 2", "#"

        - menu.with_item do
          - icon_options[:css] += " text-yellow-500!"

          = link_to "#" do
            Child 3

            = hero_icon("bolt", **icon_options)
```

URL: /examples/Daisy::Navigation::MenuComponent#nested-menus

---

=== Component: Daisy::Navigation::NavbarComponent
Group: Navigation
Title: Navbars
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::NavbarComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/navbars.html.haml

Description:
 Navbars are a common way to organize navigation links at the top of a page. 

Helpers:
- daisy_navbar

Examples:

-- Example: Basic Navbar
Description:
Navbars are simple elements which typically contain start and end sections, and sometimes a center section.

Code (HAML):
```haml
= doc_example(title: "Basic Navbar") do |doc|
  - doc.with_description do
    Navbars are simple elements which typically contain start and end
sections, and sometimes a center section.



  .w-full{ class: "max-w-[800px]" }
    = daisy_navbar(css: "bg-base-100 border border-base-200 rounded-lg shadow-xs") do |navbar|
      - navbar.with_start do
        = image_tag("loco-logo.png", class: "h-8")

        %span
          %em Loco

          %span.font-bold Motion

      - navbar.with_end do
        = link_to "https://github.com/profoundry-us/loco_motion", target: "_blank", class: "flex items-center space-x-2" do
          %svg.size-6{role: "img", viewbox: "0 0 24 24", xmlns: "http://www.w3.org/2000/svg", class: "dark:fill-base-100"}
            %title GitHub

            %path{d: "M12 .297c-6.63 0-12 5.373-12 12 0 5.303 3.438 9.8 8.205 11.385.6.113.82-.258.82-.577 0-.285-.01-1.04-.015-2.04-3.338.724-4.042-1.61-4.042-1.61C4.422 18.07 3.633 17.7 3.633 17.7c-1.087-.744.084-.729.084-.729 1.205.084 1.838 1.236 1.838 1.236 1.07 1.835 2.809 1.305 3.495.998.108-.776.417-1.305.76-1.605-2.665-.3-5.466-1.332-5.466-5.93 0-1.31.465-2.38 1.235-3.22-.135-.303-.54-1.523.105-3.176 0 0 1.005-.322 3.3 1.23.96-.267 1.98-.399 3-.405 1.02.006 2.04.138 3 .405 2.28-1.552 3.285-1.23 3.285-1.23.645 1.653.24 2.873.12 3.176.765.84 1.23 1.91 1.23 3.22 0 4.61-2.805 5.625-5.475 5.92.42.36.81 1.096.81 2.22 0 1.606-.015 2.896-.015 3.286 0 .315.21.69.825.57C20.565 22.092 24 17.592 24 12.297c0-6.627-5.373-12-12-12"}

          %span

          GitHub
```

URL: /examples/Daisy::Navigation::NavbarComponent#basic-navbar

-- Example: Advanced Navbar
Description:
Navbars items can also contain center sections and dropdowns with menus. doc_note(css: "mb-8") do Note that the size of the elements inside the navbar will affect the height of the navbar itself. The navbar will grow to fit the largest element inside it. Try hovering the green icon below to see the change.

Code (HAML):
```haml
= doc_example(title: "Advanced Navbar", example_css: "pb-24") do |doc|
  - doc.with_description do
    Navbars items can also contain center sections and dropdowns with menus.



    = doc_note(css: "mb-8") do
      Note that the size of the elements inside the navbar will affect the
height of the navbar itself. The navbar will grow to fit the largest
element inside it.

Try hovering the green icon below to see the change.



  .w-full{ class: "max-w-[800px]" }
    = daisy_navbar(css: "bg-base-100 border border-base-200 rounded-lg shadow-xs") do |navbar|
      - navbar.with_start(css: "text-lg italic") do
        Code-<strong>Fur</strong>-Life

      - navbar.with_center do
        = hero_icon("code-bracket-square", css: "transition-all size-14 hover:size-20 text-green-600 -rotate-12")

      - navbar.with_end do
        = daisy_dropdown(css: "dropdown-end") do |dropdown|
          - dropdown.with_button(css: "btn-ghost", title: "Languages", right_icon: "chevron-down")

          - dropdown.with_item do
            = link_to "Ruby", "https://www.ruby-lang.org", target: "_blank"

          - dropdown.with_item do
            = link_to "Rails", "http://www.rubyonrails.org", target: "_blank"

          - dropdown.with_item do
            = link_to "Hotwire", "https://hotwired.dev/", target: "_blank"
```

URL: /examples/Daisy::Navigation::NavbarComponent#advanced-navbar

---

=== Component: Daisy::Navigation::PaginationComponent
Group: Navigation
Title: Pagination
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::PaginationComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/pagination.html.haml

Description:
 Pagination can be implemented by utilizing the existing  component_link("Join", "Daisy::Layout::JoinComponent")  component. 

Helpers:
- (No helper methods defined)

Examples:

-- Example: Basic Pagination
Description:
Pagination is used to navigate between pages of content. It is typically used in conjunction with a list of items that are paginated.

Code (HAML):
```haml
= doc_example(title: "Basic Pagination") do |doc|
  - doc.with_description do
    %p
      Pagination is used to navigate between pages of content. It is typically

      used in conjunction with a list of items that are paginated.

  = daisy_join do |join|
    - join.with_item do
      = daisy_button(title: "1", css: "btn-primary join-item")

    - join.with_item do
      = daisy_button(title: "2", css: "join-item")

    - join.with_item do
      = daisy_button(title: "...", css: "join-item")

    - join.with_item do
      = daisy_button(title: "9", css: "join-item")

    - join.with_item do
      = daisy_button(title: "10", css: "join-item")
```

URL: /examples/Daisy::Navigation::PaginationComponent#basic-pagination

---

=== Component: Daisy::Navigation::StepsComponent
Group: Navigation
Title: Steps
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::StepsComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/steps.html.haml

Description:
 Steps allow you to create a series of steps that users can follow to  complete a task. This is useful for onboarding, form completion, and many  more. 

Helpers:
- daisy_steps

Examples:

-- Example: Basic Steps
Description:
Steps are automatically numbered and connected with lines. You can utilize colors to indicate progress through the steps.

Code (HAML):
```haml
= doc_example(title: "Basic Steps") do |doc|
  - doc.with_description do
    %p
      Steps are automatically numbered and connected with lines. You can utilize

      colors to indicate progress through the steps.

  = daisy_steps do |steps|
    - steps.with_step(title: "Write Code", css: "step-primary")

    - steps.with_step(title: "Release Code", css: "step-primary")

    - steps.with_step(title: "Profit", css: "step-secondary")

    - steps.with_step(title: "Rule the World")
```

URL: /examples/Daisy::Navigation::StepsComponent#basic-steps

-- Example: Vertical Steps
Description:
You can also display steps vertically.

Code (HAML):
```haml
= doc_example(title: "Vertical Steps") do |doc|
  - doc.with_description do
    %p
      You can also display steps vertically.

  = daisy_steps(css: "steps-vertical") do |steps|
    - steps.with_step(title: "Write Code", css: "step-primary")

    - steps.with_step(title: "Release Code", css: "step-primary")

    - steps.with_step(title: "Profit", css: "step-secondary")

    - steps.with_step(title: "Rule the World")
```

URL: /examples/Daisy::Navigation::StepsComponent#vertical-steps

-- Example: Custom Content
Description:
You can customize the content of each step to include any (short) content you want by using the `number` option. doc_note do You can also supply this via the `data-content` attribute in the html. doc_code(language: "haml") do = daisy_steps do |steps| - steps.with_step(html: { data: { content: "❤️" } }) And you can alter the colors by supplying CSS for the `after` pseudo-element. Note that you may need to use the `!important` modifier to override the default styles.

Code (HAML):
```haml
= doc_example(title: "Custom Content") do |doc|
  - doc.with_description do
    You can customize the content of each step to include any (short) content
you want by using the `number` option.



    = doc_note do
      You can also supply this via the `data-content` attribute in the html.



      = doc_code(language: "haml") do
        = daisy_steps do |steps|

          - steps.with_step(html: { data: { content: "❤️" } })

    And you can alter the colors by supplying CSS for the `after`
pseudo-element. Note that you may need to use the `!important` modifier to
override the default styles.



  = daisy_steps do |steps|
    - steps.with_step(number: "AB")

    - steps.with_step(number: "CD")

    - steps.with_step(number: "EF")

    - steps.with_step(number: "★")

    - steps.with_step(number: "✓")

    - steps.with_step(number: "✕", css: "after:bg-black! after:text-green-500!")
```

URL: /examples/Daisy::Navigation::StepsComponent#custom-content

---

=== Component: Daisy::Navigation::TabsComponent
Group: Navigation
Title: Tabs
API URL: /api-docs
Examples URL: /examples/Daisy::Navigation::TabsComponent
File: /home/loco_demo/app/views/examples/daisy/navigation/tabs.html.haml

Description:
Here are some examples showcasing tabs. 

Helpers:
- daisy_tabs

Examples:

-- Example: Basic Tabs
Description:
Tabs are transparent by default and don't automatically do anything since they are simple `<a>` (anchor) elements. You can set the `href` attribute to make them link to another page.

Code (HAML):
```haml
= doc_example(title: "Basic Tabs") do |doc|
  - doc.with_description do
    Tabs are transparent by default and don't automatically do anything since
they are simple `<a>` (anchor) elements. You can set the `href` attribute
to make them link to another page.



  = daisy_tabs(css: "tabs-border") do |tabs|
    - tabs.with_tab(title: "I Do Nothing", active: true)

    - tabs.with_tab(title: "Ditto", html: { onclick: "alert('Ha! Gotcha ;-)')" })

    - tabs.with_tab(title: "Open Google", href: "https://google.com", target: "_blank")
```

URL: /examples/Daisy::Navigation::TabsComponent#basic-tabs

-- Example: Tab Sizes
Description:
Tabs come in five different sizes: `tabs-xs`, `tabs-sm`, `tabs-md` (default), `tabs-lg`, and `tabs-xl`. Each size can be combined with any tab style.

Code (HAML):
```haml
= doc_example(title: "Tab Sizes") do |doc|
  - doc.with_description do
    Tabs come in five different sizes: `tabs-xs`, `tabs-sm`, `tabs-md` (default),
`tabs-lg`, and `tabs-xl`. Each size can be combined with any tab style.



  .space-y-8
    .space-y-2
      %p.text-sm Extra Small

      = daisy_tabs(css: "tabs-border tabs-xs") do |tabs|
        - tabs.with_tab(title: "Tab 1", active: true)

        - tabs.with_tab(title: "Tab 2")

        - tabs.with_tab(title: "Tab 3")

    .space-y-2
      %p.text-sm Small

      = daisy_tabs(css: "tabs-border tabs-sm") do |tabs|
        - tabs.with_tab(title: "Tab 1", active: true)

        - tabs.with_tab(title: "Tab 2")

        - tabs.with_tab(title: "Tab 3")

    .space-y-2
      %p.text-sm Medium (Default)

      = daisy_tabs(css: "tabs-border") do |tabs|
        - tabs.with_tab(title: "Tab 1", active: true)

        - tabs.with_tab(title: "Tab 2")

        - tabs.with_tab(title: "Tab 3")

    .space-y-2
      %p.text-sm Large

      = daisy_tabs(css: "tabs-border tabs-lg") do |tabs|
        - tabs.with_tab(title: "Tab 1", active: true)

        - tabs.with_tab(title: "Tab 2")

        - tabs.with_tab(title: "Tab 3")

    .space-y-2
      %p.text-sm Extra Large

      = daisy_tabs(css: "tabs-border tabs-xl") do |tabs|
        - tabs.with_tab(title: "Tab 1", active: true)

        - tabs.with_tab(title: "Tab 2")

        - tabs.with_tab(title: "Tab 3")
```

URL: /examples/Daisy::Navigation::TabsComponent#tab-sizes

-- Example: Radio Button Tabs with Content
Description:
If you want the tabs to change their content, you can use the `radio` option. Note that this does *not* allow the use of icons or other HTML elements in the title.

Code (HAML):
```haml
= doc_example(title: "Radio Button Tabs with Content") do |doc|
  - doc.with_description do
    If you want the tabs to change their content, you can use the `radio`
option. Note that this does *not* allow the use of icons or other HTML
elements in the title.



  = daisy_tabs(css: "tabs-lift w-[500px]", radio: true) do |tabs|
    - # Add some space before the first tab

    - tabs.with_tab(title: "", css: "w-14! cursor-auto!", disabled: true)

    - (1..3).each do |i|
      - tabs.with_tab(title: "Tab #{i}", content_wrapper_css: "bg-base-100 border-base-300 rounded-box p-6", checked: i == 1) do
        %p "Tab #{i} content"
```

URL: /examples/Daisy::Navigation::TabsComponent#radio-button-tabs-with-content

-- Example: Turbo Frame Tabs
Description:
You can also use tabs with Turbo Frames to load content dynamically.

Code (HAML):
```haml
= doc_example(title: "Turbo Frame Tabs") do |doc|
  - doc.with_description do
    You can also use tabs with Turbo Frames to load content dynamically.



  %turbo-frame{"id" => "tabs--turob-tabs-example"}
    = daisy_tabs(css: "tabs-lift w-[500px]") do |tabs|
      - (2..4).each do |i|
        - is_checked = params[:custom_tab] ? params[:custom_tab] == i.to_s : i == 3

        - tabs.with_tab(href: "?custom_tab=#{i}", checked: is_checked) do |tab|
          - tab.with_title(css: "test-tab") do
            .flex.gap-x-2.items-center
              %span.size-4
                = heroicon("bars-#{i}", class: "size-4")

              %span.whitespace-nowrap "#{i} Bars"

          - tab.with_custom_content(css: "tab-content bg-base-100 border-base-300 rounded-box p-6") do
            %p "#{i} bars content..."
```

URL: /examples/Daisy::Navigation::TabsComponent#turbo-frame-tabs

---

=== Component: Daisy::Feedback::AlertComponent
Group: Feedback
Title: Alerts
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::AlertComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/alerts.html.haml

Description:
 Alerts inform the user about important information or changes to the system. 

Helpers:
- daisy_alert

Examples:

-- Example: Basic Alerts
Description:
Alerts come in a variety of flavors and generally have an icon at the start to indicate the type of alert.

Code (HAML):
```haml
= doc_example(title: "Basic Alerts", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    %p
      Alerts come in a variety of flavors and generally have an icon at the

      start to indicate the type of alert.

  = daisy_alert(icon: "archive-box", icon_html: { variant: :outline }, css: "w-100") do |alert|
    Standard alerts are gray-t!

  = daisy_alert(icon: "information-circle", css: "alert-info w-100", icon_html: { variant: :outline }) do |alert|
    Here is some important information!

  = daisy_alert(icon: "check-circle", css: "alert-success w-100", icon_html: { variant: :outline }) do |alert|
    Success! You did it!

  = daisy_alert(icon: "exclamation-triangle", css: "alert-warning w-100", icon_html: { variant: :outline }) do |alert|
    Ooh, be careful here!

  = daisy_alert(icon: "exclamation-circle", css: "alert-error w-100", icon_html: { variant: :outline }) do |alert|
    Uh oh, something went wrong!
```

URL: /examples/Daisy::Feedback::AlertComponent#basic-alerts

-- Example: Alerts Without Icons
Description:
If you don't want an icon, you can simply omit it.

Code (HAML):
```haml
= doc_example(title: "Alerts Without Icons", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    %p
      If you don't want an icon, you can simply omit it.

  = daisy_alert(css: "alert-info w-100") do |alert|
    Here is some important information!

  = daisy_alert(css: "alert-success w-100") do |alert|
    Success! You did it!

  = daisy_alert(css: "alert-warning w-100") do |alert|
    Ooh, be careful here!

  = daisy_alert(css: "alert-error w-100") do |alert|
    Uh oh, something went wrong!
```

URL: /examples/Daisy::Feedback::AlertComponent#alerts-without-icons

-- Example: Custom Icon Alerts
Description:
You can also use a custom icon (or any HTML) if you prefer.

Code (HAML):
```haml
= doc_example(title: "Custom Icon Alerts", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    %p
      You can also use a custom icon (or any HTML) if you prefer.

  = daisy_alert do |alert|
    = hero_icon("star", css: "text-yellow-500")

    Custom icon alert!

  = daisy_alert do |alert|
    %span ❤️

    %span Custom emoji alert!

  = daisy_alert do |alert|
    = daisy_avatar(css: "size-10", src: image_path("avatars/lady-smiling-3.jpg"))

    %span Or even Daisy avatars / other elements!
```

URL: /examples/Daisy::Feedback::AlertComponent#custom-icon-alerts

-- Example: Soft Alerts
Description:
Alerts can also be displayed in soft style.

Code (HAML):
```haml
= doc_example(title: "Soft Alerts", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    Alerts can also be displayed in soft style.



  = daisy_alert(icon: "information-circle", css: "alert-info alert-soft") do
    Info alert.

  = daisy_alert(icon: "check-circle", css: "alert-success alert-soft") do
    Success alert.

  = daisy_alert(icon: "exclamation-triangle", css: "alert-warning alert-soft") do
    Warning alert.

  = daisy_alert(icon: "exclamation-circle", css: "alert-error alert-soft") do
    Error alert.
```

URL: /examples/Daisy::Feedback::AlertComponent#soft-alerts

-- Example: Outline Alerts
Description:
Alerts can also be displayed in outline style.

Code (HAML):
```haml
= doc_example(title: "Outline Alerts", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    Alerts can also be displayed in outline style.



  = daisy_alert(icon: "information-circle", css: "alert-info alert-outline") do
    Info alert.

  = daisy_alert(icon: "check-circle", css: "alert-success alert-outline") do
    Success alert.

  = daisy_alert(icon: "exclamation-triangle", css: "alert-warning alert-outline") do
    Warning alert.

  = daisy_alert(icon: "exclamation-circle", css: "alert-error alert-outline") do
    Error alert.
```

URL: /examples/Daisy::Feedback::AlertComponent#outline-alerts

-- Example: Dash Alerts
Description:
Alerts can also be displayed in dash style.

Code (HAML):
```haml
= doc_example(title: "Dash Alerts", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    Alerts can also be displayed in dash style.



  = daisy_alert(icon: "information-circle", css: "alert-info alert-dash") do
    Info alert.

  = daisy_alert(icon: "check-circle", css: "alert-success alert-dash") do
    Success alert.

  = daisy_alert(icon: "exclamation-triangle", css: "alert-warning alert-dash") do
    Warning alert.

  = daisy_alert(icon: "exclamation-circle", css: "alert-error alert-dash") do
    Error alert.
```

URL: /examples/Daisy::Feedback::AlertComponent#dash-alerts

---

=== Component: Daisy::Feedback::LoadingComponent
Group: Feedback
Title: Loaders
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::LoadingComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/loaders.html.haml

Description:
 Loaders are used to indicate that a process is happening in the background. 

Helpers:
- daisy_loading
- daisy_loader

Examples:

-- Example: Basic Loaders
Description:
Loaders come in a variety of flavors! doc_note(css: "mb-8") do This component's helper method is also aliased as `daisy_loader` because we think it sounds better 😄 However, you **must** use the `loading` class names for the CSS to work properly.

Code (HAML):
```haml
= doc_example(title: "Basic Loaders", example_css: "gap-4") do |doc|
  - doc.with_description do
    %p
      Loaders come in a variety of flavors!

    = doc_note(css: "mb-8") do
      This component's helper method is also aliased as `daisy_loader`
because we think it sounds better 😄

However, you **must** use the `loading` class names for the CSS to work
properly.



  = daisy_loading(css: "loading-spinner text-primary")

  = daisy_loading(css: "loading-dots text-secondary")

  = daisy_loading(css: "loading-ring text-accent")

  = daisy_loader(css: "loading-ball text-info")

  = daisy_loader(css: "loading-bars text-success")

  = daisy_loader(css: "loading-infinity text-error")
```

URL: /examples/Daisy::Feedback::LoadingComponent#basic-loaders

---

=== Component: Daisy::Feedback::ProgressComponent
Group: Feedback
Title: Progress Bars
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::ProgressComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/progress_bars.html.haml

Description:
 Progress bars are used to indicate the progress of a process. 

Helpers:
- daisy_progress

Examples:

-- Example: Basic Bars
Description:
Progress bars utilize `value` and a `max` attributes to set the progress and can have various colors. By default, they take up the full width of their parent container. - `value` defaults to `nil` if not provided (for indeterminate bars) - `max` defaults to `100` if not provided to mimic percentages

Code (HAML):
```haml
= doc_example(title: "Basic Bars") do |doc|
  - doc.with_description do
    Progress bars utilize `value` and a `max` attributes to set the progress
and can have various colors. By default, they take up the full width of
their parent container.

- `value` defaults to `nil` if not provided (for indeterminate bars)
- `max` defaults to `100` if not provided to mimic percentages



  .flex.flex-col.gap-4.w-56
    = daisy_progress(value: 25)

    = daisy_progress(css: "progress-primary", value: 50, max: 100)

    = daisy_progress(css: "progress-secondary", value: 75, max: 100)

    = daisy_progress(css: "progress-accent", value: 100, max: 100)
```

URL: /examples/Daisy::Feedback::ProgressComponent#basic-bars

-- Example: Indeterminate Bars
Description:
Indeterminate progress bars are used when the progress is unknown. They animate to indicate that a process is currently happening.

Code (HAML):
```haml
= doc_example(title: "Indeterminate Bars") do |doc|
  - doc.with_description do
    Indeterminate progress bars are used when the progress is unknown. They
animate to indicate that a process is currently happening.



  .flex.flex-col.gap-4.w-56
    = daisy_progress(css: "progress-info")

    = daisy_progress(css: "progress-success [animation-delay:250ms]!")

    = daisy_progress(css: "progress-warning [animation-delay:500ms]!")

    = daisy_progress(css: "progress-error [animation-delay:750ms]!")
```

URL: /examples/Daisy::Feedback::ProgressComponent#indeterminate-bars

---

=== Component: Daisy::Feedback::RadialProgressComponent
Group: Feedback
Title: Radial Progress Bars
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::RadialProgressComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/radials.html.haml

Description:
 Radial progress bars are used to indicate the progress of a process, in a  circular fasion. 

Helpers:
- daisy_radial

Examples:

-- Example: Basic Radials
Description:
Radial progress bars utilize a `value` attribute to set the progress and can have various colors / sizes, along with content inside. The max is always set to 100 due to the way they are rendered, so `value` should always be a percentage of the bar that is filled.

Code (HAML):
```haml
= doc_example(title: "Basic Radials") do |doc|
  - doc.with_description do
    Radial progress bars utilize a `value` attribute to set the progress and
can have various colors / sizes, along with content inside.

The max is always set to 100 due to the way they are rendered, so `value`
should always be a percentage of the bar that is filled.



  = daisy_radial(value: 66) do
    66%
```

URL: /examples/Daisy::Feedback::RadialProgressComponent#basic-radials

-- Example: Fancy Radials
Description:
The content doesn't have to be just text!

Code (HAML):
```haml
= doc_example(title: "Fancy Radials", example_css: "gap-4") do |doc|
  - doc.with_description do
    The content doesn't have to be just text!



  = daisy_radial(value: 68) do
    = hero_icon("beaker", css: "size-8 text-primary")

  = daisy_radial(value: 52, css: "text-success") do
    52%

  = daisy_radial(value: 13, css: "text-error text-3xl") do
    😢
```

URL: /examples/Daisy::Feedback::RadialProgressComponent#fancy-radials

-- Example: Controlling Size & Color
Description:
You can also control the size, thickness, and color of the radial progress. - `size` defaults to `5rem` and **must** include units - `thickness` defaults to `calc(size / 10)` and **must** include units

Code (HAML):
```haml
= doc_example(title: "Controlling Size & Color", example_css: "gap-4") do |doc|
  - doc.with_description do
    You can also control the size, thickness, and color of the radial
progress.

- `size` defaults to `5rem` and **must** include units
- `thickness` defaults to `calc(size / 10)` and **must** include units



  = daisy_radial(value: 19, size: "15rem", thickness: "4px", css: "bg-primary text-primary-content text-3xl border-accent border-8") do
    19%
```

URL: /examples/Daisy::Feedback::RadialProgressComponent#controlling-size-color

---

=== Component: Daisy::Feedback::SkeletonComponent
Group: Feedback
Title: Skeletons
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::SkeletonComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/skeletons.html.haml

Description:
 Skeletons are used to show a placeholder for content that is loading or  being fetched. They are often used to give the user an idea of what the  content will look like before it is loaded. 

Helpers:
- daisy_skeleton

Examples:

-- Example: Basic Skeletons
Description:
Skeletons can take on various shapes and sizes that you define.

Code (HAML):
```haml
= doc_example(title: "Basic Skeletons") do |doc|
  - doc.with_description do
    Skeletons can take on various shapes and sizes that you define.



  .flex.flex-col.gap-4.w-72
    .flex.items-center.gap-4
      = daisy_skeleton(css: "size-24 rounded-full")

      = daisy_skeleton(css: "w-36 h-20")

    - 3.times do |i|
      - width = i.even? ? "w-36" : "w-48"

      = daisy_skeleton(css: "#{width} h-5")
```

URL: /examples/Daisy::Feedback::SkeletonComponent#basic-skeletons

-- Example: Component Skeletons
Description:
You can also use skeletons to show placeholders for components by adding the `skeleton` class to many existing components.

Code (HAML):
```haml
= doc_example(title: "Component Skeletons") do |doc|
  - doc.with_description do
    You can also use skeletons to show placeholders for components by adding
the `skeleton` class to many existing components.



  .flex.flex-col.gap-8
    .flex.items-center.gap-8
      = daisy_badge(css: "badge-lg skeleton text-base-content/50") do
        Loading...

      .w-12.md:w-48

      = daisy_button(css: "skeleton text-transparent") do
        Loading...

      = daisy_button(css: "skeleton text-transparent") do
        Loading...

    = daisy_alert(css: "skeleton")

    = daisy_chat do |chat|
      - chat.with_avatar(wrapper_css: "skeleton")

      - chat.with_bubble(css: "skeleton text-transparent") do
        Here is text that won't be displayed...

    = daisy_chat do |chat|
      - chat.with_avatar(wrapper_css: "skeleton")

      - chat.with_bubble(css: "skeleton text-transparent") do
        Here is a lot more text that won't be displayed and people can't see...

    = daisy_chat(css: "chat-end") do |chat|
      - chat.with_avatar(wrapper_css: "skeleton")

      - chat.with_bubble(css: "skeleton text-transparent") do
        Here is some more text that won't be displayed...
```

URL: /examples/Daisy::Feedback::SkeletonComponent#component-skeletons

---

=== Component: Daisy::Feedback::ToastComponent
Group: Feedback
Title: Toasts
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::ToastComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/toasts.html.haml

Description:
 Toasts are used to show a message to the user that is not critical, but  important. They are often used to show a message that is not part of the  main content flow and can be dismissed by the user. 

Helpers:
- daisy_toast

Examples:

-- Example: Basic Toasts
Description:
The Toast component is a simple wrapper that positions it's contents at various spots on the page, usually in the bottom right corner.

Code (HAML):
```haml
= doc_example(title: "Basic Toasts", example_css: "h-48") do |doc|
  - doc.with_description do
    The Toast component is a simple wrapper that positions it's contents at
various spots on the page, usually in the bottom right corner.



  These toasts will appear at the bottom right of the entire page.



  = daisy_toast(css: "z-10") do
    = daisy_alert(icon: "check-circle", css: "alert-success text-white") do
      Yay! Something went well!

    = daisy_alert(icon: "exclamation-circle", css: "alert-error text-white") do
      Uh oh! Something went wrong...
```

URL: /examples/Daisy::Feedback::ToastComponent#basic-toasts

---

=== Component: Daisy::Feedback::TooltipComponent
Group: Feedback
Title: Tooltips
API URL: /api-docs
Examples URL: /examples/Daisy::Feedback::TooltipComponent
File: /home/loco_demo/app/views/examples/daisy/feedback/tooltips.html.haml

Description:
Additionally, we offer the `daisy_tooltip` component to wrap an entire component, or any block of HTML, in a tooltip. This is also aliased as `daisy_tip` for convenience. 

Helpers:
- daisy_tooltip
- daisy_tip

Examples:

-- Example: Component Tooltips
Description:
Tooltips can be added to many components by setting the `tip` attribute or by adding the `tooltip` class and setting the `data-tip` attribute to the text you want to display.

Code (HAML):
```haml
= doc_example(title: "Component Tooltips") do |doc|
  - doc.with_description do
    Tooltips can be added to many components by setting the `tip` attribute or
by adding the `tooltip` class and setting the `data-tip` attribute to the
text you want to display.



  .flex.flex-col.gap-12
    .flex.items-center.justify-center.gap-8
      - # Just pass the `tip` argument to any component that supports it!

      = daisy_button(title: "Hover Me", css: "btn-primary", tip: "This is a tooltip!")

      = daisy_badge(title: "Hover Me", css: "badge-secondary", tip: "This is a badge tooltip!")

      = daisy_link(title: "Hover Me", css: "text-accent", tip: "This is a link tooltip!")

    - # You can also use the `tooltip` class / `data-tip` attribute directly

    = daisy_alert(css: "tooltip tooltip-bottom tooltip-success", html: { data: { tip: "This is an alert tooltip!" }}) do
      Here is an alert with a bottom tooltip!

    = daisy_chat do |chat|
      - chat.with_avatar(css: "tooltip-left tooltip-info", tip: "This is a left avatar tooltip!", src: image_path("avatars/lady-smiling-3.jpg"))

      - chat.with_bubble do
        - # Note that we can't add it directly to the bubble because it won't

        - # render properly. So we render it on the text instead and add some

        - # margin to make sure it's not inside of the bubble.

        .tooltip.tooltip-right.tooltip-primary.before:ml-4.after:ml-4{ data: { tip: "This is a right chat bubble tooltip!" } }
          Here is some text that also has a tooltip!
```

URL: /examples/Daisy::Feedback::TooltipComponent#component-tooltips

-- Example: Wrapper Tooltips
Description:
Sometimes, you may need to wrap the entire component in a tooltip. For this, we offer the very basic `daisy_tooltip` (or `daisy_tip`) component which has a `tip` argument to simplify your code a bit.

Code (HAML):
```haml
= doc_example(title: "Wrapper Tooltips") do |doc|
  - doc.with_description do
    Sometimes, you may need to wrap the entire component in a tooltip.

For this, we offer the very basic `daisy_tooltip` (or `daisy_tip`)
component which has a `tip` argument to simplify your code a bit.



  .flex.flex-col.gap-8
    - # Manual-style HAML tootlip

    .tooltip{ data: { tip: "This is a progress tooltip!" } }
      = daisy_progress(value: 42, css: "w-56")

    - # Standard helper method

    = daisy_tooltip("This is a radial tooltip!", css: "flex justify-center") do
      = daisy_radial(value: 58) do
        58%

    - # Aliased helper method

    = daisy_tip(css: "tooltip-bottom tooltip-info", tip: "We're loading a skeleton tooltip!") do
      = daisy_skeleton(css: "w-56 h-24")
```

URL: /examples/Daisy::Feedback::TooltipComponent#wrapper-tooltips

---

=== Component: Daisy::Layout::DividerComponent
Group: Layout
Title: Dividers
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::DividerComponent
File: /home/loco_demo/app/views/examples/daisy/layout/dividers.html.haml

Description:
Dividers are used to separate content in a meaningful way. They can be used to separate different sections of a page or to highlight a specific piece of content. They can be used both horizontally and vertically, and can include content if desired. 

Helpers:
- daisy_divider

Examples:

-- Example: Basic Dividers
Description:
The basic divider is a simple horizontal or vertical line that can be used to separate content.

Code (HAML):
```haml
= doc_example(title: "Basic Dividers", example_css: "flex-col gap-8") do |doc|
  - doc.with_description do
    The basic divider is a simple horizontal or vertical line that can be used
to separate content.



  .w-full{ class: "md:w-1/3" }
    - card_css = "border border-base-300 bg-base-100 shadow-xl text-center"

    = daisy_card(css: card_css) do
      Top Card

    = daisy_divider do
      OR

    = daisy_card(css: card_css) do
      Bottom Card

  .flex.grow
    - card_css = "h-48 border border-base-300 bg-base-100 shadow-xl"

    = daisy_card(css: card_css, body_css: "justify-center") do
      Left Card

    = daisy_divider(css: "divider-horizontal") do
      OR

    = daisy_card(css: card_css, body_css: "justify-center") do
      Right Card
```

URL: /examples/Daisy::Layout::DividerComponent#basic-dividers

-- Example: Colored Dividers
Description:
Dividers can be colored to match the theme of the page or to highlight specific content.

Code (HAML):
```haml
= doc_example(title: "Colored Dividers") do |doc|
  - doc.with_description do
    Dividers can be colored to match the theme of the page or to highlight
specific content.



  .w-full{ class: "md:w-1/2" }
    = daisy_divider do
      Default Divider

    = daisy_divider(css: "divider-neutral") do
      Neutral

    = daisy_divider(css: "divider-primary") do
      Primary

    = daisy_divider(css: "divider-secondary") do
      Secondary

    = daisy_divider(css: "divider-accent") do
      Accent

    = daisy_divider(css: "divider-success") do
      Success

    = daisy_divider(css: "divider-warning") do
      Warning

    = daisy_divider(css: "divider-info") do
      Info

    = daisy_divider(css: "divider-error") do
      Error
```

URL: /examples/Daisy::Layout::DividerComponent#colored-dividers

---

=== Component: Daisy::Layout::DrawerComponent
Group: Layout
Title: Drawers
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::DrawerComponent
File: /home/loco_demo/app/views/examples/daisy/layout/drawers.html.haml

Description:
doc_note(css: "mb-8 max-w-2xl") do The `relative / absolute` and the `h-48` classes allow the drawer to render properly inside the example preview box instead of the whole screen. 

Helpers:
- daisy_drawer

Examples:

-- Example: Basic Drawer
Description:
The basic drawer slides in from the left side of the screen.

Code (HAML):
```haml
= doc_example(title: "Basic Drawer", tab_content_css: "p-0!") do |doc|
  - doc.with_description do
    The basic drawer slides in from the left side of the screen.



  .relative.w-full.h-48
    = daisy_drawer do |drawer|
      - drawer.with_sidebar(css: "absolute h-48") do
        .bg-base-100.p-4.w-40.h-48
          Hello sidebar!

      = daisy_button(tag_name: "label", css: "m-8 btn btn-primary", title: "Open Drawer", html: { for: drawer.id })
```

URL: /examples/Daisy::Layout::DrawerComponent#basic-drawer

-- Example: Right Drawer
Description:
The right drawer slides in from the right side of the screen. doc_note(modifier: :warning, css: "mb-8", title: "Multiple Drawers") do Drawers are not designed to work with multiple on the page, so this example behaves a bit weirdly on desktop, showing both drawers.

Code (HAML):
```haml
= doc_example(title: "Right Drawer", tab_content_css: "p-0!") do |doc|
  - doc.with_description do
    The right drawer slides in from the right side of the screen.



    = doc_note(modifier: :warning, css: "mb-8", title: "Multiple Drawers") do
      Drawers are not designed to work with multiple on the page, so this
example behaves a bit weirdly on desktop, showing both drawers.



  .w-full.h-48
    = daisy_drawer(css: "drawer-end") do |drawer|
      - drawer.with_sidebar do
        - # The calc accounts for the top nav bar

        .w-48.bg-base-100.mt-16.p-4{ class: "h-[calc(100%-4rem)]" }
          Right sidebar!

      = daisy_button(tag_name: "label", css: "m-8 btn btn-primary", title: "Open Drawer", html: { for: drawer.id })
```

URL: /examples/Daisy::Layout::DrawerComponent#right-drawer

-- Example: Advanced Drawer
Description:
The advanced drawer slides in from the left side of the screen and has multiple ways to close it, along with some extra styling.

Code (HAML):
```haml
= doc_example(title: "Advanced Drawer", tab_content_css: "p-0!") do |doc|
  - doc.with_description do
    The advanced drawer slides in from the left side of the screen and has
multiple ways to close it, along with some extra styling.



  .relative.w-full.h-48
    = daisy_drawer do |drawer|
      - drawer.with_sidebar(css: "absolute h-48") do
        .bg-base-100.p-4.h-48
          .flex.items-center.justify-between.gap-8
            %h1.text-lg
              Sidenav Menu

            = daisy_button(tag_name: "label", css: "btn-sm btn-ghost", html: { for: drawer.id }) do
              = hero_icon(icon: "x-circle", css: "size-4")

          %ul.mt-4
            %li link_to "Home", "/", class: "link-hover"

            %li link_to "API Docs", "/api-docs", class: "link-hover", target: "_blank"

            %li link_to "GitHub", "https://github.com/profoundry-us/loco_motion", class: "link-hover", target: "_blank"

      .flex.items-center.justify-center.h-48
        
          %p.my-4
            Click the button below to open the drawer!

          = daisy_button(tag_name: "label", css: "btn btn-primary", title: "Open Drawer", html: { for: drawer.id })
```

URL: /examples/Daisy::Layout::DrawerComponent#advanced-drawer

---

=== Component: Daisy::Layout::FooterComponent
Group: Layout
Title: Footers
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::FooterComponent
File: /home/loco_demo/app/views/examples/daisy/layout/footers.html.haml

Description:
Footers are used to provide additional information about a page or site. They are vertical by default, but can be made horizontal using the `footer-horizontal` class. 

Helpers:
- daisy_footer

Examples:

-- Example: Basic Nav Footer (Vertical)
Description:
The basic nav footer is vertical by default, which works well for most mobile-first designs.

Code (HAML):
```haml
= doc_example(title: "Basic Nav Footer (Vertical)") do |doc|
  - doc.with_description do
    The basic nav footer is vertical by default, which works well for most
mobile-first designs.



  = daisy_footer(css: "max-w-64 bg-neutral text-neutral-content p-10") do
    %nav
      %h6.footer-title
        LocoMotion

      = link_to "Home", "/", class: "link-hover"

      = link_to "API Docs", "/api-docs", class: "link-hover", target: "_blank"

      = link_to "GitHub", "https://github.com/profoundry-us/loco_motion", class: "link-hover", target: "_blank"

    %nav
      %h6.footer-title
        My Neat Stuff

      = link_to "Abilene Smalltalk", "https://abilenesmalltalk.com/", class: "link-hover", target: "_blank"

      = link_to "Create. Connect. Heal.", "https://profoundry.us", class: "link-hover", target: "_blank"

      = link_to "Social Marketplace Secrets", "https://sm-secrets.com/", class: "link-hover", target: "_blank"

    %nav
      %h6.footer-title
        Other Neat Stuff

      = link_to "Ruby", "https://ruby-lang.org", class: "link-hover", target: "_blank"

      = link_to "Rails", "https://rubyonrails.org/", class: "link-hover", target: "_blank"

      = link_to "EGS", "https://www.egscomics.com/", class: "link-hover", target: "_blank"
```

URL: /examples/Daisy::Layout::FooterComponent#basic-nav-footer-vertical

-- Example: Horizontal Footer
Description:
Use the `footer-horizontal` class to make the footer horizontal. You can also use responsive variants like `lg:footer-horizontal` to make it horizontal only on larger screens.

Code (HAML):
```haml
= doc_example(title: "Horizontal Footer") do |doc|
  - doc.with_description do
    Use the `footer-horizontal` class to make the footer horizontal. You can also
use responsive variants like `lg:footer-horizontal` to make it horizontal
only on larger screens.



  = daisy_footer(css: "max-w-[1000px] mx-32 bg-base-200 text-base-content p-10 lg:footer-horizontal") do
    %nav
      %h6.footer-title
        Products

      = link_to "Features", "#", class: "link-hover"

      = link_to "Pricing", "#", class: "link-hover"

      = link_to "Enterprise", "#", class: "link-hover"

    %nav
      %h6.footer-title
        Support

      = link_to "Documentation", "#", class: "link-hover"

      = link_to "Help Center", "#", class: "link-hover"

      = link_to "Contact", "#", class: "link-hover"

    %nav
      %h6.footer-title
        Legal

      = link_to "Privacy", "#", class: "link-hover"

      = link_to "Terms", "#", class: "link-hover"

      = link_to "Cookie Policy", "#", class: "link-hover"
```

URL: /examples/Daisy::Layout::FooterComponent#horizontal-footer

-- Example: Center-Aligned Footer
Description:
A simple footer with centered content, perfect for copyright notices or minimal information.

Code (HAML):
```haml
= doc_example(title: "Center-Aligned Footer") do |doc|
  - doc.with_description do
    A simple footer with centered content, perfect for copyright notices or
minimal information.



  = daisy_footer(css: "max-w-[1000px] mx-32 justify-center p-4 bg-neutral text-neutral-content") do
    .text-xs
      ="Copyright &copy; #{Date.today.year} LocoMotion."

      All rights reserved.
```

URL: /examples/Daisy::Layout::FooterComponent#center-aligned-footer

---

=== Component: Daisy::Layout::HeroComponent
Group: Layout
Title: Heroes
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::HeroComponent
File: /home/loco_demo/app/views/examples/daisy/layout/heroes.html.haml

Description:
Heroes are used to provide a large, eye-catching introduction to a page or site. They are responsive by default and will stack on smaller screens. 

Helpers:
- daisy_hero

Examples:

-- Example: Basic Hero
Description:
The basic hero is a simple hero with a title and a subtitle.

Code (HAML):
```haml
= doc_example(title: "Basic Hero") do |doc|
  - doc.with_description do
    The basic hero is a simple hero with a title and a subtitle.



  = daisy_hero(css: "max-w-[1000px] mx-16 lg:mx-32 bg-base-200 text-center") do
    - # Heroes display their children in a flex column by default

    
      %h1.text-5xl.font-bold
        Hello, World!

      %p.my-6
        Welcome to the world of LocoMotion.

      = daisy_button(css: "btn btn-primary", title: "Get Started", href: "/")
```

URL: /examples/Daisy::Layout::HeroComponent#basic-hero

-- Example: Heroes with Images
Description:
You can add an image to the hero by making it the first or last child. You can also change the layout of the hero by passing a `content_wrapper_css` to make it responsive or add other styling.

Code (HAML):
```haml
= doc_example(title: "Heroes with Images", example_css: "flex-col! gap-8") do |doc|
  - doc.with_description do
    You can add an image to the hero by making it the first or last child.

You can also change the layout of the hero by passing a
`content_wrapper_css` to make it responsive or add other styling.



  = daisy_hero(css: "block max-w-[1000px] mx-16 py-8 lg:mx-32 bg-base-200") do
    %img.h-40.rounded{ src: image_path('landscapes/mountain-river.jpg'), alt: "Beautiful Landscape" }

    
      %h1.text-5xl.font-bold
        Hello, World!

      %p.my-6
        Welcome to the world of LocoMotion.

      = daisy_button(css: "btn btn-primary", title: "Get Started", href: "/")

  = daisy_hero(css: "block max-w-[1000px] mx-16 py-8 lg:mx-32 bg-base-200") do
    
      %h1.text-5xl.font-bold
        Hello, World!

      %p.my-6
        Welcome to the world of LocoMotion.

      = daisy_button(css: "btn btn-primary", title: "Get Started", href: "/")

    %img.h-40.rounded{ src: image_path('landscapes/mountain-river.jpg'), alt: "Beautiful Landscape" }
```

URL: /examples/Daisy::Layout::HeroComponent#heroes-with-images

-- Example: Hero with Overlay
Description:
The Hero component provides an `overlay` slot allowing you to add a semi-transparent background to the hero and apply other effects (such as a blur).

Code (HAML):
```haml
= doc_example(title: "Hero with Overlay", example_css: "px-16!") do |doc|
  - doc.with_description do
    The Hero component provides an `overlay` slot allowing you to add a
semi-transparent background to the hero and apply other effects (such as a
blur).



  - image = image_path("landscapes/mountain-river.jpg")

  - fix_for_v5_bug = "z-0"

  = daisy_hero(css: "max-w-[1000px] mx-16 lg:mx-32 bg-base-200 min-h-96", content_wrapper_css: fix_for_v5_bug, html: { style: "background-image: url('#{image}')" }) do |hero|
    - hero.with_overlay(css: "bg-black/50 backdrop-blur-xs")

    .text-white
      %h1.text-5xl.font-bold
        Hello, World!

      %p.my-6
        Welcome to the world of LocoMotion.

      = daisy_button(css: "btn btn-accent", title: "Get Started", href: "/")
```

URL: /examples/Daisy::Layout::HeroComponent#hero-with-overlay

---

=== Component: Daisy::Layout::IndicatorComponent
Group: Layout
Title: Indicators
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::IndicatorComponent
File: /home/loco_demo/app/views/examples/daisy/layout/indicators.html.haml

Description:
Indicators wrap other elements and provide a visual cue that something requires the user's attention. 

Helpers:
- daisy_indicator

Examples:

-- Example: Basic Indicator
Description:
By default, the indicator is positioned in the top right corner.

Code (HAML):
```haml
= doc_example(title: "Basic Indicator", example_css: "gap-8") do |doc|
  - doc.with_description do
    By default, the indicator is positioned in the top right corner.



  = daisy_indicator do |indicator|
    - indicator.with_item do
      = daisy_badge(title: "8", css: "badge-secondary text-xl")

    = daisy_avatar(wrapper_css: "rounded-xl", src: image_path("avatars/lady-smiling-2.jpg"))

  = daisy_indicator do |indicator|
    - indicator.with_item do
      = daisy_badge(title: "New!", css: "badge-accent")

    = daisy_button(title: "Checkout", left_icon: "shopping-cart")
```

URL: /examples/Daisy::Layout::IndicatorComponent#basic-indicator

-- Example: Different Positions
Description:
You can position the indicator in all of the usual places, and indicators can contain multiple items. Additionally, you can apply many of the DaisyUI classes directly to the indicator items, which might be required for the positioning to work correctly.

Code (HAML):
```haml
= doc_example(title: "Different Positions") do |doc|
  - doc.with_description do
    You can position the indicator in all of the usual places, and indicators
can contain multiple items.

Additionally, you can apply many of the DaisyUI classes directly to the
indicator items, which might be required for the positioning to work
correctly.



  - vertical = ["indicator-top", "indicator-middle", "indicator-bottom"]

  - horizontal = ["indicator-start", "indicator-center", "indicator-end"]

  = daisy_indicator do |indicator|
    - vertical.each do |v|
      - horizontal.each do |h|
        - title = "#{v.split("-").last.capitalize} #{h.split("-").last.capitalize}"

        - indicator.with_item(css: "badge badge-secondary #{v} #{h}") do
          = title

    .w-96.h-48.bg-base-200
```

URL: /examples/Daisy::Layout::IndicatorComponent#different-positions

---

=== Component: Daisy::Layout::JoinComponent
Group: Layout
Title: Joins
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::JoinComponent
File: /home/loco_demo/app/views/examples/daisy/layout/joins.html.haml

Description:
 Joins allow you to combine multiple components into a singular, joined,  element. This is useful for components like buttons, inputs with symbols,  and many more. 

Helpers:
- daisy_join

Examples:

-- Example: Button Joins
Description:
Buttons can be joined with other components to create a single, cohesive element. doc_note(css: "mb-8") do You **must** manually add the `.join-item` CSS class to the items you want to be joined since this could be complicated to do automatically in many use-cases and would almost certainly cause rendering issues.

Code (HAML):
```haml
= doc_example(title: "Button Joins", example_css: "flex-col space-y-8") do |doc|
  - doc.with_description do
    %p
      Buttons can be joined with other components to create a single, cohesive

      element.

    = doc_note(css: "mb-8") do
      You **must** manually add the `.join-item` CSS class to the items you
want to be joined since this could be complicated to do automatically in
many use-cases and would almost certainly cause rendering issues.



  = daisy_join do |join|
    - join.with_item do
      = daisy_button(title: "Start", css: "join-item")

    - join.with_item do
      = daisy_button(title: "Center", css: "join-item")

    - join.with_item do
      = daisy_button(title: "End", css: "join-item")

  = daisy_join do |join|
    - join.with_item do
      = daisy_button(icon: "bars-3-bottom-left", css: "join-item")

    - join.with_item do
      = daisy_button(icon: "bars-3", css: "join-item")

    - join.with_item do
      = daisy_button(icon: "bars-3-bottom-right", css: "join-item")
```

URL: /examples/Daisy::Layout::JoinComponent#button-joins

-- Example: Vertical Joins
Description:
You can also join elements vertically.

Code (HAML):
```haml
= doc_example(title: "Vertical Joins") do |doc|
  - doc.with_description do
    %p
      You can also join elements vertically.

  = daisy_join(css: "join-vertical min-w-32") do |join|
    - join.with_item do
      = daisy_button(title: "Top", css: "w-full join-item")

    - join.with_item do
      = daisy_button(title: "Middle", css: "w-full join-item btn-primary")

    - join.with_item do
      = daisy_button(title: "Bottom", css: "w-full join-item")
```

URL: /examples/Daisy::Layout::JoinComponent#vertical-joins

---

=== Component: Daisy::Layout::MaskComponent
Group: Layout
Title: Masks
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::MaskComponent
File: /home/loco_demo/app/views/examples/daisy/layout/masks.html.haml

Description:
Masks allow you to create a variety of shapes and patterns! Masks are **NOT** a full component, but rather a set of CSS classes that can be applied to many existing elements. 

Helpers:
- (No helper methods defined)

Examples:

-- Example: Basic Masks
Description:
Masks can be applied to many elements, and can be combined with other classes to create interesting effects.

Code (HAML):
```haml
= doc_example(title: "Basic Masks") do |doc|
  - doc.with_description do
    Masks can be applied to many elements, and can be combined with other
classes to create interesting effects.



  - masks = ["mask-squircle", "mask-heart", "mask-hexagon", "mask-hexagon-2", "mask-decagon", "mask-pentagon", "mask-diamond", "mask-square", "mask-circle", "mask-star", "mask-star-2", "mask-triangle", "mask-triangle-2", "mask-triangle-3", "mask-triangle-4"]

  .grid.grid-cols-4.gap-4
    - masks.each do |mask|
      = daisy_tip(mask) do
        = daisy_avatar(src: image_path("avatars/lady-smiling-2.jpg"), wrapper_css: "rounded-none mask #{mask}")
```

URL: /examples/Daisy::Layout::MaskComponent#basic-masks

-- Example: Masked Text
Description:
Masks can also be applied to HTML elements. Masks apply the `inline-block` display, so you may need to override that for certain cases.

Code (HAML):
```haml
= doc_example(title: "Masked Text") do |doc|
  - doc.with_description do
    Masks can also be applied to HTML elements. Masks apply the `inline-block`
display, so you may need to override that for certain cases.



  .flex.flex-col.justify-center.gap-8
    = daisy_button(left_icon: "shopping-bag", css: "btn-primary flex mask mask-diamond animate-pulse")

    .flex.items-center.justify-center.size-20.bg-red-600.hover:bg-green-600.text-white.mask.mask-decagon
      STOP
```

URL: /examples/Daisy::Layout::MaskComponent#masked-text

---

=== Component: Daisy::Layout::StackComponent
Group: Layout
Title: Stacks
API URL: /api-docs
Examples URL: /examples/Daisy::Layout::StackComponent
File: /home/loco_demo/app/views/examples/daisy/layout/stacks.html.haml

Description:
Stacks provide a 3 dimensional effect to your content. This can be helpful for displaying lots of notifications or other content in a small space. 

Helpers:
- daisy_stack

Examples:

-- Example: Basic Stack
Description:
A Stack is a wrapper around other elements, making it's children appear on top of each other. This example uses a slightly transparent background and some clever padding to show the effect.

Code (HAML):
```haml
= doc_example(title: "Basic Stack") do |doc|
  - doc.with_description do
    A Stack is a wrapper around other elements, making it's children appear on
top of each other.

This example uses a slightly transparent background and some clever
padding to show the effect.



  - card_css = "bg-base-100/80 border-base-300 border shadow-lg text-center"

  = daisy_stack do
    = daisy_card(css: card_css + " pb-2") do
      Top Stack

    = daisy_card(css: card_css + " py-1") do
      Middle Stack

    = daisy_card(css: card_css + " pt-2") do
      Bottom Stack
```

URL: /examples/Daisy::Layout::StackComponent#basic-stack

-- Example: Animated Stack
Description:
You can also do some slick effects with a library like succeed '!' do link_to "Anime.js", "https://animejs.com/", target: "_blank"

Code (HAML):
```haml
= doc_example(title: "Animated Stack", allow_reset: true) do |doc|
  - doc.with_description do
    %p
      You can also do some slick effects with a library like

      = succeed '!' do
        = link_to "Anime.js", "https://animejs.com/", target: "_blank"

  
    %p.my-4
      Click to remove and see the cards underneath.

    .flex.justify-center.items-center
      - card_css = "border-base-300 border shadow-lg cursor-pointer text-center transition-transform"

      - card_html = { onclick: "(function(el) { anime({ targets: el, duration: 400, easing: \"easeInQuad\", translateY: \"-100%\", opacity: 0, complete: function() { el.remove() } }) })(this)" }

      = daisy_stack do
        = daisy_card(css: card_css + " bg-primary text-white", html: card_html) do
          Stack 1

        = daisy_card(css: card_css + " bg-secondary text-white", html: card_html) do
          Stack 2

        = daisy_card(css: card_css + " bg-accent text-white", html: card_html) do
          Stack 3

        = daisy_card(css: card_css + " bg-neutral text-neutral-content cursor-default!") do
          Can't Remove Me!
```

URL: /examples/Daisy::Layout::StackComponent#animated-stack

-- Example: Stack Positions
Description:
DaisyUI 5 introduces directional stacking with `stack-top`, `stack-bottom`, `stack-start`, and `stack-end` CSS classes. These allow you to position the stacked elements in different directions.

Code (HAML):
```haml
= doc_example(title: "Stack Positions") do |doc|
  - doc.with_description do
    DaisyUI 5 introduces directional stacking with `stack-top`, `stack-bottom`, `stack-start`,
and `stack-end` CSS classes. These allow you to position the stacked elements in
different directions.



  .grid.grid-cols-2.gap-6
    .space-y-2
      %p.font-medium.text-center stack-top

      .stack.stack-top.size-28
        - card_css = "border-base-300 border shadow-lg text-center flex items-center justify-center"

        = daisy_card(css: card_css + " bg-primary text-white") do
          1

        = daisy_card(css: card_css + " bg-secondary text-white") do
          2

        = daisy_card(css: card_css + " bg-accent text-white") do
          3

    .space-y-2
      %p.font-medium.text-center stack-bottom

      .stack.stack-bottom.size-28
        - card_css = "border-base-300 border shadow-lg text-center flex items-center justify-center"

        = daisy_card(css: card_css + " bg-primary text-white") do
          1

        = daisy_card(css: card_css + " bg-secondary text-white") do
          2

        = daisy_card(css: card_css + " bg-accent text-white") do
          3

    .space-y-2
      %p.font-medium.text-center stack-start

      .stack.stack-start.size-28
        - card_css = "border-base-300 border shadow-lg text-center flex items-center justify-center"

        = daisy_card(css: card_css + " bg-primary text-white") do
          1

        = daisy_card(css: card_css + " bg-secondary text-white") do
          2

        = daisy_card(css: card_css + " bg-accent text-white") do
          3

    .space-y-2
      %p.font-medium.text-center stack-end

      .stack.stack-end.size-28
        - card_css = "border-base-300 border shadow-lg text-center flex items-center justify-center"

        = daisy_card(css: card_css + " bg-primary text-white") do
          1

        = daisy_card(css: card_css + " bg-secondary text-white") do
          2

        = daisy_card(css: card_css + " bg-accent text-white") do
          3
```

URL: /examples/Daisy::Layout::StackComponent#stack-positions

-- Example: CSS Stack
Description:
The Stack component simply applies the `stack` CSS class, which you can also use directly. You can also add spacing between stacked elements using gap utilities like `gap-3`. Adjust the slider below to see how different gap values affect the stack.

Code (HAML):
```haml
= doc_example(title: "CSS Stack") do |doc|
  - doc.with_description do
    The Stack component simply applies the `stack` CSS class, which you can
also use directly.

You can also add spacing between stacked elements using gap utilities like
`gap-3`. Adjust the slider below to see how different gap values affect
the stack.



  .space-y-4{ data: { controller: "stack-gap" } }
    .flex.items-center.gap-4
      %span.text-sm.min-w-16 Gap size:

      %span.badge.badge-primary{ data: { stack_gap_target: "label" } } 3

      %input.range.range-primary{
        type: "range",
        min: "1",
        max: "5",
        value: "3",
        data: { stack_gap_target: "slider", action: "input->stack-gap#updateGap" }
        }

    .stack.w-96{ data: { stack_gap_target: "stack", class: "gap-3" } }
      %img{ src: image_path("landscapes/beach.jpg") }

      %img{ src: image_path("landscapes/desert.jpg") }

      %img{ src: image_path("landscapes/forest.jpg") }
```

URL: /examples/Daisy::Layout::StackComponent#css-stack

---

=== Component: Daisy::Mockup::BrowserComponent
Group: Mockup
Title: Browsers
API URL: /api-docs
Examples URL: /examples/Daisy::Mockup::BrowserComponent
File: /home/loco_demo/app/views/examples/daisy/mockup/browsers.html.haml

Description:
 The Browser component is a simple mockup that looks and feels like a user's  web browser. This can be useful for creating mockups of web applications or  to showcase your own website in a more realistic setting. 

Helpers:
- daisy_browser

Examples:

-- Example: Basic Browser
Description:
A basic browser has very minimal styling and can include an optional toolbar.

Code (HAML):
```haml
= doc_example(title: "Basic Browser") do |doc|
  - doc.with_description do
    %p
      A basic browser has very minimal styling and can include an optional

      toolbar.

  = daisy_browser(css: "w-full max-w-4xl border border-base-300") do |browser|
    - browser.with_toolbar do
      %input.input.input-bordered{ placeholder: "https://loco-motion.io" }

    .border-t.border-base-300.px-4.py-16.text-center
      This is the content of the browser. You can put anything you want here.
```

URL: /examples/Daisy::Mockup::BrowserComponent#basic-browser

-- Example: Styled Browser
Description:
You can apply whatever styling you wish to make the browser stand out more.

Code (HAML):
```haml
= doc_example(title: "Styled Browser") do |doc|
  - doc.with_description do
    %p
      You can apply whatever styling you wish to make the browser stand out

      more.

  = daisy_browser(css: "w-full max-w-4xl bg-accent border border-accent") do |browser|
    - browser.with_toolbar do
      %input.input.input-bordered.border-accent-content{ class: "ml-4! pl-2! w-full!", placeholder: "https://loco-motion.io" }

    .bg-base-200.border-t.border-accent.px-4.py-16.text-center
      This browser mockup is very easy to style and make look however you want!
```

URL: /examples/Daisy::Mockup::BrowserComponent#styled-browser

---

=== Component: Daisy::Mockup::CodeComponent
Group: Mockup
Title: Code Blocks
API URL: /api-docs
Examples URL: /examples/Daisy::Mockup::CodeComponent
File: /home/loco_demo/app/views/examples/daisy/mockup/code_blocks.html.haml

Description:
 Code blocks are a great way to display code snippets in your site or  documentation. They can be used to show examples, provide context, or call  out something important. 

Helpers:
- daisy_code

Examples:

-- Example: Basic Code Block
Description:
A basic code block is just a simple block of code with no additional styling. You can pass the `prefix` attribute to add a prefix to the code block.

Code (HAML):
```haml
= doc_example(title: "Basic Code Block") do |doc|
  - doc.with_description do
    A basic code block is just a simple block of code with no additional
styling. You can pass the `prefix` attribute to add a prefix to the code
block.



  = daisy_code(prefix: "$") do
    echo "Hello, world!"
```

URL: /examples/Daisy::Mockup::CodeComponent#basic-code-block

-- Example: Multi-line Code Block
Description:
You can also create multi-line code blocks by calling the `with_line` slot multiple times.

Code (HAML):
```haml
= doc_example(title: "Multi-line Code Block") do |doc|
  - doc.with_description do
    You can also create multi-line code blocks by calling the `with_line`
slot multiple times.



  = daisy_code do |code|
    - code.with_line(prefix: "$") do
      yarn add @profoundry-us/loco_motion

    - code.with_line(prefix: ">", css: "text-warning") do
      Installing...

    - code.with_line(prefix: ">", css: "text-success") do
      Done!
```

URL: /examples/Daisy::Mockup::CodeComponent#multi-line-code-block

-- Example: Code Block with Language
Description:
You can also pass full code blocks by utilizing the `:plain` filter provided by HAML. doc_note(modifier: :warning, css: "mb-8") do You cannot use the `prefix` option and the `:plain` filter at the same time as we have to apply some special styling

Code (HAML):
```haml
= doc_example(title: "Code Block with Language") do |doc|
  - doc.with_description do
    You can also pass full code blocks by utilizing the `:plain` filter
provided by HAML.



    = doc_note(modifier: :warning, css: "mb-8") do
      You cannot use the `prefix` option and the `:plain` filter at the same
time as we have to apply some special styling



  = daisy_code do
    def hello_world
  puts "Hello, world!"
end
```

URL: /examples/Daisy::Mockup::CodeComponent#code-block-with-language

-- Example: Numbered / Highlighted Lines
Description:
You can also highlight specific lines in your code block by utilizing standard Tailwind classes.

Code (HAML):
```haml
= doc_example(title: "Numbered / Highlighted Lines") do |doc|
  - doc.with_description do
    You can also highlight specific lines in your code block by utilizing
standard Tailwind classes.



  = daisy_code do |code|
    - code.with_line(prefix: "1") do
      Here is the first line

    - code.with_line(prefix: "2") do
      Here is the second line

    - code.with_line(prefix: "3", css: "bg-warning text-warning-content") do
      Here is the third line

    - code.with_line(prefix: "4") do
      Here is the fourth line

    - code.with_line(prefix: "5", css: "bg-green-400 text-purple-600") do
      Here is the fifth line
```

URL: /examples/Daisy::Mockup::CodeComponent#numbered-highlighted-lines

---

=== Component: Daisy::Mockup::DeviceComponent
Group: Mockup
Title: Devices
API URL: /api-docs
Examples URL: /examples/Daisy::Mockup::DeviceComponent
File: /home/loco_demo/app/views/examples/daisy/mockup/devices.html.haml

Description:
Device mockups are a great way to showcase your designs in a realistic way! 

Helpers:
- daisy_device

Examples:

-- Example: Phone
Description:
You can use the `daisy_device` component to display a phone mockup.

Code (HAML):
```haml
= doc_example(title: "Phone") do |doc|
  - doc.with_description do
    You can use the `daisy_device` component to display a phone mockup.



  = daisy_device(css: "mockup-phone", display_css: "overflow-auto") do
    .flex.flex-col.gap-4.p-4.pt-12.pb-8.bg-base-100
      .text-center.text-2xl.font-bold
        Beautiful Landscapes

      = image_tag("landscapes/beach.jpg", class: "rounded-lg")

      = image_tag("landscapes/desert.jpg", class: "rounded-lg")

      = image_tag("landscapes/forest.jpg", class: "rounded-lg")

      = image_tag("landscapes/mountain-river.jpg", class: "rounded-lg")
```

URL: /examples/Daisy::Mockup::DeviceComponent#phone

-- Example: Tablet
Description:
You can also hide the camera by passing `show_camera: false` to the component and add any colors you want to the device.

Code (HAML):
```haml
= doc_example(title: "Tablet") do |doc|
  - doc.with_description do
    You can also hide the camera by passing `show_camera: false` to the
component and add any colors you want to the device.



  = daisy_device(css: "mockup-phone border-red-600", display_css: "overflow-auto w-[736px] h-[414px]", show_camera: false) do
    .flex.flex-col.gap-4.p-4.bg-base-100
      .text-center.text-2xl.font-bold
        Beautiful Landscapes

      = image_tag("landscapes/beach.jpg", class: "rounded-xl")

      = image_tag("landscapes/desert.jpg", class: "rounded-xl")

      = image_tag("landscapes/forest.jpg", class: "rounded-xl")

      = image_tag("landscapes/mountain-river.jpg", class: "rounded-xl")
```

URL: /examples/Daisy::Mockup::DeviceComponent#tablet

---

=== Component: Daisy::Mockup::FrameComponent
Group: Mockup
Title: Frames
API URL: /api-docs
Examples URL: /examples/Daisy::Mockup::FrameComponent
File: /home/loco_demo/app/views/examples/daisy/mockup/frames.html.haml

Description:
 Frames showcase your work as if they were displayed in a window. This is  very similar to the existing  component_link("Browser", "Daisy::Mockup::BrowserComponent")  component but without some of the additional styling such as the browser  toolbar. 

Helpers:
- daisy_frame

Examples:

-- Example: Basic Frame
Description:
A basic frame is just a simple frame with no additional styling.

Code (HAML):
```haml
= doc_example(title: "Basic Frame") do |doc|
  - doc.with_description do
    A basic frame is just a simple frame with no additional styling.



  = daisy_frame(css: "w-full max-w-4xl border border-base-300") do |frame|
    .border-t.border-base-300.px-4.py-16.text-center
      This is the content of the browser. You can put anything you want here.

      .mt-4
        = image_tag("landscapes/desert.jpg", class: "max-w-lg mx-auto rounded-xl")
```

URL: /examples/Daisy::Mockup::FrameComponent#basic-frame

-- Example: Styled Frame
Description:
You can style the frames however you like!

Code (HAML):
```haml
= doc_example(title: "Styled Frame") do |doc|
  - doc.with_description do
    You can style the frames however you like!



  = daisy_frame(css: "w-full max-w-4xl bg-purple-200 border border-purple-600") do |frame|
    .bg-base-100.border-t.border-purple-300.px-4.py-16.text-center
      = image_tag("loco-logo.png", class: "max-w-xs mx-auto")

      %p
        This is the content of the browser. You can put anything you want here.

      .mt-4
        = image_tag("landscapes/desert.jpg", class: "max-w-lg mx-auto rounded-xl")
```

URL: /examples/Daisy::Mockup::FrameComponent#styled-frame

---

