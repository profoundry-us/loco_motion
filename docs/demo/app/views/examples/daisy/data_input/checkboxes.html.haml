= doc_title(title: "Checkboxes", comp: @comp) do |title|
  :markdown
    Checkboxes are great for toggling an item on / off, or selecting multiple
    items from a list.


= doc_example(title: "Basic Checkboxes") do |doc|
  - doc.with_description do
    :markdown
      Basic checkboxes can be rendered with or without a label through standard
      heirarchy means, or by utilizing the `start_label` and `end_label`
      wrappers.

    = doc_note(css: "mb-8") do
      :markdown
        While the
        <a href="#{doc_url("LocoMotion/Concerns/LabelableComponent")}" target="_blank">LabelableComponent</a>
        Concern does implement a `floating_label` attribute, it is not relevant
        for checkboxes and does not render properly.

  .my-2.flex.flex-col.gap-4
    %div
      = daisy_checkbox(name: "checkbox0", id: "checkbox0")
      .tooltip.tooltip-warning{ "data-tip": "CANNOT click the text to activate" }
        Checkbox with no label

    = daisy_label(for: "checkbox1") do
      = daisy_checkbox(name: "checkbox1", id: "checkbox1")
      Checkbox with regular label

    = daisy_checkbox(name: "checkbox2", id: "checkbox2", checked: true, end_label: "Checkbox using end_label wrapper")

    = daisy_checkbox(name: "checkbox2", id: "checkbox2", checked: true, start_label: "Checkbox using start_label wrapper")

    = daisy_checkbox(name: "checkbox2", id: "checkbox2", checked: true) do |checkbox|
      - checkbox.with_end_label do
        :markdown
          Checkbox using `end_label` block

    = daisy_checkbox(name: "checkbox2", id: "checkbox2", checked: true) do |checkbox|
      - checkbox.with_start_label(css: "flex flex-row items-center") do
        Using
        = daisy_badge(css: "mx-1 badge-secondary badge-sm") do
          start_label
      - checkbox.with_end_label(css: "flex flex-row items-center") do
        and
        = daisy_badge(css: "mx-1 badge-secondary badge-sm") do
          end_label
        blocks


= doc_example(title: "Checkboxes with Colors") do |doc|
  - doc.with_description do
    :markdown
      Checkboxes can be styled with different colors using DaisyUI's color classes.

    = doc_note(css: "mb-8") do
      :markdown
        Uses `checkbox-neutral` with dark backgrounds for dark mode compatibility.

  .my-2.grid.grid-cols-1.md:grid-cols-2.gap-4
    = daisy_label(for: "checkbox-dark") do
      = daisy_checkbox(name: "checkbox-dark", id: "checkbox-dark", css: "checkbox-neutral theme-controller", value: "dark", checked: false)
      Enable Dark Mode

    = daisy_label(for: "checkbox3") do
      = daisy_checkbox(name: "checkbox3", css: "checkbox-primary", id: "checkbox3", checked: true)
      Primary

    = daisy_label(for: "checkbox4") do
      = daisy_checkbox(name: "checkbox4", css: "checkbox-secondary", id: "checkbox4", checked: true)
      Secondary

    = daisy_label(for: "checkbox5") do
      = daisy_checkbox(name: "checkbox5", css: "checkbox-accent", id: "checkbox5", checked: true)
      Accent

    = daisy_label(for: "checkbox6") do
      = daisy_checkbox(name: "checkbox6", css: "checkbox-success", id: "checkbox6", checked: true)
      Success

    = daisy_label(for: "checkbox7") do
      = daisy_checkbox(name: "checkbox7", css: "checkbox-warning", id: "checkbox7", checked: true)
      Warning

    = daisy_label(for: "checkbox8") do
      = daisy_checkbox(name: "checkbox8", css: "checkbox-error", id: "checkbox8", checked: true)
      Error

    = daisy_label(for: "checkbox9") do
      = daisy_checkbox(name: "checkbox9", css: "checkbox-info", id: "checkbox9", checked: true)
      Info


= doc_example(title: "Checkbox Sizes") do |doc|
  - doc.with_description do
    :markdown
      Checkboxes come in different sizes using DaisyUI's size classes.

  .my-2.flex.flex-col.gap-8
    .flex.items-center.gap-4
      = daisy_label(for: "checkbox-xs") do
        = daisy_checkbox(name: "checkbox-xs", id: "checkbox-xs", css: "checkbox-xs", checked: true)
        Extra Small

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox-sm") do
        = daisy_checkbox(name: "checkbox-sm", id: "checkbox-sm", css: "checkbox-sm", checked: true)
        Small

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox-md") do
        = daisy_checkbox(name: "checkbox-md", id: "checkbox-md", css: "checkbox-md", checked: true)
        Medium (Default)

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox-lg") do
        = daisy_checkbox(name: "checkbox-lg", id: "checkbox-lg", css: "checkbox-lg", checked: true)
        Large

    .flex.items-center.gap-4
      = daisy_label(for: "checkbox-xl") do
        = daisy_checkbox(name: "checkbox-xl", id: "checkbox-xl", css: "checkbox-xl", checked: true)
        Extra Large


= doc_example(title: "Combining Colors and Sizes") do |doc|
  - doc.with_description do
    :markdown
      You can combine both color and size classes to create different variations.

  .my-2.grid.grid-cols-1.md:grid-cols-2.lg:grid-cols-3.gap-6
    = daisy_label(for: "checkbox-primary-sm") do
      = daisy_checkbox(name: "checkbox-primary-sm", id: "checkbox-primary-sm", css: "checkbox-primary checkbox-sm", checked: true)
      Primary Small

    = daisy_label(for: "checkbox-secondary-md") do
      = daisy_checkbox(name: "checkbox-secondary-md", id: "checkbox-secondary-md", css: "checkbox-secondary checkbox-md", checked: true)
      Secondary Medium

    = daisy_label(for: "checkbox-accent-lg") do
      = daisy_checkbox(name: "checkbox-accent-lg", id: "checkbox-accent-lg", css: "checkbox-accent checkbox-lg", checked: true)
      Accent Large

    = daisy_label(for: "checkbox-info-sm") do
      = daisy_checkbox(name: "checkbox-info-sm", id: "checkbox-info-sm", css: "checkbox-info checkbox-sm", checked: true)
      Info Small

    = daisy_label(for: "checkbox-success-md") do
      = daisy_checkbox(name: "checkbox-success-md", id: "checkbox-success-md", css: "checkbox-success checkbox-md", checked: true)
      Success Medium

    = daisy_label(for: "checkbox-warning-lg") do
      = daisy_checkbox(name: "checkbox-warning-lg", id: "checkbox-warning-lg", css: "checkbox-warning checkbox-lg", checked: true)
      Warning Large


= doc_example(title: "Indeterminate State") do |doc|
  - doc.with_description do
    :markdown
      Checkboxes can be set to an indeterminate state to indicate that the value
      is neither checked nor unchecked. This is commonly used for "Select All"
      checkboxes where some items are selected and others are not.

    = doc_note(css: "mb-8") do
      :markdown
        You must set the `indeterminate` attribute through JavaScript.

  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "checkbox10") do
      = daisy_checkbox(name: "checkbox10", id: "checkbox10")
      Indeterminate Checkbox

    :javascript
      document.getElementById("checkbox10").indeterminate = true;


= doc_example(title: "Disabled Checkbox") do |doc|
  - doc.with_description do
    :markdown
      Checkboxes can be disabled using the `disabled` attribute.

  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "checkbox11", css: "cursor-not-allowed") do
      = daisy_checkbox(name: "checkbox11", id: "checkbox11", disabled: true)
      Disabled Checkbox


= doc_example(title: "Custom ID") do |doc|
  - doc.with_description do
    :markdown
      Checkboxes can be given a custom ID using the `id` attribute.

  .my-2.flex.flex-col.gap-4
    = daisy_label(for: "custom-checkbox") do
      = daisy_checkbox(name: "checkbox12", id: "custom-checkbox")
      Checkbox with Custom ID


= doc_example(title: "Rails Form Example") do |doc|
  - doc.with_description do
    :markdown
      The built-in Form Builder extension provides an even easier way to use
      checkboxes in your pages by extracting the name and ID attributes from the
      form object and attributes.

  = form_with(url: "#", method: :post, scope: :user, class: "flex flex-col gap-8") do |form|
    = form.daisy_label(:terms) do
      = form.daisy_checkbox(:terms)
      I accept the terms and conditions

    = form.daisy_checkbox(:newsletter, toggle: true, end_label: "Subscribe to newsletter")


= doc_example(title: "Using Label Component") do |doc|
  - doc.with_description do
    :markdown
      You can also use the Label component directly with checkboxes for fully
      custom approach.

  .my-2.flex.flex-col.gap-4
    .flex.items-center.space-x-2
      = daisy_checkbox(name: "agree", id: "agree")
      = daisy_label(for: "agree", title: "I agree to the terms of service")

    .flex.items-center.space-x-2.mt-4
      = daisy_checkbox(name: "updates", id: "updates", toggle: true)
      = daisy_label(for: "updates") do
        Receive product updates
